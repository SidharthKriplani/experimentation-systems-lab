// Product Analytics Lab — SQL Lab Problem Bank
// Single quotes only. No backticks. Apostrophes escaped as \'.
// Each problem references datamartId — no per-problem schema or seed.
// expectedRowCount and checkValues are verified against actual datamart seed data.

export const sqlLabProblems = [

  // ─── EASY (12 problems, first 3 isFree) ────────────────────────────────────

  {
    id: 'sql-e01',
    title: 'Re-engagement Targets',
    company: 'Amazon',
    companyDomain: 'amazon.com',
    difficulty: 'Easy',
    isFree: true,
    tags: ['anti-join', 'LEFT JOIN', 'NULL handling'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 5,
    datamartId: 'ecomm',
    prompt: 'Marketing wants to run a re-engagement email campaign. They need a list of customers who signed up but have never placed any order. Return user_id and email, ordered by user_id.',
    expectedColumns: ['user_id', 'email'],
    expectedRowCount: 3,
    checkValues: [{ user_id: '13', email: 'mia@example.com' }],
    solution: 'SELECT u.user_id, u.email\nFROM users u\nLEFT JOIN orders o ON u.user_id = o.user_id\nWHERE o.order_id IS NULL\nORDER BY u.user_id',
    debrief: 'The classic anti-join pattern: LEFT JOIN then filter WHERE the right side IS NULL. A common wrong answer uses NOT IN (SELECT user_id FROM orders) — which silently returns zero rows if orders contains a single NULL user_id. The LEFT JOIN approach is null-safe and production-ready. Three users qualify: mia (13), ned (14), and ora (15). The interviewer follow-up that exposes the weak answer: "What happens to your query if one order has a NULL user_id?" NOT IN with NULLs breaks; LEFT JOIN does not.',
    sqliteNote: null,
  },

  {
    id: 'sql-e02',
    title: 'Session Conversion by Source',
    company: 'Shopify',
    companyDomain: 'shopify.com',
    difficulty: 'Easy',
    isFree: true,
    tags: ['GROUP BY', 'aggregation', 'conversion rate', 'SUM'],
    roles: ['PA', 'DA', 'BA'],
    priority: 1,
    estimatedMin: 5,
    datamartId: 'ecomm',
    prompt: 'The growth team wants to know which traffic source drives the highest purchase conversion rate. Using the sessions table, return each source with total_sessions, conversions, and conversion_pct (rounded to 1 decimal). Order by conversion_pct descending.',
    expectedColumns: ['source', 'total_sessions', 'conversions', 'conversion_pct'],
    expectedRowCount: 5,
    checkValues: [{ source: 'referral' }],
    solution: 'SELECT source,\n  COUNT(*) AS total_sessions,\n  SUM(converted) AS conversions,\n  ROUND(100.0 * SUM(converted) / COUNT(*), 1) AS conversion_pct\nFROM sessions\nGROUP BY source\nORDER BY conversion_pct DESC',
    debrief: 'Referral converts at 100% (3/3 sessions) — the highest-performing source, but also the smallest. The trap is using AVG(converted) instead of an explicit SUM/COUNT ratio. AVG works numerically here since converted is 0/1, but it hides your intent and signals weak SQL fluency in an interview. Always write the explicit formula. Social has 0% conversion — every session bounced. That is a budget decision hiding in the data.',
    sqliteNote: null,
  },

  {
    id: 'sql-e03',
    title: 'Free-Plan Accounts for Upsell',
    company: 'Salesforce',
    companyDomain: 'salesforce.com',
    difficulty: 'Easy',
    isFree: true,
    tags: ['multi-table JOIN', 'filter', 'tier logic'],
    roles: ['PA', 'DA', 'PM', 'BA'],
    priority: 1,
    estimatedMin: 5,
    datamartId: 'saas',
    prompt: 'Your CSM team wants to identify accounts currently on the free plan and prioritize them for upgrade outreach. Return account_id and company_name for all accounts with an active free-tier subscription, ordered by account_id.',
    expectedColumns: ['account_id', 'company_name'],
    expectedRowCount: 2,
    checkValues: [{ company_name: 'India Foods' }],
    solution: 'SELECT a.account_id, a.company_name\nFROM accounts a\nJOIN subscriptions s ON a.account_id = s.account_id\nJOIN plans p ON s.plan_id = p.plan_id\nWHERE p.tier = \'free\' AND s.status = \'active\'\nORDER BY a.account_id',
    debrief: 'The weak answer filters on mrr = 0 or plan name = \'Starter\' — which breaks the moment the plan is renamed or repriced. Filtering on p.tier = \'free\' is the stable business-logic approach. The second common mistake: forgetting s.status = \'active\', which would include accounts that have already churned off the free plan. Two accounts qualify: India Foods (9) and Lima Education (12).',
    sqliteNote: null,
  },

  {
    id: 'sql-e04',
    title: 'Active MRR by Account',
    company: 'HubSpot',
    companyDomain: 'hubspot.com',
    difficulty: 'Easy',
    isFree: false,
    tags: ['JOIN', 'filter', 'ORDER BY', 'revenue'],
    roles: ['PA', 'DA', 'BA'],
    priority: 1,
    estimatedMin: 5,
    datamartId: 'saas',
    prompt: 'Finance is building a revenue dashboard. Return company_name and current mrr for every account with an active subscription, ordered from highest MRR to lowest.',
    expectedColumns: ['company_name', 'mrr'],
    expectedRowCount: 12,
    checkValues: [{ company_name: 'Golf Logistics' }],
    solution: 'SELECT a.company_name, s.mrr\nFROM accounts a\nJOIN subscriptions s ON a.account_id = s.account_id\nWHERE s.status = \'active\'\nORDER BY s.mrr DESC',
    debrief: 'The most common mistake: using ended_at IS NULL instead of status = \'active\'. These are not equivalent — a paused subscription may have ended_at IS NULL but should not appear in a revenue report. Always filter on the explicit status column. Three accounts sit at 2999 MRR (Acme, Echo Tech, Kilo Pharma); Golf Logistics is at 999. Accounts 9 and 12 show 0 MRR — they are on the free plan and are your primary upsell targets.',
    sqliteNote: null,
  },

  {
    id: 'sql-e05',
    title: 'High-Risk Account Exposure',
    company: 'Stripe',
    companyDomain: 'stripe.com',
    difficulty: 'Easy',
    isFree: false,
    tags: ['JOIN', 'filter', 'risk classification'],
    roles: ['DA', 'PA'],
    priority: 2,
    estimatedMin: 5,
    datamartId: 'fintech',
    prompt: 'The risk team needs to audit all accounts associated with high-risk users. Return account_id, account_type, and account status for each such account, ordered by account_id.',
    expectedColumns: ['account_id', 'account_type', 'status'],
    expectedRowCount: 2,
    checkValues: [{ account_id: '6', account_type: 'checking' }],
    solution: 'SELECT a.account_id, a.account_type, a.status\nFROM accounts a\nJOIN users u ON a.user_id = u.user_id\nWHERE u.risk_tier = \'high\'\nORDER BY a.account_id',
    debrief: 'Account 6 (user 4, frozen) and account 12 (user 9, closed) are the two high-risk accounts. The weak answer queries only accounts.status to find suspicious accounts — missing that the risk classification lives on the user, not the account. The JOIN on users is required. The interviewer follow-up: "What if a high-risk user opened three accounts?" — this query handles it correctly by returning all accounts per user, while a subquery on just accounts.user_id would miss the risk_tier join entirely.',
    sqliteNote: null,
  },

  {
    id: 'sql-e06',
    title: 'Open Dispute Queue',
    company: 'PayPal',
    companyDomain: 'paypal.com',
    difficulty: 'Easy',
    isFree: false,
    tags: ['filter', 'NULL handling', 'IS NULL'],
    roles: ['DA', 'PA', 'BA'],
    priority: 2,
    estimatedMin: 5,
    datamartId: 'fintech',
    prompt: 'Compliance reviews all unresolved disputes daily. Return dispute_id, txn_id, amount, and opened_at for all disputes that have not yet been resolved. Order by opened_at ascending.',
    expectedColumns: ['dispute_id', 'txn_id', 'amount', 'opened_at'],
    expectedRowCount: 3,
    checkValues: [{ dispute_id: '3' }],
    solution: 'SELECT dispute_id, txn_id, amount, opened_at\nFROM disputes\nWHERE resolved_at IS NULL\nORDER BY opened_at',
    debrief: 'Three disputes are unresolved: 3 ($89.99), 4 ($490.00), and 6 ($3,500.00). The classic NULL trap: WHERE resolved_at = NULL returns zero rows because NULL comparisons with = evaluate to NULL (unknown), not TRUE. IS NULL is the only correct predicate. Dispute 6 — the largest at $3,500 — is the most time-sensitive and should surface at the top of a real compliance queue. The follow-up: "How would you add a column for days open?" requires julianday() arithmetic in SQLite.',
    sqliteNote: null,
  },

  {
    id: 'sql-e07',
    title: 'Disengaged Users',
    company: 'TikTok',
    companyDomain: 'tiktok.com',
    difficulty: 'Easy',
    isFree: false,
    tags: ['anti-join', 'LEFT JOIN', 'NULL handling', 'engagement'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 5,
    datamartId: 'consumer',
    prompt: 'The growth team wants to identify registered users who have never engaged with any content. Return user_id and username for all zero-interaction users, ordered by user_id.',
    expectedColumns: ['user_id', 'username'],
    expectedRowCount: 2,
    checkValues: [{ user_id: '14', username: 'nina' }],
    solution: 'SELECT u.user_id, u.username\nFROM users u\nLEFT JOIN interactions i ON u.user_id = i.user_id\nWHERE i.interaction_id IS NULL\nORDER BY u.user_id',
    debrief: 'Users 14 (nina) and 15 (oscar) have never interacted with any content. The anti-join pattern appears again — the domain changes but the SQL structure is identical to the "no orders" problem. Recognizing this pattern across contexts is the signal of a fluent analyst. The NOT EXISTS alternative is logically correct but slower at scale. The weak answer adds a HAVING COUNT(i.interaction_id) = 0 after a GROUP BY — syntactically wrong without the LEFT JOIN.',
    sqliteNote: null,
  },

  {
    id: 'sql-e08',
    title: 'Top-Performing Content',
    company: 'YouTube',
    companyDomain: 'youtube.com',
    difficulty: 'Easy',
    isFree: false,
    tags: ['GROUP BY', 'COUNT', 'ORDER BY', 'LIMIT'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 5,
    datamartId: 'consumer',
    prompt: 'The editorial team wants to double down on what works. Which single content piece has received the most interactions? Return its content_id and total interaction_count.',
    expectedColumns: ['content_id', 'interaction_count'],
    expectedRowCount: 1,
    checkValues: [{ content_id: '1', interaction_count: '8' }],
    solution: 'SELECT content_id, COUNT(*) AS interaction_count\nFROM interactions\nGROUP BY content_id\nORDER BY interaction_count DESC\nLIMIT 1',
    debrief: 'Content piece 1 leads with 8 interactions — engaged by users 1 through 8 across view, like, share, save, and comment actions. The weak answer returns all content pieces ranked, missing the LIMIT 1. A strong answer joins to the content table to surface the category (fitness) and content type (video) alongside the ID — the editorial team needs context, not just a number. The follow-up: "What if two pieces tied?" — LIMIT 1 picks arbitrarily; RANK() OVER (ORDER BY ...) handles ties correctly.',
    sqliteNote: null,
  },

  {
    id: 'sql-e09',
    title: 'Provider No-Show Rate',
    company: 'Zocdoc',
    companyDomain: 'zocdoc.com',
    difficulty: 'Easy',
    isFree: false,
    tags: ['GROUP BY', 'SUM', 'COUNT', 'ROUND', 'JOIN', 'rate calculation'],
    roles: ['DA', 'PA', 'BA'],
    priority: 2,
    estimatedMin: 8,
    datamartId: 'health',
    prompt: 'Operations is frustrated with provider no-shows and wants to know who the worst offender is. Which provider has the highest no-show rate, and what does the underlying appointment data look like?',
    expectedColumns: ['name', 'total_appts', 'no_shows', 'no_show_rate'],
    expectedRowCount: 1,
    checkValues: [{ name: 'Dr. Smith' }],
    solution: 'SELECT p.name,\n  COUNT(*) AS total_appts,\n  SUM(a.no_show) AS no_shows,\n  ROUND(100.0 * SUM(a.no_show) / COUNT(*), 1) AS no_show_rate\nFROM appointments a\nJOIN providers p ON a.provider_id = p.provider_id\nGROUP BY p.provider_id, p.name\nORDER BY no_show_rate DESC\nLIMIT 1',
    debrief: 'Dr. Smith has a 40% no-show rate (4 out of 10 appointments) — the highest in the practice. GROUP BY p.provider_id, p.name is the correct form: grouping on name alone would incorrectly merge two providers who share a name. AVG(no_show) is numerically equivalent here since no_show is 0/1, but the SUM/COUNT idiom is the interviewer-preferred form because it is explicit and generalizes. The follow-up: "Is 40% actually high? What context do you need?" — benchmarks by specialty, appointment type mix, patient risk factors.',
    sqliteNote: null,
  },

  {
    id: 'sql-e10',
    title: 'Most Prescribed Drug',
    company: 'CVS Health',
    companyDomain: 'cvshealth.com',
    difficulty: 'Easy',
    isFree: false,
    tags: ['GROUP BY', 'COUNT', 'ORDER BY', 'LIMIT'],
    roles: ['DA', 'BA', 'PA'],
    priority: 2,
    estimatedMin: 5,
    datamartId: 'health',
    prompt: 'The pharmacy analytics team is reviewing prescribing patterns to spot formulary optimization opportunities. Which drug has been prescribed most frequently? Return drug_name and prescription_count.',
    expectedColumns: ['drug_name', 'prescription_count'],
    expectedRowCount: 1,
    checkValues: [{ drug_name: 'Lisinopril', prescription_count: '5' }],
    solution: 'SELECT drug_name, COUNT(*) AS prescription_count\nFROM prescriptions\nGROUP BY drug_name\nORDER BY prescription_count DESC\nLIMIT 1',
    debrief: 'Lisinopril leads with 5 prescriptions (rxs 1, 3, 8, 12, 16) — all written for hypertension management. The weak answer groups by patient_id instead of drug_name, answering "who has the most prescriptions" rather than "what is the most prescribed drug." These are different questions. Strong follow-up: join to providers to see which provider drives the most Lisinopril volume. Also note: this counts prescription events. If the question were "how many distinct patients are on Lisinopril?" you need COUNT(DISTINCT patient_id).',
    sqliteNote: null,
  },

  {
    id: 'sql-e11',
    title: 'Products That Never Sold',
    company: 'eBay',
    companyDomain: 'ebay.com',
    difficulty: 'Easy',
    isFree: false,
    tags: ['anti-join', 'LEFT JOIN', 'NULL handling', 'inventory'],
    roles: ['DA', 'PA', 'BA'],
    priority: 2,
    estimatedMin: 5,
    datamartId: 'ecomm',
    prompt: 'The warehouse team is auditing inventory. Which products have never appeared in any order? Return product_id, name, category, and is_active for all such products.',
    expectedColumns: ['product_id', 'name', 'category', 'is_active'],
    expectedRowCount: 1,
    checkValues: [{ name: 'Plant Pot' }],
    solution: 'SELECT p.product_id, p.name, p.category, p.is_active\nFROM products p\nLEFT JOIN order_items oi ON p.product_id = oi.product_id\nWHERE oi.item_id IS NULL\nORDER BY p.product_id',
    debrief: 'Only Product 10 (Plant Pot, home, is_active=0) has never appeared in an order. The weak answer filters on is_active = 0 first — that finds discontinued products, not unordered products. These overlap here but diverge in production: a product can be active but unsold, or discontinued but historically popular. The LEFT JOIN on order_items with WHERE item_id IS NULL is the correct anti-join. Business implication: Plant Pot was never sold and is already marked inactive — safe to clear from inventory.',
    sqliteNote: null,
  },

  {
    id: 'sql-e12',
    title: 'Users Who Never Logged In',
    company: 'Mixpanel',
    companyDomain: 'mixpanel.com',
    difficulty: 'Easy',
    isFree: false,
    tags: ['NULL handling', 'IS NULL', 'JOIN', 'user activation'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 5,
    datamartId: 'saas',
    prompt: 'Customer success wants to flag users who have never logged in since being added to their account. Return email and company_name for all such users, ordered by user_id.',
    expectedColumns: ['email', 'company_name'],
    expectedRowCount: 2,
    checkValues: [{ email: 'viewer2@retail.com' }],
    solution: 'SELECT u.email, a.company_name\nFROM users u\nJOIN accounts a ON u.account_id = a.account_id\nWHERE u.last_active_at IS NULL\nORDER BY u.user_id',
    debrief: 'Two users have last_active_at IS NULL: viewer2@retail.com (Blue Retail) and viewer4@delta.com (Delta Health). Both are viewer-role users — a pattern worth flagging: viewer seats are often provisioned and forgotten. The classic NULL trap: WHERE last_active_at = NULL returns zero rows because = NULL evaluates to NULL in SQL, not TRUE. IS NULL is the only correct predicate. The follow-up: "How would you find users inactive for 90+ days?" requires a date comparison with a reference date.',
    sqliteNote: null,
  },


  // ─── MEDIUM (10 problems) ──────────────────────────────────────────────────

  {
    id: 'sql-m01',
    title: 'Detect Account Upgrades',
    company: 'Gainsight',
    companyDomain: 'gainsight.com',
    difficulty: 'Medium',
    isFree: false,
    tags: ['CTE', 'LAG', 'window functions', 'multi-JOIN'],
    roles: ['PA', 'DA', 'BA'],
    priority: 1,
    estimatedMin: 12,
    datamartId: 'saas',
    prompt: 'Revenue ops wants to track expansion revenue. Identify any account that has ever upgraded its subscription plan — defined as switching to a plan with a higher MRR than the previous one. Return account_id, company_name, old_plan name, and new_plan name.',
    expectedColumns: ['account_id', 'company_name', 'old_plan', 'new_plan'],
    expectedRowCount: 1,
    checkValues: [{ company_name: 'Acme Corp', old_plan: 'Business', new_plan: 'Enterprise' }],
    solution: 'WITH sub_history AS (\n  SELECT account_id, plan_id, mrr, started_at,\n    LAG(mrr) OVER (PARTITION BY account_id ORDER BY started_at) AS prev_mrr,\n    LAG(plan_id) OVER (PARTITION BY account_id ORDER BY started_at) AS prev_plan_id\n  FROM subscriptions\n)\nSELECT sh.account_id, a.company_name,\n  p1.name AS old_plan, p2.name AS new_plan\nFROM sub_history sh\nJOIN accounts a ON sh.account_id = a.account_id\nJOIN plans p1 ON sh.prev_plan_id = p1.plan_id\nJOIN plans p2 ON sh.plan_id = p2.plan_id\nWHERE sh.mrr > sh.prev_mrr\nORDER BY sh.account_id',
    debrief: 'Only Acme Corp upgraded — from Business (999 MRR) to Enterprise (2999 MRR). Account 3 (Capitol Finance) switched from Business to Growth, which is a downgrade (mrr dropped), so it is correctly excluded. The LAG() OVER (PARTITION BY account_id ORDER BY started_at) pattern is the canonical way to compare consecutive rows per entity. The weak answer tries a self-join on subscriptions — which works but requires reasoning about row uniqueness that LAG makes trivial. The CTE clarifies intent and separates the windowing from the filter logic.',
    sqliteNote: null,
  },

  {
    id: 'sql-m02',
    title: 'Broad Spending Pattern Detection',
    company: 'Plaid',
    companyDomain: 'plaid.com',
    difficulty: 'Medium',
    isFree: false,
    tags: ['HAVING', 'COUNT DISTINCT', 'multi-table JOIN', 'aggregation'],
    roles: ['DA', 'PA', 'BA'],
    priority: 1,
    estimatedMin: 10,
    datamartId: 'fintech',
    prompt: 'The risk team has found that users who transact across 3 or more distinct spend categories are higher-value customers. Identify these users. Return user_id, email, and their distinct_categories count, ordered by distinct_categories descending.',
    expectedColumns: ['user_id', 'email', 'distinct_categories'],
    expectedRowCount: 3,
    checkValues: [{ user_id: '1', distinct_categories: '5' }],
    solution: 'SELECT u.user_id, u.email, COUNT(DISTINCT t.category) AS distinct_categories\nFROM users u\nJOIN accounts a ON u.user_id = a.user_id\nJOIN transactions t ON a.account_id = t.account_id\nGROUP BY u.user_id, u.email\nHAVING COUNT(DISTINCT t.category) >= 3\nORDER BY distinct_categories DESC, u.user_id',
    debrief: 'Three users qualify: user 1 (5 categories), user 5 (4 categories), user 3 (3 categories). The chain of JOINs is required: category lives on the transaction, but the risk classification lives on the user — you need accounts to bridge them. The weak answer groups only on transactions without joining users, returning account-level results instead of user-level. COUNT(DISTINCT category) is the key — COUNT(category) would inflate by counting repeated categories.',
    sqliteNote: null,
  },

  {
    id: 'sql-m03',
    title: 'Like Rate by Content Category',
    company: 'Pinterest',
    companyDomain: 'pinterest.com',
    difficulty: 'Medium',
    isFree: false,
    tags: ['CASE WHEN', 'GROUP BY', 'JOIN', 'aggregation', 'rate calculation'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 10,
    datamartId: 'consumer',
    prompt: 'The editorial team wants to understand which content categories drive the most engagement signals. For each content category, calculate the total interactions, like count, and like_pct (percentage of interactions that are likes, rounded to 1 decimal). Order by like_pct descending.',
    expectedColumns: ['category', 'total_interactions', 'likes', 'like_pct'],
    expectedRowCount: 5,
    checkValues: [{ category: 'cooking', likes: '2' }],
    solution: 'SELECT c.category,\n  COUNT(*) AS total_interactions,\n  SUM(CASE WHEN i.action = \'like\' THEN 1 ELSE 0 END) AS likes,\n  ROUND(100.0 * SUM(CASE WHEN i.action = \'like\' THEN 1 ELSE 0 END) / COUNT(*), 1) AS like_pct\nFROM interactions i\nJOIN content c ON i.content_id = c.content_id\nGROUP BY c.category\nORDER BY like_pct DESC',
    debrief: 'Cooking leads at 33.3% like rate (2/6 interactions), followed by fitness at 27.3% (3/11). Travel has the lowest at 20.0%. The JOIN on content is required — the interactions table does not store category directly. The weak answer filters WHERE action = \'like\' before grouping, which gives only the like counts without the denominator needed for the rate. CASE WHEN inside SUM is the pivot idiom that lets you compute both numerator and denominator in a single pass.',
    sqliteNote: null,
  },

  {
    id: 'sql-m04',
    title: 'H1 vs H2 Order Volume',
    company: 'Wayfair',
    companyDomain: 'wayfair.com',
    difficulty: 'Medium',
    isFree: false,
    tags: ['CASE WHEN', 'conditional aggregation', 'strftime', 'date grouping'],
    roles: ['PA', 'DA', 'BA'],
    priority: 2,
    estimatedMin: 10,
    datamartId: 'ecomm',
    prompt: 'Finance is doing a mid-year review and wants a quick H1 vs H2 comparison of order volume for 2023. Can you pull that — ideally in a single row so it\'s easy to paste into a slide?',
    expectedColumns: ['h1_2023', 'h2_2023'],
    expectedRowCount: 1,
    checkValues: [{ h1_2023: '9', h2_2023: '9' }],
    solution: 'SELECT\n  SUM(CASE WHEN strftime(\'%Y-%m\', created_at) BETWEEN \'2023-01\' AND \'2023-06\' THEN 1 ELSE 0 END) AS h1_2023,\n  SUM(CASE WHEN strftime(\'%Y-%m\', created_at) BETWEEN \'2023-07\' AND \'2023-12\' THEN 1 ELSE 0 END) AS h2_2023\nFROM orders',
    debrief: 'What the stakeholder wants: A side-by-side count of orders in the first six months vs the last six months of 2023, in one row.\n\nAmbiguities resolved: "H1" is January through June, "H2" is July through December — standard calendar halves. All order statuses are included unless the business defines "order" as completed-only (document this assumption). Outputting h1_2023 and h2_2023 as column names makes the slide-paste use case obvious.\n\nSQL approach: Use conditional aggregation — SUM(CASE WHEN strftime(\'%m\', created_at) <= \'06\' THEN 1 ELSE 0 END) for H1, the complement for H2. A UNION of two GROUP BY queries also works but produces two rows, not one.\n\nWhat weak SQL looks like: Two separate SELECT COUNT(*) queries submitted independently. That gives the right numbers but not the single-row format requested. Or using WHERE month <= 6 without casting — SQLite date functions need strftime.\n\nInterviewer follow-up: "What if I wanted this broken out by order status as well — how would you extend this?"',
    sqliteNote: null,
  },

  {
    id: 'sql-m05',
    title: 'No-Show Rate by Visit Type',
    company: 'Doximity',
    companyDomain: 'doximity.com',
    difficulty: 'Medium',
    isFree: false,
    tags: ['CASE WHEN', 'GROUP BY', 'aggregation', 'pivot'],
    roles: ['DA', 'PA', 'BA'],
    priority: 2,
    estimatedMin: 10,
    datamartId: 'health',
    prompt: 'Operations wants to compare no-show rates across visit modalities to decide where to invest in reminder outreach. For each visit_type, return total_appts, attended count, no_show_count, and no_show_rate (as % rounded to 1 decimal). Order by total_appts descending.',
    expectedColumns: ['visit_type', 'total_appts', 'attended', 'no_show_count'],
    expectedRowCount: 3,
    checkValues: [{ visit_type: 'in_person', no_show_count: '4' }],
    solution: 'SELECT visit_type,\n  COUNT(*) AS total_appts,\n  SUM(CASE WHEN no_show = 0 THEN 1 ELSE 0 END) AS attended,\n  SUM(CASE WHEN no_show = 1 THEN 1 ELSE 0 END) AS no_show_count,\n  ROUND(100.0 * SUM(no_show) / COUNT(*), 1) AS no_show_rate\nFROM appointments\nGROUP BY visit_type\nORDER BY total_appts DESC',
    debrief: 'In-person has the highest absolute no-shows (4 of 12 = 33.3%), telehealth has 1 of 7 (14.3%), and follow-up has zero no-shows. The business implication: reminder investments should target in-person appointments first. The weak answer uses SUM(no_show) directly for no_show_count — which is correct numerically since no_show is 0/1, but using CASE WHEN makes the intent explicit. Interviewers reward explicit, auditable logic over relying on integer semantics.',
    sqliteNote: null,
  },

  {
    id: 'sql-m06',
    title: 'Disputed Transaction Exposure by Category',
    company: 'Marqeta',
    companyDomain: 'marqeta.com',
    difficulty: 'Medium',
    isFree: false,
    tags: ['GROUP BY', 'filter', 'aggregation', 'fraud analytics'],
    roles: ['DA', 'PA', 'BA'],
    priority: 2,
    estimatedMin: 8,
    datamartId: 'fintech',
    prompt: 'The fraud team needs to understand which merchant categories carry the most disputed volume. For transactions with status = \'disputed\', return each category with dispute_count and total_disputed_amount, ordered by total_disputed_amount descending.',
    expectedColumns: ['category', 'dispute_count', 'total_disputed_amount'],
    expectedRowCount: 2,
    checkValues: [{ category: 'shopping', dispute_count: '4' }],
    solution: 'SELECT category,\n  COUNT(*) AS dispute_count,\n  SUM(amount) AS total_disputed_amount\nFROM transactions\nWHERE status = \'disputed\'\nGROUP BY category\nORDER BY total_disputed_amount DESC',
    debrief: 'Shopping carries 4 disputes totaling $4,819.99 — dominated by the $3,500 dispute in transaction 31. Travel is second with 2 disputes and $870. The weak answer joins to the disputes table unnecessarily — the transactions table already has the status column and category, so the join adds complexity with no benefit. The follow-up: "How would you identify which merchant drove the most disputes?" requires joining transactions to merchants and grouping by merchant name instead.',
    sqliteNote: null,
  },

  {
    id: 'sql-m07',
    title: 'Premium Users Missing Category',
    company: 'Spotify',
    companyDomain: 'spotify.com',
    difficulty: 'Medium',
    isFree: false,
    tags: ['subquery', 'NOT IN', 'filter', 'JOIN', 'anti-join variant'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 12,
    datamartId: 'consumer',
    prompt: 'The personalization team wants to recommend fitness content to premium users who have not yet engaged with that category. Return user_id and username for all premium users who have zero interactions with any fitness content, ordered by user_id.',
    expectedColumns: ['user_id', 'username'],
    expectedRowCount: 2,
    checkValues: [{ user_id: '10', username: 'julia' }],
    solution: 'SELECT u.user_id, u.username\nFROM users u\nWHERE u.is_premium = 1\n  AND u.user_id NOT IN (\n    SELECT DISTINCT i.user_id\n    FROM interactions i\n    JOIN content c ON i.content_id = c.content_id\n    WHERE c.category = \'fitness\'\n  )\nORDER BY u.user_id',
    debrief: 'Julia (10) and Lisa (12) are the two premium users who have never engaged with fitness content. Four other premium users (Alice, Carol, Emma, Grace) already interacted with the fitness video. The NOT IN subquery is readable here — just note it breaks if the subquery returns a NULL user_id, which is why a LEFT JOIN alternative is more production-safe. The key insight: two separate JOINs are required — one to get users who DID engage with fitness, then exclude them from the premium user list.',
    sqliteNote: null,
  },

  {
    id: 'sql-m08',
    title: 'No-Show Rate by Specialty',
    company: 'One Medical',
    companyDomain: 'onemedical.com',
    difficulty: 'Medium',
    isFree: false,
    tags: ['GROUP BY', 'JOIN', 'aggregation', 'SUM', 'ROUND'],
    roles: ['DA', 'PA', 'BA'],
    priority: 2,
    estimatedMin: 10,
    datamartId: 'health',
    prompt: 'The medical director wants a specialty-level view of no-show rates to allocate reminder call resources. Return specialty, total_appts, no_shows, and no_show_rate (as % rounded to 1 decimal) for each specialty. Order by no_show_rate descending, then total_appts descending.',
    expectedColumns: ['specialty', 'total_appts', 'no_shows', 'no_show_rate'],
    expectedRowCount: 5,
    checkValues: [{ specialty: 'primary_care', no_show_rate: '33.3' }],
    solution: 'SELECT p.specialty,\n  COUNT(*) AS total_appts,\n  SUM(a.no_show) AS no_shows,\n  ROUND(100.0 * SUM(a.no_show) / COUNT(*), 1) AS no_show_rate\nFROM appointments a\nJOIN providers p ON a.provider_id = p.provider_id\nGROUP BY p.specialty\nORDER BY no_show_rate DESC, total_appts DESC',
    debrief: 'Primary care and orthopedics share the highest no-show rate at 33.3%. Primary care handles this at much higher volume (12 appts vs 3), making it the priority for intervention. Dermatology and psychiatry show zero no-shows — either the patients are more motivated or the appointment durations are shorter with lower friction to reschedule. The GROUP BY p.specialty aggregates across multiple providers per specialty correctly. The weak answer groups by provider_id and misses the specialty-level rollup the question asked for.',
    sqliteNote: null,
  },

  {
    id: 'sql-m09',
    title: 'Monthly Order Volume Trend',
    company: 'Instacart',
    companyDomain: 'instacart.com',
    difficulty: 'Medium',
    isFree: false,
    tags: ['strftime', 'GROUP BY', 'date aggregation', 'time series'],
    roles: ['PA', 'DA', 'BA'],
    priority: 1,
    estimatedMin: 8,
    datamartId: 'ecomm',
    prompt: 'The operations team tracks monthly order volume to plan warehouse staffing. Return each order_month (as YYYY-MM) and the total order_count, ordered chronologically. Include all statuses.',
    expectedColumns: ['order_month', 'order_count'],
    expectedRowCount: 15,
    checkValues: [{ order_month: '2024-01', order_count: '4' }],
    solution: 'SELECT strftime(\'%Y-%m\', created_at) AS order_month,\n  COUNT(*) AS order_count\nFROM orders\nGROUP BY order_month\nORDER BY order_month',
    debrief: 'January 2024 is the peak month with 4 orders — driven by user 5 placing 3 back-to-back orders (Jan 10, 11, 12) plus user 10. Most months average 1-2 orders. The weak answer uses DATE_TRUNC(\'month\', created_at) — which is PostgreSQL syntax and fails in SQLite. strftime(\'%Y-%m\', date_column) is the SQLite-native idiom for month truncation. The debrief worth raising: this includes cancelled and returned orders. Should staffing forecasting use total orders or completed orders only? That is a business decision the analyst should surface.',
    sqliteNote: 'Uses strftime() instead of DATE_TRUNC() — SQLite-specific syntax.',
  },

  {
    id: 'sql-m10',
    title: 'Accounts with High Inactivity Rate',
    company: 'Amplitude',
    companyDomain: 'amplitude.com',
    difficulty: 'Medium',
    isFree: false,
    tags: ['HAVING', 'CASE WHEN', 'GROUP BY', 'JOIN', 'activation analytics'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 12,
    datamartId: 'saas',
    prompt: 'Customer success is triaging at-risk accounts. Flag any account where 30% or more of its users have never logged in (last_active_at IS NULL). Return account_id, company_name, total_users, never_active count, and inactive_pct (rounded to 1 decimal). Order by inactive_pct descending.',
    expectedColumns: ['account_id', 'company_name', 'total_users', 'never_active', 'inactive_pct'],
    expectedRowCount: 2,
    checkValues: [{ company_name: 'Blue Retail' }],
    solution: 'SELECT a.account_id, a.company_name,\n  COUNT(*) AS total_users,\n  SUM(CASE WHEN u.last_active_at IS NULL THEN 1 ELSE 0 END) AS never_active,\n  ROUND(100.0 * SUM(CASE WHEN u.last_active_at IS NULL THEN 1 ELSE 0 END) / COUNT(*), 1) AS inactive_pct\nFROM accounts a\nJOIN users u ON a.account_id = u.account_id\nGROUP BY a.account_id, a.company_name\nHAVING inactive_pct >= 30.0\nORDER BY inactive_pct DESC',
    debrief: 'Blue Retail and Delta Health both have exactly 1 of 3 users who never logged in (33.3%). Both viewer-role users — a pattern: viewer seats are provisioned and forgotten, which is a known SaaS churn signal. The HAVING clause filters after aggregation; a WHERE clause cannot reference the computed inactive_pct alias. The GROUP BY a.account_id, a.company_name is necessary because company_name is not functionally dependent on account_id in the SQL engine\'s view. The weak answer re-runs the numerator expression twice — using a CTE or subquery to alias it once is cleaner.',
    sqliteNote: null,
  },

  {
    id: 'sql-m11',
    title: 'Category Sales Volume',
    company: 'Amazon',
    companyDomain: 'amazon.com',
    difficulty: 'Medium',
    tags: ['3-table JOIN', 'GROUP BY', 'SUM'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 8,
    datamartId: 'ecomm',
    prompt: 'The merchandising team wants to see which product categories drive the most fulfilled orders. Join orders, order_items, and products to return category and items_sold — the count of items from completed orders — ordered by items_sold descending.',
    expectedColumns: ['category', 'items_sold'],
    expectedRowCount: 4,
    checkValues: [{ category: 'electronics', items_sold: '10' }],
    solution: 'SELECT p.category, COUNT(oi.item_id) AS items_sold\nFROM orders o\nJOIN order_items oi ON o.order_id = oi.order_id\nJOIN products p ON oi.product_id = p.product_id\nWHERE o.status = \'completed\'\nGROUP BY p.category\nORDER BY items_sold DESC',
    debrief: 'Electronics leads with 10 items sold across completed orders, followed by apparel (7), books (5), and home (3). The WHERE on status before the JOIN means returned and cancelled items are excluded — this is fulfilled revenue, not gross order volume. A common mistake is omitting the status filter, which inflates category counts. Note that home products (9) appear in two separate order_items rows for the same order — the item_id granularity correctly counts these as separate items.',
    sqliteNote: null,
  },

  {
    id: 'sql-m12',
    title: 'Zero-Activity Accounts',
    company: 'Amplitude',
    companyDomain: 'amplitude.com',
    difficulty: 'Medium',
    tags: ['CTE', 'LEFT JOIN', 'anti-join', 'IS NULL'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 8,
    datamartId: 'saas',
    prompt: 'Customer success is hunting for accounts that have gone completely dark — no product activity at all. Which accounts have never generated any event in the system?',
    expectedColumns: ['account_id', 'company_name'],
    expectedRowCount: 7,
    checkValues: [],
    solution: 'WITH has_events AS (\n  SELECT DISTINCT account_id FROM events\n)\nSELECT a.account_id, a.company_name\nFROM accounts a\nLEFT JOIN has_events e ON a.account_id = e.account_id\nWHERE e.account_id IS NULL\nORDER BY a.account_id',
    debrief: 'What the stakeholder wants: A list of accounts with zero rows in the events table — these are the churn risks.\n\nAmbiguities resolved: "Activity" means any row in the events table, regardless of event type. An account with zero events is completely inactive. We include all accounts, not just active subscriptions (document this — a churned account with no events is still a useful data point).\n\nSQL approach: Anti-join. Two valid approaches: (1) LEFT JOIN events ON account_id, filter WHERE event IS NULL. (2) NOT IN / NOT EXISTS subquery on distinct account_ids from events. CTE collecting distinct event account_ids then LEFT JOINing is clean and readable.\n\nWhat weak SQL looks like: WHERE events.account_id IS NULL without a LEFT JOIN — this produces no rows because the INNER JOIN already filtered them out. Or querying events and looking for low counts instead of zero.\n\nInterviewer follow-up: "How would you modify this to find accounts that haven\'t had an event in the last 30 days — not just accounts with zero events ever?"',
    sqliteNote: null,
  },

  {
    id: 'sql-m13',
    title: 'Latest Transaction Per Account',
    company: 'Stripe',
    companyDomain: 'stripe.com',
    difficulty: 'Medium',
    tags: ['ROW_NUMBER', 'CTE', 'window function', 'deduplication'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 10,
    datamartId: 'fintech',
    prompt: 'The reconciliation team needs to see the most recent transaction for each account — one row per account, showing what the latest activity looked like.',
    expectedColumns: ['txn_id', 'account_id', 'amount', 'occurred_at'],
    expectedRowCount: 15,
    checkValues: [],
    solution: 'WITH ranked AS (\n  SELECT txn_id, account_id, amount, occurred_at,\n         ROW_NUMBER() OVER (PARTITION BY account_id ORDER BY occurred_at DESC) AS rn\n  FROM transactions\n)\nSELECT txn_id, account_id, amount, occurred_at\nFROM ranked\nWHERE rn = 1\nORDER BY account_id',
    debrief: 'What the stakeholder wants: A single row per account showing the latest transaction details.\n\nAmbiguities resolved: "Most recent" means highest occurred_at. If two transactions share the same timestamp, pick consistently (e.g. highest txn_id). All transaction statuses are included — the reconciliation team wants the full picture, not just completed ones.\n\nSQL approach: ROW_NUMBER() OVER (PARTITION BY account_id ORDER BY occurred_at DESC) in a CTE, then filter WHERE rn = 1. Alternative: MAX(occurred_at) subquery joined back to transactions — correct but fails on timestamp ties without a secondary sort.\n\nWhat weak SQL looks like: GROUP BY account_id with MAX(occurred_at) then re-joining — works unless two transactions share the same max timestamp, in which case you get multiple rows per account silently.\n\nInterviewer follow-up: "If I also wanted the second-most-recent transaction per account, how would you change this?"',
    sqliteNote: null,
  },

  {
    id: 'sql-m14',
    title: 'Engagement Quality by Content',
    company: 'YouTube',
    companyDomain: 'youtube.com',
    difficulty: 'Medium',
    tags: ['CASE WHEN', 'SUM', 'conditional aggregation'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 8,
    datamartId: 'consumer',
    prompt: 'The content team wants to compare engagement quality across pieces — not just total volume, but the breakdown of how people are actually interacting. Views, likes, and everything else, per content item.',
    expectedColumns: ['content_id', 'like_count', 'view_count', 'total_interactions'],
    expectedRowCount: 8,
    checkValues: [{ content_id: '1', like_count: '2', view_count: '3', total_interactions: '8' }],
    solution: 'SELECT content_id,\n  SUM(CASE WHEN action = \'like\' THEN 1 ELSE 0 END) AS like_count,\n  SUM(CASE WHEN action = \'view\' THEN 1 ELSE 0 END) AS view_count,\n  COUNT(*) AS total_interactions\nFROM interactions\nGROUP BY content_id\nORDER BY total_interactions DESC',
    debrief: 'What the stakeholder wants: Per-content breakdown of interaction types — how many views, likes, and other actions each piece received.\n\nAmbiguities resolved: "Engagement quality" here means the raw counts by type, not a derived score. Return all content pieces including those with zero interactions of a given type (NULL or 0 — choose 0 for readability). Order by total interactions descending to surface the most-engaged content first.\n\nSQL approach: Conditional aggregation — SUM(CASE WHEN action = \'like\' THEN 1 ELSE 0 END) per content_id. This is a pivot over a categorical column. One pass over the table, all types in one row.\n\nWhat weak SQL looks like: Separate subqueries or JOINs for each action type — produces the right answer but reads as multiple queries duct-taped together. Interviewers notice this.\n\nInterviewer follow-up: "How would you compute what percentage of total interactions are likes, per content piece?"',
    sqliteNote: null,
  },

  {
    id: 'sql-m15',
    title: 'Monthly Event Volume',
    company: 'Mixpanel',
    companyDomain: 'mixpanel.com',
    difficulty: 'Medium',
    tags: ['strftime', 'GROUP BY', 'date formatting'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 7,
    datamartId: 'saas',
    prompt: 'The product team wants to spot trends in platform usage over time. Pull a month-by-month event count so they can see how usage is evolving.',
    expectedColumns: ['month', 'event_count'],
    expectedRowCount: 3,
    checkValues: [{ month: '2024-01', event_count: '38' }],
    solution: 'SELECT strftime(\'%Y-%m\', occurred_at) AS month, COUNT(*) AS event_count\nFROM events\nGROUP BY month\nORDER BY month',
    debrief: 'What the stakeholder wants: A chronological count of events by calendar month.\n\nAmbiguities resolved: "Month" means calendar month, formatted as YYYY-MM for chronological sort. Include all months with at least one event — months with zero events are excluded unless asked otherwise (a date spine is a separate, harder pattern). All event types are included.\n\nSQL approach: GROUP BY strftime(\'%Y-%m\', occurred_at), ORDER BY month ASC. strftime is SQLite\'s date-truncation function — equivalent to DATE_TRUNC in PostgreSQL.\n\nWhat weak SQL looks like: GROUP BY the raw occurred_at column — this groups by the full timestamp, not the month. Or using SUBSTR(occurred_at, 1, 7) — works but is less readable and error-prone on non-standard date formats.\n\nInterviewer follow-up: "What if the product team also wants to see a month-over-month growth rate — what would you add?"',
    sqliteNote: 'strftime is SQLite\'s date formatting function. Equivalent to DATE_TRUNC(\'month\', ...) in Postgres/BigQuery.',
  },

  {
    id: 'sql-m16',
    title: 'Running Spend Per User',
    company: 'Shopify',
    companyDomain: 'shopify.com',
    difficulty: 'Medium',
    tags: ['SUM OVER', 'window function', 'running total'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 10,
    datamartId: 'ecomm',
    prompt: 'Finance wants to track how each customer\'s cumulative spend grows as orders come in over time. Show the order-by-order running total per user.',
    expectedColumns: ['order_id', 'user_id', 'created_at', 'subtotal', 'running_total'],
    expectedRowCount: 28,
    checkValues: [],
    solution: 'SELECT order_id, user_id, created_at, subtotal,\n  SUM(subtotal) OVER (PARTITION BY user_id ORDER BY created_at) AS running_total\nFROM orders\nORDER BY user_id, created_at',
    debrief: 'What the stakeholder wants: For each order row, the cumulative sum of that user\'s spending up to and including that order.\n\nAmbiguities resolved: "Cumulative" means a running sum partitioned by user, ordered by order date. All order statuses are included (finance wants the full ledger, not just completed). If two orders share the same created_at, the running total includes both in arbitrary order — document this.\n\nSQL approach: SUM(subtotal) OVER (PARTITION BY user_id ORDER BY created_at ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW). The ROWS BETWEEN frame is important — without it, SQLite uses RANGE which can produce unexpected results on tied dates.\n\nWhat weak SQL looks like: A self-join WHERE o2.created_at <= o1.created_at, summing o2.subtotal — correct but O(n²) and does not scale. Window function is the professional answer.\n\nInterviewer follow-up: "If the user placed a return (status = \'returned\'), should that subtract from the running total? How would you handle it?"',
    sqliteNote: null,
  },

  {
    id: 'sql-m17',
    title: 'Above-Average Enterprise Accounts',
    company: 'HubSpot',
    companyDomain: 'hubspot.com',
    difficulty: 'Medium',
    tags: ['correlated subquery', 'WHERE', 'AVG'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 10,
    datamartId: 'saas',
    prompt: 'Sales wants to identify the larger accounts within each industry vertical — those with more employees than the average for their sector. Return account_id, company_name, industry, and employee_count for accounts where employee_count exceeds the average employee_count for their industry. Order by industry, employee_count descending.',
    expectedColumns: ['account_id', 'company_name', 'industry', 'employee_count'],
    expectedRowCount: 4,
    checkValues: [],
    solution: 'SELECT a1.account_id, a1.company_name, a1.industry, a1.employee_count\nFROM accounts a1\nWHERE a1.employee_count > (\n  SELECT AVG(a2.employee_count)\n  FROM accounts a2\n  WHERE a2.industry = a1.industry\n)\nORDER BY a1.industry, a1.employee_count DESC',
    debrief: 'Four accounts exceed their industry average: Kilo Pharma (healthcare, 250), Foxtrot Media (media, 40), Blue Retail\'s counterpart Juliet Fashion (retail, 90), and Echo Tech (tech, 300). Single-account industries (logistics, hospitality, manufacturing, food, education) cannot produce any above-average result — a useful edge case to mention in an interview. The correlated subquery re-runs for each outer row; for large tables, rewrite using a window function: AVG(employee_count) OVER (PARTITION BY industry) is equivalent and faster.',
    sqliteNote: null,
  },

  {
    id: 'sql-m18',
    title: 'Flagged Merchant Transactions',
    company: 'PayPal',
    companyDomain: 'paypal.com',
    difficulty: 'Medium',
    tags: ['3-table JOIN', 'WHERE', 'boolean filter'],
    roles: ['PA', 'DA', 'PM', 'BA'],
    priority: 1,
    estimatedMin: 8,
    datamartId: 'fintech',
    prompt: 'The compliance team needs all transactions that routed through a flagged merchant. Join transactions to merchants to return txn_id, account_id, amount, and merchant name (aliased as merchant_name) for transactions where the merchant is_flagged = 1. Order by amount descending.',
    expectedColumns: ['txn_id', 'account_id', 'amount', 'merchant_name'],
    expectedRowCount: 5,
    checkValues: [],
    solution: 'SELECT t.txn_id, t.account_id, t.amount, m.name AS merchant_name\nFROM transactions t\nJOIN merchants m ON t.merchant_id = m.merchant_id\nWHERE m.is_flagged = 1\nORDER BY t.amount DESC',
    debrief: 'Five transactions across three flagged merchants — the largest is $3,500. Three of the five are also disputed (status = \'disputed\'), making them double-flagged: the merchant is flagged AND the transaction is in dispute. The compliance follow-up is to join this result to disputes on txn_id to confirm which rows have both signals. A merchant being flagged does not automatically mean each transaction is fraudulent — it raises the risk signal and triggers enhanced review, not automatic reversal.',
    sqliteNote: null,
  },

  {
    id: 'sql-m19',
    title: 'No-Show Details with Provider',
    company: 'One Medical',
    companyDomain: 'onemedical.com',
    difficulty: 'Medium',
    tags: ['3-table JOIN', 'WHERE', 'no_show filter'],
    roles: ['PA', 'DA', 'BA'],
    priority: 1,
    estimatedMin: 8,
    datamartId: 'health',
    prompt: 'The outreach team wants a full view of missed appointments enriched with clinical context. Join appointments, patients, and providers to return appt_id, patient gender, provider name (as provider_name), provider specialty, and scheduled_at for all no-show appointments (no_show = 1). Order by scheduled_at.',
    expectedColumns: ['appt_id', 'gender', 'provider_name', 'specialty', 'scheduled_at'],
    expectedRowCount: 6,
    checkValues: [],
    solution: 'SELECT a.appt_id, p.gender, pr.name AS provider_name, pr.specialty, a.scheduled_at\nFROM appointments a\nJOIN patients p ON a.patient_id = p.patient_id\nJOIN providers pr ON a.provider_id = pr.provider_id\nWHERE a.no_show = 1\nORDER BY a.scheduled_at',
    debrief: 'Six no-shows enriched with patient gender and provider specialty. The 3-table join pattern is common in clinical analytics: the appointments table is the fact table, patients and providers are dimension tables. Always join to the fact table first, then branch out to dimensions. If gender shows a skew among no-shows, that becomes a targeting insight for reminder campaigns. If a single specialty accounts for most no-shows, the issue is upstream (patient population mix, scheduling friction) rather than individual patient behavior.',
    sqliteNote: null,
  },

  {
    id: 'sql-m20',
    title: 'Top Products by Volume',
    company: 'Amazon',
    companyDomain: 'amazon.com',
    difficulty: 'Medium',
    tags: ['RANK', 'CTE', 'window function'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 10,
    datamartId: 'ecomm',
    prompt: 'Merchandising wants to spotlight the best-selling products. Which products are at the top by order volume — and handle ties fairly.',
    expectedColumns: ['product_id', 'items_sold', 'rnk'],
    expectedRowCount: 3,
    checkValues: [{ product_id: '1', items_sold: '6', rnk: '1' }],
    solution: 'WITH product_sales AS (\n  SELECT product_id, COUNT(*) AS items_sold\n  FROM order_items\n  GROUP BY product_id\n),\nranked AS (\n  SELECT product_id, items_sold,\n         RANK() OVER (ORDER BY items_sold DESC) AS rnk\n  FROM product_sales\n)\nSELECT product_id, items_sold, rnk\nFROM ranked\nWHERE rnk <= 3\nORDER BY rnk, product_id',
    debrief: 'What the stakeholder wants: The top-ranked products by how many times they appear in orders, with ties handled consistently.\n\nAmbiguities resolved: "Top" without a specified cutoff — interpreted as rank <= 3 (document this assumption, ask the stakeholder if they want top 5 or top N). "Fairly" means tied products share the same rank. All order statuses are included unless "sold" means completed only.\n\nSQL approach: CTE summing order_items per product, then RANK() OVER (ORDER BY items_sold DESC). RANK() shares the rank for ties and skips subsequent ranks (1,1,3). DENSE_RANK does not skip (1,1,2). ROW_NUMBER breaks ties arbitrarily — wrong here because the stakeholder said "handle ties fairly."\n\nWhat weak SQL looks like: ORDER BY items_sold DESC LIMIT 3 — misses tied products at the cutoff. A rank that uses ROW_NUMBER — arbitrarily breaks ties.\n\nInterviewer follow-up: "What\'s the difference between RANK, DENSE_RANK, and ROW_NUMBER — when would you use each?"',
    sqliteNote: null,
  },

  {
    id: 'sql-m21',
    title: 'First Interaction Per User',
    company: 'TikTok',
    companyDomain: 'tiktok.com',
    difficulty: 'Medium',
    tags: ['ROW_NUMBER', 'CTE', 'window function', 'first-touch'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 10,
    datamartId: 'consumer',
    prompt: 'The onboarding team wants to understand what each user discovered first on the platform. What was every user\'s very first interaction?',
    expectedColumns: ['user_id', 'content_id', 'action', 'occurred_at'],
    expectedRowCount: 13,
    checkValues: [{ user_id: '1', content_id: '1', action: 'view' }],
    solution: 'WITH ranked AS (\n  SELECT user_id, content_id, action, occurred_at,\n         ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY occurred_at) AS rn\n  FROM interactions\n)\nSELECT user_id, content_id, action, occurred_at\nFROM ranked\nWHERE rn = 1\nORDER BY user_id',
    debrief: 'What the stakeholder wants: One row per user showing the first interaction they ever performed.\n\nAmbiguities resolved: "First" means earliest occurred_at. If two interactions share the same timestamp, pick one consistently (e.g. lowest interaction_id). Include all users who have at least one interaction — users with no interactions do not appear.\n\nSQL approach: ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY occurred_at ASC), filter WHERE rn = 1. Alternative: MIN(occurred_at) per user joined back to interactions — correct but fails on timestamp ties.\n\nWhat weak SQL looks like: GROUP BY user_id, MIN(occurred_at) — gives the date but loses the content_id and action from that specific row without a re-join. The re-join then has the tie problem.\n\nInterviewer follow-up: "How would you find the first AND second interaction per user in the same query?"',
    sqliteNote: null,
  },

  {
    id: 'sql-m22',
    title: 'MRR by Industry',
    company: 'Salesforce',
    companyDomain: 'salesforce.com',
    difficulty: 'Medium',
    tags: ['CTE', 'JOIN', 'GROUP BY', 'SUM'],
    roles: ['PA', 'DA', 'PM', 'BA'],
    priority: 1,
    estimatedMin: 8,
    datamartId: 'saas',
    prompt: 'Finance needs to see how recurring revenue is distributed across industry verticals. Give me a breakdown of active MRR and account count by industry.',
    expectedColumns: ['industry', 'total_mrr', 'active_accounts'],
    expectedRowCount: 9,
    checkValues: [],
    solution: 'WITH active AS (\n  SELECT s.sub_id, s.mrr, a.industry\n  FROM subscriptions s\n  JOIN accounts a ON s.account_id = a.account_id\n  WHERE s.status = \'active\'\n)\nSELECT industry, SUM(mrr) AS total_mrr, COUNT(sub_id) AS active_accounts\nFROM active\nGROUP BY industry\nORDER BY total_mrr DESC',
    debrief: 'What the stakeholder wants: Per-industry total MRR and account count, for active subscriptions only.\n\nAmbiguities resolved: "Active MRR" means subscriptions with status = \'active\'. An industry with no active subscriptions should not appear (if it should, a LEFT JOIN is needed — document the choice). Accounts with multiple active subscriptions contribute MRR from each.\n\nSQL approach: JOIN subscriptions to accounts on account_id, filter WHERE status = \'active\', GROUP BY industry, SUM(mrr). A CTE for clarity when there are multiple filter conditions.\n\nWhat weak SQL looks like: Joining accounts to plans directly without going through subscriptions — the MRR lives on subscriptions, not plans. Or forgetting the status filter and including churned MRR.\n\nInterviewer follow-up: "How would you also show the percentage of total MRR each industry represents?"',
    sqliteNote: null,
  },

  {
    id: 'sql-m23',
    title: 'Accounts with Mixed Transaction Outcomes',
    company: 'Robinhood',
    companyDomain: 'robinhood.com',
    difficulty: 'Medium',
    tags: ['EXISTS', 'correlated subquery', 'AND'],
    roles: ['PA', 'DA', 'BA'],
    priority: 2,
    estimatedMin: 10,
    datamartId: 'fintech',
    prompt: 'The risk team wants to flag accounts that have both clean and disputed activity — a sign of mixed behavioral patterns. Return account_id for all accounts that have at least one completed transaction AND at least one disputed transaction in the transactions table. Order by account_id.',
    expectedColumns: ['account_id'],
    expectedRowCount: 6,
    checkValues: [{ account_id: '1' }],
    solution: 'SELECT DISTINCT a.account_id\nFROM accounts a\nWHERE EXISTS (\n  SELECT 1 FROM transactions t\n  WHERE t.account_id = a.account_id AND t.status = \'completed\'\n)\nAND EXISTS (\n  SELECT 1 FROM transactions t\n  WHERE t.account_id = a.account_id AND t.status = \'disputed\'\n)\nORDER BY a.account_id',
    debrief: 'Six accounts have both completed and disputed transactions. EXISTS is efficient because it short-circuits on the first matching row — it does not need to count or aggregate. An alternative approach is two CTEs (one for accounts with disputed txns, one for completed) joined together, which is more readable for complex conditions. In an interview, mention that DISTINCT on accounts is unnecessary if you start from the accounts table (each account_id appears once), but it makes the intent explicit.',
    sqliteNote: null,
  },

  {
    id: 'sql-m24',
    title: 'MRR Rank Within Industry',
    company: 'Gainsight',
    companyDomain: 'gainsight.com',
    difficulty: 'Medium',
    tags: ['RANK', 'PARTITION BY', 'window function', 'JOIN'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 10,
    datamartId: 'saas',
    prompt: 'Customer success wants to know where each account stands within its own industry by MRR — who\'s the biggest fish in each vertical?',
    expectedColumns: ['account_id', 'company_name', 'industry', 'mrr', 'industry_rank'],
    expectedRowCount: 12,
    checkValues: [],
    solution: 'SELECT a.account_id, a.company_name, a.industry, s.mrr,\n  RANK() OVER (PARTITION BY a.industry ORDER BY s.mrr DESC) AS industry_rank\nFROM subscriptions s\nJOIN accounts a ON s.account_id = a.account_id\nWHERE s.status = \'active\'\nORDER BY a.industry, industry_rank',
    debrief: 'What the stakeholder wants: Each account\'s MRR rank within its industry peer group.\n\nAmbiguities resolved: "Rank" means 1 = highest MRR within the industry. Active subscriptions only (churned accounts have no current MRR). If an account has multiple active subscriptions, sum their MRR first before ranking (document this — the stakeholder said "by MRR" which implies current revenue, not historical).\n\nSQL approach: RANK() OVER (PARTITION BY industry ORDER BY mrr DESC). Ties share the same rank. DENSE_RANK is also acceptable — discuss the difference with the interviewer.\n\nWhat weak SQL looks like: Ranking all accounts globally and then filtering by industry — this gives the global rank within the filtered set, not the industry-specific rank. Or grouping by industry and using MAX — loses per-account detail.\n\nInterviewer follow-up: "What if you only wanted to show the top-ranked account per industry — how would you filter this?"',
    sqliteNote: null,
  },

  {
    id: 'sql-m25',
    title: 'Broadly Purchased Products',
    company: 'Shopify',
    companyDomain: 'shopify.com',
    difficulty: 'Medium',
    tags: ['HAVING', 'COUNT DISTINCT', 'JOIN'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 8,
    datamartId: 'ecomm',
    prompt: 'The product team wants to identify items with broad appeal — purchased by at least 3 distinct users. Join order_items to orders and return product_id and unique_buyers for all products where the distinct buyer count >= 3. Order by unique_buyers descending, then product_id.',
    expectedColumns: ['product_id', 'unique_buyers'],
    expectedRowCount: 7,
    checkValues: [{ product_id: '1', unique_buyers: '5' }],
    solution: 'SELECT oi.product_id, COUNT(DISTINCT o.user_id) AS unique_buyers\nFROM order_items oi\nJOIN orders o ON oi.order_id = o.order_id\nGROUP BY oi.product_id\nHAVING COUNT(DISTINCT o.user_id) >= 3\nORDER BY unique_buyers DESC, oi.product_id',
    debrief: 'Seven of nine products have been ordered by 3 or more distinct users. Product 1 (Wireless Headphones) has the broadest reach at 5 buyers. Products 5 (Yoga Mat) and 8 (Data Science Guide) have only 2 buyers each — either niche items or under-discovered. COUNT(DISTINCT user_id) requires the JOIN to orders to get the user_id; the order_items table alone only has order_id. HAVING filters after grouping — this cannot be done in a WHERE clause because unique_buyers is not yet computed at that point.',
    sqliteNote: null,
  },

  {
    id: 'sql-m26',
    title: 'Session Gap Analysis',
    company: 'Amplitude',
    companyDomain: 'amplitude.com',
    difficulty: 'Medium',
    tags: ['LAG', 'window function', 'julianday', 'date arithmetic'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 12,
    datamartId: 'ecomm',
    prompt: 'The growth team wants to understand how long users wait between visits. For each session, how many days passed since that user\'s previous session?',
    expectedColumns: ['user_id', 'started_at', 'prev_session', 'days_gap'],
    expectedRowCount: 25,
    checkValues: [],
    solution: 'SELECT user_id, started_at,\n  LAG(started_at) OVER (PARTITION BY user_id ORDER BY started_at) AS prev_session,\n  ROUND(julianday(started_at) - julianday(LAG(started_at) OVER (PARTITION BY user_id ORDER BY started_at)), 0) AS days_gap\nFROM sessions\nORDER BY user_id, started_at',
    debrief: 'What the stakeholder wants: Per-session view showing the gap in days since the previous session for the same user.\n\nAmbiguities resolved: "Days" means calendar days between session start times (started_at). A user\'s first session has no previous session — return NULL for that row. Use all sessions regardless of device or conversion status.\n\nSQL approach: LAG(started_at) OVER (PARTITION BY user_id ORDER BY started_at) gives the previous session date. ROUND(julianday(started_at) - julianday(prev_session)) converts the date difference to days. julianday is SQLite\'s day-level arithmetic function.\n\nWhat weak SQL looks like: A self-join WHERE s2.started_at < s1.started_at, then MAX to get the immediately prior — produces the right result but is far harder to read and reason about than LAG.\n\nInterviewer follow-up: "How would you compute the average days between sessions per user — and how would you handle users who\'ve only had one session?"',
    sqliteNote: 'julianday() converts a date string to a floating-point day number. julianday(a) - julianday(b) gives exact days elapsed.',
  },

  {
    id: 'sql-m28',
    title: 'Top Creators by Engagement',
    company: 'YouTube',
    companyDomain: 'youtube.com',
    difficulty: 'Medium',
    tags: ['CTE', 'RANK', 'JOIN', 'window function'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 12,
    datamartId: 'consumer',
    prompt: 'The creator team wants to identify their top performers by total engagement. Who are the top 3 creators by interaction count across all their content?',
    expectedColumns: ['creator_id', 'total_interactions', 'rnk'],
    expectedRowCount: 3,
    checkValues: [{ creator_id: '1', total_interactions: '12', rnk: '1' }],
    solution: 'WITH creator_stats AS (\n  SELECT c.creator_id, COUNT(i.interaction_id) AS total_interactions\n  FROM content c\n  JOIN interactions i ON c.content_id = i.content_id\n  GROUP BY c.creator_id\n),\nranked AS (\n  SELECT creator_id, total_interactions,\n         RANK() OVER (ORDER BY total_interactions DESC) AS rnk\n  FROM creator_stats\n)\nSELECT creator_id, total_interactions, rnk\nFROM ranked\nWHERE rnk <= 3\nORDER BY rnk',
    debrief: 'What the stakeholder wants: The top 3 creators ranked by total interactions on their published content.\n\nAmbiguities resolved: "Top 3" — use rank <= 3, with ties sharing the rank (not LIMIT 3 which drops tied creators). "Engagement" means any row in the interactions table linked through content to the creator. Only creators who have at least one interaction appear.\n\nSQL approach: CTE 1 — join content to interactions, SUM per creator_id. CTE 2 — RANK() OVER (ORDER BY total_interactions DESC). Filter WHERE rnk <= 3.\n\nWhat weak SQL looks like: LIMIT 3 without a rank — silently drops tied creators at position 3. Or aggregating in a single step without a CTE — works but harder to audit when the logic is complex.\n\nInterviewer follow-up: "What if you wanted to show creators who have zero interactions — how would you modify this?"',
    sqliteNote: null,
  },

  {
    id: 'sql-m29',
    title: 'Next User Event',
    company: 'Amplitude',
    companyDomain: 'amplitude.com',
    difficulty: 'Medium',
    tags: ['LEAD', 'window function', 'PARTITION BY'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 8,
    datamartId: 'saas',
    prompt: 'The product team is analysing event sequencing and wants to understand how quickly users come back after each action. For every event, what was the same user\'s next event and when did it happen?',
    expectedColumns: ['event_id', 'user_id', 'event_name', 'occurred_at', 'next_event_date'],
    expectedRowCount: 50,
    checkValues: [],
    solution: 'SELECT event_id, user_id, event_name, occurred_at,\n       LEAD(occurred_at) OVER (PARTITION BY user_id ORDER BY occurred_at) AS next_event_date\nFROM events\nORDER BY user_id, occurred_at',
    debrief: 'What the stakeholder wants: Each event row enriched with the date (and optionally name) of the next event the same user performed.\n\nAmbiguities resolved: "Next" means the chronologically next event for the same user by occurred_at. The last event per user has no next event — return NULL. Include all events including those with no successor.\n\nSQL approach: LEAD(occurred_at) OVER (PARTITION BY user_id ORDER BY occurred_at) gives the next event date. LEAD(event_name) OVER the same window gives the next event name. LEAD is the forward-looking counterpart to LAG.\n\nWhat weak SQL looks like: A self-join WHERE e2.occurred_at > e1.occurred_at AND e2.user_id = e1.user_id, then MIN — technically correct but slow and hard to read at scale.\n\nInterviewer follow-up: "How would you compute the time gap in hours between each event and its successor?"',
    sqliteNote: null,
  },

  {
    id: 'sql-m30',
    title: 'Running Average Order Value',
    company: 'Shopify',
    companyDomain: 'shopify.com',
    difficulty: 'Medium',
    tags: ['AVG OVER', 'running average', 'window function', 'ROWS BETWEEN'],
    roles: ['PA', 'DA'],
    priority: 1,
    estimatedMin: 9,
    datamartId: 'ecomm',
    prompt: 'Finance tracks how each customer\'s spending pattern evolves over time. Show the running average order value for each user as their orders accumulate.',
    expectedColumns: ['user_id', 'order_id', 'created_at', 'subtotal', 'running_avg_subtotal'],
    expectedRowCount: 28,
    checkValues: [],
    solution: 'SELECT user_id, order_id, created_at, subtotal,\n       ROUND(AVG(subtotal) OVER (PARTITION BY user_id ORDER BY created_at ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW), 2) AS running_avg_subtotal\nFROM orders\nORDER BY user_id, created_at',
    debrief: 'What the stakeholder wants: For each order row, the average order value for that user up to and including that order.\n\nAmbiguities resolved: "Running average" means a cumulative average, not a rolling N-day average. All order statuses are included unless Finance defines "order value" as completed-only (document this). If two orders share the same date, the running average includes both.\n\nSQL approach: AVG(subtotal) OVER (PARTITION BY user_id ORDER BY created_at ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW). The ROWS BETWEEN frame ensures the average grows with each row rather than the default RANGE behaviour on tied dates.\n\nWhat weak SQL looks like: Computing total spend / count separately per user — gives a single average per user, not a per-row running average. Missing the ROWS frame — subtle bug on tied dates.\n\nInterviewer follow-up: "How would you compute a 3-order rolling average instead of a cumulative one?"',
    sqliteNote: null,
  },

  {
    id: 'sql-m31',
    title: 'Balance Quartile Ranking',
    company: 'Revolut',
    companyDomain: 'revolut.com',
    difficulty: 'Medium',
    tags: ['NTILE', 'window function', 'percentile'],
    roles: ['PA', 'DA', 'BA'],
    priority: 2,
    estimatedMin: 7,
    datamartId: 'fintech',
    prompt: 'The risk team wants to segment accounts into four balance tiers for targeted outreach. Divide our accounts into four equal-size groups based on account balance, with the highest-balance accounts in tier 1.',
    expectedColumns: ['account_id', 'user_id', 'balance', 'currency', 'balance_tier'],
    expectedRowCount: 15,
    checkValues: [],
    solution: 'SELECT account_id, user_id, balance, currency,\n       NTILE(4) OVER (ORDER BY balance DESC) AS balance_tier\nFROM accounts\nORDER BY balance_tier, balance DESC',
    debrief: 'What the stakeholder wants: Each account assigned to one of four equal-size tiers by balance, with 1 = highest.\n\nAmbiguities resolved: "Equal-size groups" means NTILE (not manual percentile thresholds). 1 = highest balance group (order BY balance DESC). If the account count isn\'t divisible by 4, NTILE distributes the remainder across the lower-numbered tiles — document this behaviour.\n\nSQL approach: NTILE(4) OVER (ORDER BY balance DESC). Returns 1–4 where 1 = top quartile. Note: NTILE does not handle ties within a bucket boundary — two accounts with the same balance may land in different tiles.\n\nWhat weak SQL looks like: Using PERCENT_RANK or hardcoded balance thresholds to define buckets — these do not produce equal-size groups. PERCENT_RANK gives a 0–1 score, not a bucket label.\n\nInterviewer follow-up: "What\'s the difference between NTILE and PERCENT_RANK? When would you use each?"',
    sqliteNote: null,
  },

  {
    id: 'sql-m32',
    title: 'First Transaction Benchmark',
    company: 'Stripe',
    companyDomain: 'stripe.com',
    difficulty: 'Medium',
    tags: ['FIRST_VALUE', 'window function', 'PARTITION BY'],
    roles: ['PA', 'DA'],
    priority: 2,
    estimatedMin: 8,
    datamartId: 'fintech',
    prompt: 'The analytics team wants to compare each transaction amount against the account\'s very first transaction, to detect whether spending ramps up or down over time. For every row in the transactions table, show txn id, account id, amount, occurred date, and the amount of that account\'s earliest transaction.',
    expectedColumns: ['txn_id', 'account_id', 'amount', 'occurred_at', 'first_txn_amount'],
    expectedRowCount: 40,
    checkValues: [],
    solution: 'SELECT txn_id, account_id, amount, occurred_at,\n       FIRST_VALUE(amount) OVER (PARTITION BY account_id ORDER BY occurred_at) AS first_txn_amount\nFROM transactions\nORDER BY account_id, occurred_at',
    debrief: 'FIRST_VALUE() returns the value from the first row of the window frame. Partitioning by account and ordering by date ensures each row carries the account\'s opening transaction amount. Pair with (amount - first_txn_amount) to compute an absolute spend delta per transaction.',
    sqliteNote: null,
  },

  {
    id: 'sql-m33',
    title: 'Completed Order Detail View',
    company: 'Amazon',
    companyDomain: 'amazon.com',
    difficulty: 'Medium',
    tags: ['4-table JOIN', 'multi-table JOIN', 'WHERE filter'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 9,
    datamartId: 'ecomm',
    prompt: 'The merchandising team needs a line-item view of every fulfilled order to reconcile inventory. Join users, orders, order_items, and products to produce one row per item in each completed order. Return the customer email, order id, order date, product name, category, and unit price. Exclude cancelled and returned orders.',
    expectedColumns: ['email', 'order_id', 'created_at', 'product_name', 'category', 'unit_price'],
    expectedRowCount: 25,
    checkValues: [{ order_id: '1', category: 'electronics' }],
    solution: 'SELECT u.email, o.order_id, o.created_at, p.name AS product_name, p.category, oi.unit_price\nFROM orders o\nJOIN users u ON o.user_id = u.user_id\nJOIN order_items oi ON o.order_id = oi.order_id\nJOIN products p ON oi.product_id = p.product_id\nWHERE o.status = \'completed\'\nORDER BY o.order_id',
    debrief: 'The result has 25 rows because two orders (14 and 23) each contain two items, while the other 21 completed orders contain one item each. Chain JOINs in logical order — orders then items then products — to avoid cross-product explosions. Filter status early in the WHERE clause.',
    sqliteNote: null,
  },

  {
    id: 'sql-m34',
    title: 'Traffic Source Conversion Rate',
    company: 'Shopify',
    companyDomain: 'shopify.com',
    difficulty: 'Medium',
    tags: ['CTE', 'GROUP BY', 'conversion rate', 'ROUND'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 10,
    datamartId: 'ecomm',
    prompt: 'The growth team wants to evaluate which acquisition channel drives the highest purchase intent. Using the sessions table, calculate the conversion rate (percentage of sessions where converted = 1) for each traffic source. Round to one decimal place and rank sources by conversion rate descending. Use a CTE for clarity.',
    expectedColumns: ['source', 'conversion_rate'],
    expectedRowCount: 5,
    checkValues: [],
    solution: 'WITH stats AS (\n  SELECT source, COUNT(*) AS total, SUM(converted) AS conversions\n  FROM sessions\n  GROUP BY source\n)\nSELECT source, ROUND(100.0 * conversions / total, 1) AS conversion_rate\nFROM stats\nORDER BY conversion_rate DESC',
    debrief: 'Referral leads at 100% (3/3), paid at 71.4% (5/7), organic at 55.6% (5/9), email at 33.3% (1/3), and social at 0% (0/3). The CTE separates raw aggregation from the rate calculation, making each step auditable. Multiply by 100.0 (not 100) to force floating-point division.',
    sqliteNote: null,
  },

  {
    id: 'sql-m35',
    title: 'Conversion Rate by Device Type',
    company: 'Shopify',
    companyDomain: 'shopify.com',
    difficulty: 'Medium',
    tags: ['conditional aggregation', 'GROUP BY', 'ROUND'],
    roles: ['PA', 'DA'],
    priority: 1,
    estimatedMin: 7,
    datamartId: 'ecomm',
    prompt: 'The UX team suspects mobile users convert at a lower rate due to checkout friction. Can you compare conversion rates across device types so we can quantify the gap?',
    expectedColumns: ['device', 'total_sessions', 'conversion_rate'],
    expectedRowCount: 3,
    checkValues: [],
    solution: 'SELECT device,\n       COUNT(*) AS total_sessions,\n       ROUND(100.0 * SUM(converted) / COUNT(*), 1) AS conversion_rate\nFROM sessions\nGROUP BY device\nORDER BY conversion_rate DESC',
    debrief: 'What the stakeholder wants: Conversion rate per device type, to see whether mobile underperforms vs desktop.\n\nAmbiguities resolved: "Conversion" means a session where converted = 1. Conversion rate = conversions / total sessions per device type, expressed as a percentage. Include all devices even if conversion rate is zero.\n\nSQL approach: GROUP BY device, COUNT(*) total sessions, SUM(converted) conversions, ROUND(100.0 * SUM(converted) / COUNT(*), 1) conv_rate. The 100.0 forces float division — integer division would truncate to 0 for rates below 1%.\n\nWhat weak SQL looks like: Using AVG(converted) — actually correct (AVG of a 0/1 flag = proportion) but not multiplied by 100, so the result reads as a proportion not a percentage. Or integer division: SUM(converted) / COUNT(*) truncates to 0 in most cases.\n\nInterviewer follow-up: "How would you test whether the mobile conversion gap is statistically significant?"',
    sqliteNote: null,
  },

  {
    id: 'sql-m36',
    title: 'Returned Then Re-Purchased Customers',
    company: 'Zalando',
    companyDomain: 'zalando.com',
    difficulty: 'Medium',
    tags: ['self-join', 'multi-condition JOIN', 'DISTINCT'],
    roles: ['PA', 'DA', 'BA'],
    priority: 2,
    estimatedMin: 10,
    datamartId: 'ecomm',
    prompt: 'The retention team wants to identify customers who submitted a return but later placed a completed order — these are high-value customers who resolved their dissatisfaction. Return each qualifying customer\'s email and user id, ordered by user id.',
    expectedColumns: ['email', 'user_id'],
    expectedRowCount: 3,
    checkValues: [{ user_id: '4', email: 'dan@example.com' }],
    solution: 'SELECT DISTINCT u.email, u.user_id\nFROM users u\nJOIN orders o1 ON u.user_id = o1.user_id AND o1.status = \'returned\'\nJOIN orders o2 ON u.user_id = o2.user_id AND o2.status = \'completed\'\nORDER BY u.user_id',
    debrief: 'Joining orders to itself twice — once for returned, once for completed — is the self-join pattern. Users 4, 10, and 12 each have at least one returned and one completed order. DISTINCT prevents duplicates when a user has multiple returns or completions. An EXISTS correlated subquery is an equally valid approach.',
    sqliteNote: null,
  },

  {
    id: 'sql-m37',
    title: 'High-Value Customers by Lifetime Spend',
    company: 'Amazon',
    companyDomain: 'amazon.com',
    difficulty: 'Medium',
    tags: ['HAVING', 'SUM', 'GROUP BY'],
    roles: ['PA', 'DA', 'PM', 'BA'],
    priority: 1,
    estimatedMin: 7,
    datamartId: 'ecomm',
    prompt: 'The loyalty team defines a high-value customer as anyone whose total order subtotal exceeds $200, across all order statuses. Return each qualifying user id and their rounded total spend, ordered by total spent descending.',
    expectedColumns: ['user_id', 'total_spent'],
    expectedRowCount: 6,
    checkValues: [],
    solution: 'SELECT user_id, ROUND(SUM(subtotal), 2) AS total_spent\nFROM orders\nGROUP BY user_id\nHAVING SUM(subtotal) > 200\nORDER BY total_spent DESC',
    debrief: 'Six users exceed $200 in total subtotal across all statuses: users 5, 1, 9, 7, 12, and 2. HAVING filters after aggregation — you cannot use WHERE to filter on an aggregate. Repeat the full SUM expression in HAVING rather than referencing the alias to avoid dialect-specific aliasing issues.',
    sqliteNote: null,
  },

  {
    id: 'sql-m39',
    title: 'Account Event Date Range',
    company: 'Mixpanel',
    companyDomain: 'mixpanel.com',
    difficulty: 'Medium',
    tags: ['MIN window', 'MAX window', 'window function', 'PARTITION BY'],
    roles: ['PA', 'DA'],
    priority: 2,
    estimatedMin: 8,
    datamartId: 'saas',
    prompt: 'Customer success wants to see each account\'s full activity window alongside every event in the log. For each event row, show how far back the account\'s history goes and when they were last active.',
    expectedColumns: ['event_id', 'account_id', 'occurred_at', 'first_event_date', 'last_event_date'],
    expectedRowCount: 50,
    checkValues: [{ event_id: '1', first_event_date: '2024-01-02' }],
    solution: 'SELECT event_id, account_id, occurred_at,\n       MIN(occurred_at) OVER (PARTITION BY account_id) AS first_event_date,\n       MAX(occurred_at) OVER (PARTITION BY account_id) AS last_event_date\nFROM events\nORDER BY account_id, occurred_at',
    debrief: 'What the stakeholder wants: A per-event view showing each account\'s first and last recorded event dates, so the CS team can quickly assess account tenure and recency from any event row.\n\nAmbiguities resolved: \'Full activity window\' means first_event_date and last_event_date per account, not per user. Inactive accounts (no events) don\'t appear in the events table and are naturally excluded — if the request were \'all accounts\' a LEFT JOIN from accounts would be needed.\n\nSQL approach: MIN(occurred_at) OVER (PARTITION BY account_id) and MAX(occurred_at) OVER (PARTITION BY account_id) compute the bookend dates per account. Without an ORDER BY clause in the OVER, these compute the full-partition aggregate — every row in the same account gets the same first and last date regardless of which row we\'re on.\n\nWhat weak SQL looks like: Adding ORDER BY occurred_at to the OVER — this turns MIN into a running minimum and MAX into a running maximum, changing the semantics entirely. Or using a correlated subquery for each row — this works but scans the events table multiple times instead of a single window pass.\n\nInterviewer follow-up: \'How would you flag accounts whose last event was more than 30 days ago?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-m40',
    title: 'Unique Viewers by Content Category',
    company: 'TikTok',
    companyDomain: 'tiktok.com',
    difficulty: 'Medium',
    tags: ['COUNT DISTINCT', 'JOIN', 'GROUP BY'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 7,
    datamartId: 'consumer',
    prompt: 'The content strategy team wants to know which categories attract the broadest audiences, measured by unique users who interacted with any content in that category. Join the interactions and content tables, count distinct users per category, and order by unique viewer count descending.',
    expectedColumns: ['category', 'unique_viewers'],
    expectedRowCount: 5,
    checkValues: [{ category: 'fitness', unique_viewers: '9' }],
    solution: 'SELECT c.category, COUNT(DISTINCT i.user_id) AS unique_viewers\nFROM content c\nJOIN interactions i ON c.content_id = i.content_id\nGROUP BY c.category\nORDER BY unique_viewers DESC',
    debrief: 'Fitness leads with 9 unique viewers (content 1 and 6 combined), cooking has 6 (content 3 and 7), and finance, travel, and tech each have 4. COUNT(DISTINCT user_id) is essential — without DISTINCT, users who interacted with multiple pieces in one category would be double-counted.',
    sqliteNote: null,
  },

  {
    id: 'sql-m41',
    title: 'Transaction Size Buckets',
    company: 'PayPal',
    companyDomain: 'paypal.com',
    difficulty: 'Medium',
    tags: ['CASE WHEN', 'bucketing', 'GROUP BY'],
    roles: ['PA', 'DA', 'BA'],
    priority: 1,
    estimatedMin: 7,
    datamartId: 'fintech',
    prompt: 'The risk team wants to understand how our transaction volume is distributed by size so they can calibrate fraud detection thresholds. Break down our transactions into small, medium, and large buckets.',
    expectedColumns: ['size_bucket', 'txn_count'],
    expectedRowCount: 3,
    checkValues: [{ size_bucket: 'small', txn_count: '19' }],
    solution: 'SELECT CASE WHEN amount < 100 THEN \'small\' WHEN amount < 1000 THEN \'medium\' ELSE \'large\' END AS size_bucket,\n       COUNT(*) AS txn_count\nFROM transactions\nGROUP BY 1\nORDER BY txn_count DESC',
    debrief: 'What the stakeholder wants: A count of transactions in each size tier to understand the distribution before setting anomaly thresholds — knowing whether most transactions are small vs. large changes how fraud rules are calibrated.\n\nAmbiguities resolved: The candidate must define the tier thresholds. A reasonable split for consumer fintech: small = under $100, medium = $100 to $999, large = $1,000 or more. These are business decisions that should be stated and documented — the stakeholder said \'small, medium, and large\' without defining the boundaries.\n\nSQL approach: CASE WHEN inside SELECT creates the size_bucket label using top-to-bottom evaluation — the second condition implicitly means $100 to $999 because values under $100 are already handled by condition 1. GROUP BY 1 groups by the derived expression. ORDER BY txn_count DESC surfaces the dominant bucket first.\n\nWhat weak SQL looks like: Using NTILE(3) — this creates equal-row-count buckets, not business-meaningful value ranges. Bucket 1 in NTILE is not the same as \'transactions under $100\'. For fraud threshold calibration, fixed value thresholds are needed, not equal-count quantiles.\n\nInterviewer follow-up: \'What additional cut would you add if fraud rate is highest in the $500-$999 range?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-m42',
    title: 'Patient Age at Appointment',
    company: 'Teladoc',
    companyDomain: 'teladoc.com',
    difficulty: 'Medium',
    tags: ['julianday', 'date arithmetic', 'JOIN', 'CAST'],
    roles: ['PA', 'DA', 'BA'],
    priority: 2,
    estimatedMin: 9,
    datamartId: 'health',
    prompt: 'The clinical quality team needs each patient\'s age at the time of their appointment to stratify care outcomes by age group. Pull each appointment with the patient\'s age at the visit.',
    expectedColumns: ['appt_id', 'patient_id', 'gender', 'scheduled_at', 'age_at_visit'],
    expectedRowCount: 10,
    checkValues: [],
    solution: 'SELECT a.appt_id, p.patient_id, p.gender, a.scheduled_at,\n       CAST((julianday(a.scheduled_at) - julianday(p.dob)) / 365.25 AS INTEGER) AS age_at_visit\nFROM appointments a\nJOIN patients p ON a.patient_id = p.patient_id\nORDER BY a.appt_id\nLIMIT 10',
    debrief: 'What the stakeholder wants: A per-appointment record showing how old each patient was at the time of the visit — not their current age — so outcomes can be stratified by age cohort.\n\nAmbiguities resolved: \'Age\' means completed whole years at appointment time, not decimal age. Age is calculated from the patient\'s date of birth to the appointment date, then truncated to an integer. SQLite has no AGE() function so julianday() is the standard approach. The LIMIT 10 scopes to the first 10 appointments by appt_id.\n\nSQL approach: julianday(scheduled_at) - julianday(dob) gives elapsed days. Dividing by 365.25 accounts for leap years. CAST(...AS INTEGER) truncates to whole years. The JOIN on patient_id is required because dob is in the patients table and the appointment date is in appointments.\n\nWhat weak SQL looks like: strftime(\'%Y\', scheduled_at) - strftime(\'%Y\', dob) for year difference — this ignores whether the birthday has passed yet in the appointment year, producing an off-by-one error for patients seen before their birthday in a given year.\n\nInterviewer follow-up: \'How would you group these into standard age bands (18-34, 35-54, 55+) for reporting?\'',
    sqliteNote: 'Uses julianday() which is SQLite-specific. In PostgreSQL use: EXTRACT(YEAR FROM AGE(a.scheduled_at::date, p.dob::date)) AS age_at_visit.',
  },

  {
    id: 'sql-m43',
    title: 'Interaction Breakdown with Total',
    company: 'YouTube',
    companyDomain: 'youtube.com',
    difficulty: 'Medium',
    tags: ['UNION ALL', 'aggregation', 'GROUP BY'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 8,
    datamartId: 'consumer',
    prompt: 'The engagement team wants a breakdown of interaction counts by action type, plus a TOTAL row at the top so QA can verify the numbers add up. Can you put that together?',
    expectedColumns: ['action', 'count'],
    expectedRowCount: 6,
    checkValues: [{ action: 'view', count: '14' }],
    solution: 'SELECT action, COUNT(*) AS count\nFROM interactions\nGROUP BY action\nUNION ALL\nSELECT \'TOTAL\', COUNT(*)\nFROM interactions\nORDER BY 2 DESC',
    debrief: 'What the stakeholder wants: A summary of interactions per action type with a TOTAL row appended, so QA can cross-check that per-action counts sum to the overall count.\n\nAmbiguities resolved: The total is a separate summary row, not a column. The result has one row per action type plus one TOTAL row — 6 rows in this dataset. The TOTAL row is for QA and display; it should not be used for downstream aggregation.\n\nSQL approach: GROUP BY action gives the per-action breakdown. UNION ALL appends a separate SELECT COUNT(*) row labeled \'TOTAL\'. ORDER BY 2 DESC sorts by count descending, so TOTAL (the largest number) appears first. ORDER BY references the second column positionally — alias-based ORDER BY may not resolve consistently across UNION branches.\n\nWhat weak SQL looks like: Using UNION instead of UNION ALL — UNION deduplicates, and if any action type happened to have the same count as TOTAL it would be silently dropped. Or GROUP BY ROLLUP — cleaner in PostgreSQL but not standard in SQLite; interviewers may want the UNION ALL pattern explicitly.\n\nInterviewer follow-up: \'How would you add a percentage column showing each action type\'s share of total interactions?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-m45',
    title: 'High-Volume Providers',
    company: 'Doximity',
    companyDomain: 'doximity.com',
    difficulty: 'Medium',
    tags: ['HAVING', 'COUNT', 'GROUP BY'],
    roles: ['PA', 'DA', 'BA'],
    priority: 1,
    estimatedMin: 6,
    datamartId: 'health',
    prompt: 'The operations team needs to identify providers carrying a disproportionate share of appointments to assess capacity risk. Return each provider with more than 2 appointments, along with their appointment count, ordered by count descending.',
    expectedColumns: ['provider_id', 'appt_count'],
    expectedRowCount: 4,
    checkValues: [{ provider_id: '1', appt_count: '10' }],
    solution: 'SELECT provider_id, COUNT(*) AS appt_count\nFROM appointments\nGROUP BY provider_id\nHAVING COUNT(*) > 2\nORDER BY appt_count DESC',
    debrief: 'Providers 1 (10), 2 (4), 3 (4), and 5 (3) exceed the threshold. Providers 4 and 6 each have exactly 2 and are excluded. HAVING COUNT(*) > 2 is the correct filter — using WHERE to filter on an aggregate before grouping is a syntax error in all SQL dialects.',
    sqliteNote: null,
  },

  {
    id: 'sql-m46',
    title: 'Account Activity Tier',
    company: 'HubSpot',
    companyDomain: 'hubspot.com',
    difficulty: 'Medium',
    tags: ['CTE', 'LEFT JOIN', 'CASE WHEN', 'activity tier'],
    roles: ['PA', 'DA', 'PM', 'BA'],
    priority: 1,
    estimatedMin: 10,
    datamartId: 'saas',
    prompt: 'Customer success wants to segment all our accounts into activity tiers — high, medium, low, and inactive — based on their event volume. Make sure dormant accounts with no events show up as inactive, not missing.',
    expectedColumns: ['account_id', 'company_name', 'event_count', 'activity_tier'],
    expectedRowCount: 15,
    checkValues: [{ account_id: '1', event_count: '11', activity_tier: 'high' }],
    solution: 'WITH event_counts AS (\n  SELECT a.account_id, a.company_name, COUNT(e.event_id) AS event_count\n  FROM accounts a\n  LEFT JOIN events e ON a.account_id = e.account_id\n  GROUP BY a.account_id, a.company_name\n)\nSELECT account_id, company_name, event_count,\n       CASE WHEN event_count > 5 THEN \'high\' WHEN event_count >= 2 THEN \'medium\' WHEN event_count = 1 THEN \'low\' ELSE \'inactive\' END AS activity_tier\nFROM event_counts\nORDER BY event_count DESC',
    debrief: 'What the stakeholder wants: Every account classified into a named engagement tier (high/medium/low/inactive) based on event count, so the CS team can route accounts to the right outreach workflow.\n\nAmbiguities resolved: Tier thresholds must be defined by the candidate. A reasonable set: high = more than 5 events, medium = 2 to 5, low = exactly 1, inactive = 0. The stakeholder said \'all accounts\' — so inactive accounts with zero events must appear, which drives the LEFT JOIN decision.\n\nSQL approach: A CTE counts events per account via LEFT JOIN from accounts to events (so all 15 accounts appear including those with zero events). COUNT(e.event_id) — not COUNT(*) — counts only non-NULL event_ids, returning 0 for unmatched accounts. The outer SELECT wraps the count in CASE WHEN to assign tier labels.\n\nWhat weak SQL looks like: INNER JOIN from accounts to events — drops all 7 inactive accounts. Or COUNT(*) instead of COUNT(e.event_id) with a LEFT JOIN — COUNT(*) returns 1 for unmatched rows (the NULL right-side row), so every inactive account shows event_count = 1 and is classified as \'low\' instead of \'inactive\'.\n\nInterviewer follow-up: \'How would you alert the CS team when an account drops from high to inactive between two consecutive monthly snapshots?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-m47',
    title: 'Days Since Previous Order',
    company: 'Shopify',
    companyDomain: 'shopify.com',
    difficulty: 'Medium',
    tags: ['LAG', 'window function', 'PARTITION BY', 'julianday'],
    roles: ['PA', 'DA'],
    priority: 1,
    estimatedMin: 9,
    datamartId: 'ecomm',
    prompt: 'The retention team wants to understand how frequently our customers come back to place another order. For each order, can you show how many days passed since that customer\'s previous purchase?',
    expectedColumns: ['user_id', 'order_id', 'created_at', 'days_since_prev'],
    expectedRowCount: 28,
    checkValues: [],
    solution: 'SELECT user_id, order_id, created_at,\n       CAST(julianday(created_at) - julianday(LAG(created_at) OVER (PARTITION BY user_id ORDER BY created_at)) AS INTEGER) AS days_since_prev\nFROM orders\nORDER BY user_id, created_at',
    debrief: 'What the stakeholder wants: A per-order view showing each customer\'s reorder gap — days elapsed since their previous order — so the team can identify declining purchase frequency and trigger re-engagement.\n\nAmbiguities resolved: Should returned or cancelled orders count? Including all statuses means a return still resets the reorder clock. If the team only wants completed purchases, a WHERE status = \'completed\' filter should be added and the business should decide. First orders per user return NULL for days_since_prev — expected and useful for distinguishing new vs. returning customers.\n\nSQL approach: LAG(created_at) OVER (PARTITION BY user_id ORDER BY created_at) retrieves the prior order\'s date within each user\'s partition. julianday difference converts the date gap to days. CAST to INTEGER gives whole days. First-row LAG returns NULL naturally.\n\nWhat weak SQL looks like: A self-join with MAX(created_at) < current order date — this works but requires a correlated subquery or complex join condition, scans large tables slowly, and still needs NULL handling for first orders. LAG is simpler, faster, and more readable.\n\nInterviewer follow-up: \'How would you flag users whose reorder gap has increased by more than 30% compared to their historical average?\'',
    sqliteNote: 'Uses julianday() for date arithmetic. In PostgreSQL use: (created_at::date - LAG(created_at::date) OVER (...)) AS days_since_prev.',
  },

  {
    id: 'sql-m48',
    title: 'Event Sequence Number per Account',
    company: 'Amplitude',
    companyDomain: 'amplitude.com',
    difficulty: 'Medium',
    tags: ['ROW_NUMBER', 'window function', 'PARTITION BY'],
    roles: ['PA', 'DA'],
    priority: 1,
    estimatedMin: 7,
    datamartId: 'saas',
    prompt: 'The product team is building funnel analysis and needs to know where each event falls in an account\'s history — its position in the sequence of actions that account has taken.',
    expectedColumns: ['event_id', 'account_id', 'event_name', 'occurred_at', 'event_seq'],
    expectedRowCount: 50,
    checkValues: [{ event_id: '1', event_seq: '1' }],
    solution: 'SELECT event_id, account_id, event_name, occurred_at,\n       ROW_NUMBER() OVER (PARTITION BY account_id ORDER BY occurred_at) AS event_seq\nFROM events\nORDER BY account_id, occurred_at',
    debrief: 'What the stakeholder wants: A sequential position number for each event within its account, so the product team can identify funnel steps (e.g., event 1 = first touch, event 5 = reached activation) and study which steps lead to conversion.\n\nAmbiguities resolved: Sequence is per account (not per user, not global). Ordering criterion is occurred_at — chronological. If two events share the same timestamp, tie-breaking is arbitrary but must be deterministic; breaking ties by event_id ASC is a safe default.\n\nSQL approach: ROW_NUMBER() OVER (PARTITION BY account_id ORDER BY occurred_at) assigns a unique sequential integer per account starting at 1. Unlike RANK or DENSE_RANK, ROW_NUMBER never repeats a number even when timestamps are equal.\n\nWhat weak SQL looks like: Using RANK() — if two events share a timestamp, RANK assigns the same number to both and skips the next rank (e.g., 1, 2, 2, 4). For funnel step labeling this breaks the sequence numbering. DENSE_RANK avoids skipping but still assigns identical numbers to ties, which distorts funnel step counts.\n\nInterviewer follow-up: \'How would you use this sequence number to identify accounts that skipped step 3 and went directly from step 2 to step 4?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-m49',
    title: 'Above-Average Balance Accounts',
    company: 'Revolut',
    companyDomain: 'revolut.com',
    difficulty: 'Medium',
    tags: ['scalar subquery', 'WHERE', 'AVG'],
    roles: ['PA', 'DA', 'BA'],
    priority: 1,
    estimatedMin: 8,
    datamartId: 'fintech',
    prompt: 'The product team wants to identify accounts with above-average balances to target for premium investment offerings. Return the account id, user id, balance, and currency for all accounts whose balance exceeds the overall average balance, ordered by balance descending.',
    expectedColumns: ['account_id', 'user_id', 'balance', 'currency'],
    expectedRowCount: 7,
    checkValues: [{ account_id: '5' }],
    solution: 'SELECT account_id, user_id, balance, currency\nFROM accounts\nWHERE balance > (SELECT AVG(balance) FROM accounts)\nORDER BY balance DESC',
    debrief: 'The overall average balance is approximately $7,375. Seven accounts exceed it: account 5 (25,000), 8 (15,000), 2 (12,000), 10 (9,300), 4 (8,750), 15 (8,200), and 13 (7,600). The scalar subquery runs once and its result is compared against each row — this is simpler than a self-join and equivalent to a HAVING filter after GROUP BY.',
    sqliteNote: null,
  },

  {
    id: 'sql-m51',
    title: 'Cumulative Interactions per User',
    company: 'TikTok',
    companyDomain: 'tiktok.com',
    difficulty: 'Medium',
    tags: ['COUNT OVER', 'running total', 'window function', 'ROWS BETWEEN'],
    roles: ['PA', 'DA'],
    priority: 2,
    estimatedMin: 8,
    datamartId: 'consumer',
    prompt: 'The engagement team is building a growth chart showing how each user\'s total interactions accumulate over time. Can you pull a running count of interactions per user, row by row?',
    expectedColumns: ['interaction_id', 'user_id', 'action', 'occurred_at', 'cumulative_interactions'],
    expectedRowCount: 30,
    checkValues: [{ interaction_id: '1', cumulative_interactions: '1' }],
    solution: 'SELECT interaction_id, user_id, action, occurred_at,\n       COUNT(*) OVER (PARTITION BY user_id ORDER BY occurred_at ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS cumulative_interactions\nFROM interactions\nORDER BY user_id, occurred_at',
    debrief: 'What the stakeholder wants: For each interaction row, the running total of that user\'s interactions up to and including that row — used to plot each user\'s engagement growth curve over time.\n\nAmbiguities resolved: \'Up to and including that row\' means ordered by occurred_at. If two interactions share the same date, the ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW frame includes each row individually. Counting by date (aggregating rows with the same date) would give a different result — clarify with the stakeholder which they want.\n\nSQL approach: COUNT(*) OVER (PARTITION BY user_id ORDER BY occurred_at ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) accumulates one per row within the user\'s partition. The explicit ROWS frame is best practice — without it, the default RANGE frame groups all rows with the same occurred_at value into the same boundary, potentially inflating counts for tied dates.\n\nWhat weak SQL looks like: Omitting the ROWS BETWEEN clause — with ORDER BY present, the default RANGE frame includes all rows with the same occurred_at as the current row in the running count, which can double-count interactions on the same day and make the cumulative total jump non-monotonically.\n\nInterviewer follow-up: \'How would you modify this to show running interaction count broken down by action type?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-m52',
    title: 'Dispute Status Dashboard',
    company: 'Stripe',
    companyDomain: 'stripe.com',
    difficulty: 'Medium',
    tags: ['COALESCE', 'NULL handling', 'JOIN'],
    roles: ['PA', 'DA', 'BA'],
    priority: 1,
    estimatedMin: 7,
    datamartId: 'fintech',
    prompt: 'The fraud ops team wants a dispute dashboard showing a clean status label for every dispute. Right now open disputes show up as blank — they need to read \'open\' instead.',
    expectedColumns: ['dispute_id', 'txn_id', 'opened_at', 'current_status'],
    expectedRowCount: 6,
    checkValues: [{ dispute_id: '3', current_status: 'open' }],
    solution: 'SELECT dispute_id, txn_id, opened_at, COALESCE(outcome, \'open\') AS current_status\nFROM disputes\nORDER BY opened_at',
    debrief: 'What the stakeholder wants: A dispute list with a human-readable status for every row — no NULLs reaching the dashboard where they render as blanks and confuse the ops team.\n\nAmbiguities resolved: The taxonomy has three states: \'won\', \'lost\', and \'open\'. Open disputes have a NULL outcome because no resolution has been recorded. The fix is to display NULL as \'open\' — this is a presentation decision; the underlying NULL should stay in the database.\n\nSQL approach: COALESCE(outcome, \'open\') returns the first non-NULL value. When outcome is populated it returns the outcome; when NULL it returns \'open\'. Single-column transformation, no JOIN required.\n\nWhat weak SQL looks like: CASE WHEN outcome IS NULL THEN \'open\' ELSE outcome END — functionally identical but more verbose; COALESCE is the idiomatic choice for simple NULL replacement. A common error is WHERE outcome IS NOT NULL before the COALESCE — this drops open disputes entirely, defeating the purpose of the dashboard.\n\nInterviewer follow-up: \'How would you add a column showing how many days each dispute has been open?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-m53',
    title: 'Account Active Days',
    company: 'Mixpanel',
    companyDomain: 'mixpanel.com',
    difficulty: 'Medium',
    tags: ['julianday', 'date arithmetic', 'GROUP BY', 'MIN', 'MAX'],
    roles: ['PA', 'DA'],
    priority: 2,
    estimatedMin: 9,
    datamartId: 'saas',
    prompt: 'The customer success team wants to measure how long each account has been actively using the product, defined as days between their first and last recorded event. Only include accounts that have at least one event. Return account id, company name, first event date, last event date, and total active days.',
    expectedColumns: ['account_id', 'company_name', 'first_event', 'last_event', 'active_days'],
    expectedRowCount: 8,
    checkValues: [{ account_id: '1', active_days: '34' }],
    solution: 'SELECT a.account_id, a.company_name,\n       MIN(e.occurred_at) AS first_event,\n       MAX(e.occurred_at) AS last_event,\n       CAST(julianday(MAX(e.occurred_at)) - julianday(MIN(e.occurred_at)) AS INTEGER) AS active_days\nFROM accounts a\nJOIN events e ON a.account_id = e.account_id\nGROUP BY a.account_id, a.company_name\nORDER BY active_days DESC',
    debrief: 'Account 1 has events spanning Jan 2 to Feb 5 — 34 days. Account 8 has only one event so active_days = 0. The JOIN (not LEFT JOIN) naturally excludes accounts 9–15 which have no events. MIN and MAX in GROUP BY aggregations extract the bookend dates without a window function.',
    sqliteNote: 'Uses julianday() for date arithmetic. In PostgreSQL use: (MAX(e.occurred_at)::date - MIN(e.occurred_at)::date) AS active_days.',
  },

  {
    id: 'sql-m54',
    title: 'Accounts With Disputed Transactions',
    company: 'Visa',
    companyDomain: 'visa.com',
    difficulty: 'Medium',
    tags: ['EXISTS', 'correlated subquery', 'JOIN'],
    roles: ['PA', 'DA', 'BA'],
    priority: 1,
    estimatedMin: 9,
    datamartId: 'fintech',
    prompt: 'The risk team needs to flag accounts that have at least one disputed transaction so their owners can be notified. Return the account id and user id for each account with a disputed transaction, ordered by account id.',
    expectedColumns: ['account_id', 'user_id'],
    expectedRowCount: 6,
    checkValues: [{ account_id: '1' }],
    solution: 'SELECT a.account_id, a.user_id\nFROM accounts a\nWHERE EXISTS (\n  SELECT 1 FROM transactions t\n  WHERE t.account_id = a.account_id\n    AND t.status = \'disputed\'\n)\nORDER BY a.account_id',
    debrief: 'EXISTS short-circuits — it stops scanning as soon as one matching row is found, making it efficient for "has at least one" checks. Accounts 1, 4, 5, 8, 11, and 15 each have at least one disputed transaction. A JOIN with DISTINCT produces the same result but EXISTS communicates intent more clearly.',
    sqliteNote: null,
  },

  {
    id: 'sql-m56',
    title: 'Fitness Content Audience',
    company: 'YouTube',
    companyDomain: 'youtube.com',
    difficulty: 'Medium',
    tags: ['IN subquery', 'DISTINCT', 'JOIN'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 8,
    datamartId: 'consumer',
    prompt: 'The content partnerships team wants to identify all users who have engaged with any fitness category content, to build a lookalike audience for a health brand campaign. Return the distinct user ids of everyone who has interacted with at least one fitness content piece, ordered by user id.',
    expectedColumns: ['user_id'],
    expectedRowCount: 9,
    checkValues: [{ user_id: '1' }],
    solution: 'SELECT DISTINCT i.user_id\nFROM interactions i\nWHERE i.content_id IN (\n  SELECT content_id FROM content WHERE category = \'fitness\'\n)\nORDER BY i.user_id',
    debrief: 'The IN subquery returns content ids 1 and 6, which are both fitness category. Nine distinct users have interacted with at least one: users 1–8 (from content 1) plus user 11 (from content 6). DISTINCT is required since a user could interact multiple times with the same fitness piece.',
    sqliteNote: null,
  },

  {
    id: 'sql-m57',
    title: 'Product Sales Rank',
    company: 'Amazon',
    companyDomain: 'amazon.com',
    difficulty: 'Medium',
    tags: ['DENSE_RANK', 'CTE', 'window function', 'JOIN'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 10,
    datamartId: 'ecomm',
    prompt: 'The merchandising team wants a product sales leaderboard — which products are being ordered the most? Rank them so tied products get the same position.',
    expectedColumns: ['product_id', 'name', 'times_ordered', 'rnk'],
    expectedRowCount: 9,
    checkValues: [{ product_id: '1', times_ordered: '6', rnk: '1' }],
    solution: 'WITH order_counts AS (\n  SELECT oi.product_id, p.name, COUNT(*) AS times_ordered\n  FROM order_items oi\n  JOIN products p ON oi.product_id = p.product_id\n  GROUP BY oi.product_id, p.name\n)\nSELECT product_id, name, times_ordered,\n       DENSE_RANK() OVER (ORDER BY times_ordered DESC) AS rnk\nFROM order_counts\nORDER BY rnk, product_id',
    debrief: 'What the stakeholder wants: A ranked list of products by order frequency, with tied products sharing the same rank so no product is unfairly penalized for a tie.\n\nAmbiguities resolved: \'Ordered the most\' means times appearing in order_items (line count), not total units or revenue. \'Same rank for ties\' disambiguates between RANK and DENSE_RANK: both give identical numbers to tied rows, but RANK skips subsequent ranks while DENSE_RANK does not. For a leaderboard, DENSE_RANK is preferred so rank 3 follows rank 2 without gaps.\n\nSQL approach: A CTE aggregates order_items joined to products to get times_ordered per product. The outer SELECT applies DENSE_RANK() OVER (ORDER BY times_ordered DESC). Products not in order_items (discontinued) are excluded by the JOIN — a LEFT JOIN would retain them at rank N with 0 orders.\n\nWhat weak SQL looks like: ORDER BY times_ordered DESC LIMIT 10 without a rank function — doesn\'t handle ties correctly; a tied 10th and 11th product produces an arbitrary cutoff. Using RANK — if products 2 and 3 both have 4 orders and share rank 2, RANK skips rank 3 and assigns rank 4 to the next product, creating a gap that confuses stakeholders reading a leaderboard.\n\nInterviewer follow-up: \'How would you show only the top 3 ranks, including all tied products?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-m58',
    title: 'Active Accounts in January',
    company: 'HubSpot',
    companyDomain: 'hubspot.com',
    difficulty: 'Medium',
    tags: ['HAVING', 'COUNT', 'date filter', 'GROUP BY'],
    roles: ['PA', 'DA', 'BA'],
    priority: 2,
    estimatedMin: 8,
    datamartId: 'saas',
    prompt: 'The product team wants to identify accounts that were heavily engaged in January 2024, defined as more than 3 events in that month. Return account id and their January event count, ordered by event count descending.',
    expectedColumns: ['account_id', 'jan_events'],
    expectedRowCount: 5,
    checkValues: [{ account_id: '1', jan_events: '10' }],
    solution: 'SELECT account_id, COUNT(*) AS jan_events\nFROM events\nWHERE occurred_at LIKE \'2024-01%\'\nGROUP BY account_id\nHAVING COUNT(*) > 3\nORDER BY jan_events DESC',
    debrief: 'Five accounts logged more than 3 events in January: account 1 (10), 2 (8), 3 (8), 4 (7), and 5 (4). Account 6 logged only 1 event and is excluded. The LIKE filter on a text date column works because dates are stored in YYYY-MM-DD format — prefix matching is reliable.',
    sqliteNote: null,
  },

  {
    id: 'sql-m60',
    title: 'No-Show Rate by Provider',
    company: 'Doximity',
    companyDomain: 'doximity.com',
    difficulty: 'Medium',
    tags: ['GROUP BY', 'SUM', 'ROUND', 'conditional aggregation'],
    roles: ['PA', 'DA', 'BA'],
    priority: 1,
    estimatedMin: 8,
    datamartId: 'health',
    prompt: 'The operations team tracks provider no-show rates to identify scheduling inefficiencies. For each provider, return their total appointment count, number of no-shows, and no-show rate as a percentage rounded to 1 decimal. Order by no-show rate descending.',
    expectedColumns: ['provider_id', 'total_appts', 'no_shows', 'no_show_rate'],
    expectedRowCount: 6,
    checkValues: [],
    solution: 'SELECT provider_id,\n       COUNT(*) AS total_appts,\n       SUM(no_show) AS no_shows,\n       ROUND(100.0 * SUM(no_show) / COUNT(*), 1) AS no_show_rate\nFROM appointments\nGROUP BY provider_id\nORDER BY no_show_rate DESC',
    debrief: 'Provider 1 has the highest no-show rate at 40% (4 out of 10 appointments). Provider 5 is at 33.3%, provider 2 at 25%, and providers 3, 4, and 6 have zero no-shows. SUM(no_show) works because the column stores 1 for no-show and 0 otherwise — summing equals counting the positive flag.',
    sqliteNote: null,
  },

  {
    id: 'sql-m61',
    title: 'Content Engagement Diversity',
    company: 'TikTok',
    companyDomain: 'tiktok.com',
    difficulty: 'Medium',
    tags: ['COUNT DISTINCT', 'LEFT JOIN', 'GROUP BY'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 8,
    datamartId: 'consumer',
    prompt: 'The algorithm team uses diversity of engagement actions as a quality signal — content that attracts views, likes, saves, and comments is healthier than content that only gets views. For each content piece, return content id, content type, category, and the number of distinct interaction action types it received.',
    expectedColumns: ['content_id', 'content_type', 'category', 'unique_actions'],
    expectedRowCount: 8,
    checkValues: [{ content_id: '1', unique_actions: '5' }],
    solution: 'SELECT c.content_id, c.content_type, c.category,\n       COUNT(DISTINCT i.action) AS unique_actions\nFROM content c\nLEFT JOIN interactions i ON c.content_id = i.content_id\nGROUP BY c.content_id, c.content_type, c.category\nORDER BY unique_actions DESC',
    debrief: 'Content 1 tops engagement diversity with 5 distinct action types (view, like, share, save, comment). Content 5 follows with 4. LEFT JOIN is used here to include all 8 content pieces even if some had zero interactions — in this dataset all do, but the pattern is correct for production data.',
    sqliteNote: null,
  },

  {
    id: 'sql-m62',
    title: 'Premium vs Non-Premium Spending',
    company: 'Amazon',
    companyDomain: 'amazon.com',
    difficulty: 'Medium',
    tags: ['GROUP BY', 'AVG', 'JOIN', 'CASE WHEN'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 8,
    datamartId: 'ecomm',
    prompt: 'The product team wants to quantify the spending difference between premium and non-premium customers. Join users to orders and compute the order count and average order subtotal for each premium segment. Use all order statuses. Return is_premium (1 or 0), order count, and average subtotal rounded to 2 decimal places.',
    expectedColumns: ['is_premium', 'order_count', 'avg_subtotal'],
    expectedRowCount: 2,
    checkValues: [],
    solution: 'SELECT u.is_premium,\n       COUNT(o.order_id) AS order_count,\n       ROUND(AVG(o.subtotal), 2) AS avg_subtotal\nFROM orders o\nJOIN users u ON o.user_id = u.user_id\nGROUP BY u.is_premium\nORDER BY u.is_premium DESC',
    debrief: 'Premium users (is_premium=1) account for 17 orders; non-premium for 11. Premium average subtotals tend to be higher because premium users like user 5 and user 9 make frequent high-value purchases. Note: user 13 is premium but has no orders, so is excluded from the JOIN result.',
    sqliteNote: null,
  },

  {
    id: 'sql-m64',
    title: 'Most Recent Transaction per Account',
    company: 'Stripe',
    companyDomain: 'stripe.com',
    difficulty: 'Medium',
    tags: ['ROW_NUMBER', 'subquery', 'window function', 'PARTITION BY'],
    roles: ['PA', 'DA', 'BA'],
    priority: 1,
    estimatedMin: 10,
    datamartId: 'fintech',
    prompt: 'The account management team wants a quick portfolio snapshot — just the most recent transaction for each account. What did each account transact last?',
    expectedColumns: ['account_id', 'txn_id', 'amount', 'occurred_at', 'category'],
    expectedRowCount: 15,
    checkValues: [{ account_id: '1', txn_id: '38' }],
    solution: 'SELECT account_id, txn_id, amount, occurred_at, category\nFROM (\n  SELECT account_id, txn_id, amount, occurred_at, category,\n         ROW_NUMBER() OVER (PARTITION BY account_id ORDER BY occurred_at DESC) AS rn\n  FROM transactions\n)\nWHERE rn = 1\nORDER BY account_id',
    debrief: 'What the stakeholder wants: One row per account showing that account\'s latest transaction, so account managers can see at a glance what each customer did most recently — category, amount, and date.\n\nAmbiguities resolved: \'Most recent\' means by occurred_at descending, not by amount or txn_id. One row per account — if an account has two transactions on the same date, one is selected. The tie-breaking rule (e.g., higher txn_id) should be stated explicitly; here the solution relies on occurred_at DESC.\n\nSQL approach: ROW_NUMBER() OVER (PARTITION BY account_id ORDER BY occurred_at DESC) labels the most recent transaction per account as rn=1. A subquery wraps this; the outer WHERE rn = 1 filters to one row per account. This is the standard latest-per-group pattern.\n\nWhat weak SQL looks like: MAX(occurred_at) GROUP BY account_id followed by a self-join — this retrieves the date but requires a second JOIN to get amount and category. It also breaks on ties where two transactions share the same max date, potentially returning multiple rows per account.\n\nInterviewer follow-up: \'How would you modify this to return the latest two transactions per account instead of one?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-m66',
    title: 'Order Status Distribution',
    company: 'Shopify',
    companyDomain: 'shopify.com',
    difficulty: 'Medium',
    tags: ['GROUP BY', 'COUNT', 'ROUND'],
    roles: ['PA', 'DA', 'BA'],
    priority: 1,
    estimatedMin: 6,
    datamartId: 'ecomm',
    prompt: 'The operations team tracks order health by status. Count orders by status and include the percentage of total orders for each status, rounded to 1 decimal. Return status, count, and percentage, ordered by count descending.',
    expectedColumns: ['status', 'order_count', 'pct_of_total'],
    expectedRowCount: 3,
    checkValues: [{ status: 'completed', order_count: '23' }],
    solution: 'SELECT status,\n       COUNT(*) AS order_count,\n       ROUND(100.0 * COUNT(*) / (SELECT COUNT(*) FROM orders), 1) AS pct_of_total\nFROM orders\nGROUP BY status\nORDER BY order_count DESC',
    debrief: 'Completed: 23 orders (82.1%), returned: 3 (10.7%), cancelled: 2 (7.1%). The scalar subquery in the SELECT computes the denominator once and is reused per row. This is cleaner than a self-join for percentage-of-total calculations and widely supported across SQL dialects.',
    sqliteNote: null,
  },

  {
    id: 'sql-m70',
    title: 'Orders in the Last 90 Days',
    company: 'Amazon',
    companyDomain: 'amazon.com',
    difficulty: 'Medium',
    tags: ['julianday', 'date arithmetic', 'scalar subquery'],
    roles: ['PA', 'DA'],
    priority: 2,
    estimatedMin: 9,
    datamartId: 'ecomm',
    prompt: 'The recent-activity report flags orders placed within the last 90 days of the dataset. Use the maximum order date in the table as your reference point. Return order id, user id, order date, status, and subtotal for qualifying orders, sorted by date descending.',
    expectedColumns: ['order_id', 'user_id', 'created_at', 'status', 'subtotal'],
    expectedRowCount: 6,
    checkValues: [{ order_id: '27' }],
    solution: 'SELECT order_id, user_id, created_at, status, subtotal\nFROM orders\nWHERE julianday(created_at) >= julianday((SELECT MAX(created_at) FROM orders)) - 90\nORDER BY created_at DESC',
    debrief: 'The dataset\'s latest order is 2024-04-30, so the 90-day window starts 2024-01-31. Six orders qualify: 26, 28, 24, 20, 27, 16. Using MAX(created_at) as the anchor rather than a hardcoded date makes the query self-updating as new rows are appended.',
    sqliteNote: 'Uses julianday() for date arithmetic. In PostgreSQL use: WHERE created_at::date >= (SELECT MAX(created_at::date) FROM orders) - INTERVAL \'90 days\'.',
  },

  {
    id: 'sql-m71',
    title: 'Active Products Above Average Price',
    company: 'Amazon',
    companyDomain: 'amazon.com',
    difficulty: 'Medium',
    tags: ['scalar subquery', 'WHERE', 'AVG', 'IS active filter'],
    roles: ['PA', 'DA', 'BA'],
    priority: 1,
    estimatedMin: 8,
    datamartId: 'ecomm',
    prompt: 'The pricing team wants to identify active products priced above the average list price of all active products, to assess whether their premium tier is correctly sized. Return product id, name, category, and list price for qualifying products, ordered by list price descending.',
    expectedColumns: ['product_id', 'name', 'category', 'list_price'],
    expectedRowCount: 3,
    checkValues: [{ product_id: '3', list_price: '249.99' }],
    solution: 'SELECT product_id, name, category, list_price\nFROM products\nWHERE is_active = 1\n  AND list_price > (SELECT AVG(list_price) FROM products WHERE is_active = 1)\nORDER BY list_price DESC',
    debrief: 'The average list price of the 9 active products is approximately $88.88. Three products exceed it: Smart Watch ($249.99), Winter Jacket ($159.99), and Wireless Headphones ($89.99). Product 10 (Plant Pot) is inactive and is excluded from both the filter and the average calculation.',
    sqliteNote: null,
  },

  {
    id: 'sql-m72',
    title: 'Merchant Transaction Summary',
    company: 'Visa',
    companyDomain: 'visa.com',
    difficulty: 'Medium',
    tags: ['JOIN', 'GROUP BY', 'SUM', 'COUNT'],
    roles: ['PA', 'DA', 'BA'],
    priority: 1,
    estimatedMin: 8,
    datamartId: 'fintech',
    prompt: 'The payments analytics team needs a merchant-level report showing transaction volume and total spend. Join merchants to transactions and return merchant name, transaction count, and total transaction amount (rounded to 2 decimal places), ordered by transaction count descending, then total amount descending.',
    expectedColumns: ['merchant_name', 'txn_count', 'total_amount'],
    expectedRowCount: 10,
    checkValues: [{ merchant_name: 'Amazon', txn_count: '9' }],
    solution: 'SELECT m.name AS merchant_name, COUNT(t.txn_id) AS txn_count, ROUND(SUM(t.amount), 2) AS total_amount\nFROM merchants m\nJOIN transactions t ON m.merchant_id = t.merchant_id\nGROUP BY m.merchant_id, m.name\nORDER BY txn_count DESC, total_amount DESC',
    debrief: 'Amazon and Whole Foods tie at 9 transactions each. Amazon ranks first due to its higher total amount ($1,719.99 vs $649.50). The flagged merchants QuickTransfer and FastCash appear in this list — pairing with is_flagged would quickly surface high-risk merchant concentration.',
    sqliteNote: null,
  },

  {
    id: 'sql-m73',
    title: 'Patients With Multiple Prescriptions',
    company: 'Doximity',
    companyDomain: 'doximity.com',
    difficulty: 'Medium',
    tags: ['HAVING', 'COUNT', 'GROUP BY'],
    roles: ['PA', 'DA', 'BA'],
    priority: 2,
    estimatedMin: 7,
    datamartId: 'health',
    prompt: 'The care coordination team wants to identify patients on multiple medications — a key indicator of chronic condition complexity. Return patient id and prescription count for all patients with more than one prescription, ordered by count descending.',
    expectedColumns: ['patient_id', 'rx_count'],
    expectedRowCount: 5,
    checkValues: [{ patient_id: '1', rx_count: '2' }],
    solution: 'SELECT patient_id, COUNT(*) AS rx_count\nFROM prescriptions\nGROUP BY patient_id\nHAVING COUNT(*) > 1\nORDER BY rx_count DESC, patient_id ASC',
    debrief: 'Five patients have more than one prescription: patients 1, 2, 3, 10, and 11, each with exactly 2. In clinical analytics this cohort would be flagged for medication reconciliation review. HAVING COUNT(*) > 1 is the canonical pattern — a WHERE clause cannot reference the aggregate.',
    sqliteNote: null,
  },

  {
    id: 'sql-m74',
    title: 'Session Duration in Minutes',
    company: 'TikTok',
    companyDomain: 'tiktok.com',
    difficulty: 'Medium',
    tags: ['julianday', 'date arithmetic', 'ROUND'],
    roles: ['PA', 'DA'],
    priority: 2,
    estimatedMin: 9,
    datamartId: 'consumer',
    prompt: 'The engagement team is building a session-duration distribution to understand how long users actually spend in the app. They need the length of every session in minutes.',
    expectedColumns: ['session_id', 'user_id', 'device_os', 'duration_min'],
    expectedRowCount: 20,
    checkValues: [{ session_id: '15', duration_min: '70' }],
    solution: 'SELECT session_id, user_id, device_os,\n       ROUND((julianday(ended_at) - julianday(started_at)) * 1440) AS duration_min\nFROM sessions\nORDER BY duration_min DESC',
    debrief: 'What the stakeholder wants: Duration in minutes for every session so the team can plot a distribution and understand typical session depth — p50, p90, and outlier sessions.\n\nAmbiguities resolved: Duration is from started_at to ended_at. Sessions with a NULL ended_at (still active or improperly closed) return NULL from julianday subtraction and are effectively excluded. If the team wants to include in-progress sessions, COALESCE(ended_at, CURRENT_TIMESTAMP) handles them. Duration is rounded to the nearest whole minute.\n\nSQL approach: julianday(ended_at) - julianday(started_at) gives elapsed fractional days. Multiplying by 1440 (minutes per day) converts to minutes. ROUND(...) rounds to the nearest whole number. SQLite has no native timestamp subtraction — julianday is the standard workaround.\n\nWhat weak SQL looks like: Using CAST instead of ROUND — CAST truncates toward zero, so a 4.9-minute session becomes 4 minutes instead of 5, understating duration. Or directly subtracting string-format timestamps — SQLite returns 0 for string arithmetic because timestamp subtraction is not defined on text types.\n\nInterviewer follow-up: \'How would you calculate the median session duration across all users?\'',
    sqliteNote: 'Uses julianday() for time arithmetic. In PostgreSQL use: ROUND(EXTRACT(EPOCH FROM (ended_at::timestamp - started_at::timestamp)) / 60) AS duration_min.',
  },

  {
    id: 'sql-m75',
    title: 'Flagged Merchant Risk Report',
    company: 'Stripe',
    companyDomain: 'stripe.com',
    difficulty: 'Medium',
    tags: ['JOIN', 'WHERE filter', 'GROUP BY', 'SUM'],
    roles: ['PA', 'DA', 'BA'],
    priority: 1,
    estimatedMin: 8,
    datamartId: 'fintech',
    prompt: 'The fraud team needs a summary of transactions routed through flagged (high-risk) merchants. Join transactions to merchants, filter to flagged merchants only, and return merchant name, country, transaction count, and total transaction amount, ordered by total amount descending.',
    expectedColumns: ['merchant_name', 'country', 'flagged_txns', 'total_amount'],
    expectedRowCount: 3,
    checkValues: [{ merchant_name: 'QuickTransfer', flagged_txns: '2' }],
    solution: 'SELECT m.name AS merchant_name, m.country, COUNT(t.txn_id) AS flagged_txns, ROUND(SUM(t.amount), 2) AS total_amount\nFROM merchants m\nJOIN transactions t ON m.merchant_id = t.merchant_id\nWHERE m.is_flagged = 1\nGROUP BY m.merchant_id, m.name, m.country\nORDER BY total_amount DESC',
    debrief: 'QuickTransfer (Nigeria) leads with $4,450 across 2 transactions, followed by CryptoXchange (China) at $2,200, and FastCash (Brazil) at $1,205. All three are foreign-jurisdiction merchants with suspicious patterns. Filtering on is_flagged = 1 narrows the JOIN output to the 3 risk-flag merchants before aggregation.',
    sqliteNote: null,
  },

  // ─── HARD (6 problems) ────────────────────────────────────────────────────

  {
    id: 'sql-h01',
    title: 'Jan-to-Feb User Retention',
    company: 'Mixpanel',
    companyDomain: 'mixpanel.com',
    difficulty: 'Hard',
    isFree: false,
    tags: ['CTE', 'retention', 'LEFT JOIN', 'COUNT DISTINCT', 'cohort'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 18,
    datamartId: 'saas',
    prompt: 'The product team wants to know how well we\'re retaining users month over month. What percentage of users who were active in January 2024 were still active in February?',
    expectedColumns: ['jan_active', 'retained_in_feb', 'retention_pct'],
    expectedRowCount: 1,
    checkValues: [{ jan_active: '12', retained_in_feb: '7', retention_pct: '58.3' }],
    solution: 'WITH jan_users AS (\n  SELECT DISTINCT user_id FROM events\n  WHERE occurred_at BETWEEN \'2024-01-01\' AND \'2024-01-31\'\n),\nfeb_users AS (\n  SELECT DISTINCT user_id FROM events\n  WHERE occurred_at BETWEEN \'2024-02-01\' AND \'2024-02-28\'\n)\nSELECT\n  COUNT(j.user_id) AS jan_active,\n  COUNT(f.user_id) AS retained_in_feb,\n  ROUND(100.0 * COUNT(f.user_id) / COUNT(j.user_id), 1) AS retention_pct\nFROM jan_users j\nLEFT JOIN feb_users f ON j.user_id = f.user_id',
    debrief: 'What the stakeholder wants: A single retention rate showing how many January-active users returned in February — a first-order stickiness signal that feeds into the monthly health dashboard.\n\nAmbiguities resolved: \'Active\' means at least one event recorded in the events table — not just registered. \'Retained\' means any event in February, not necessarily the same type as January. Whether to use calendar-month boundaries or a strict 30-day window is a choice — calendar months are used here. All three values (jan_active, retained_in_feb, retention_pct) are returned in a single summary row.\n\nSQL approach: Two CTEs collect distinct user_ids for January and February separately. A LEFT JOIN from jan_users to feb_users means unretained users produce a NULL in f.user_id. COUNT(f.user_id) skips NULLs, giving the correct retained count without a WHERE clause. Retention pct = 100.0 * retained / total.\n\nWhat weak SQL looks like: A correlated subquery per user to check February activity — logically correct but O(n squared) on large event tables. Or INNER JOIN from jan_users to feb_users — this drops unretained users from the denominator, making retention appear to be 100%.\n\nInterviewer follow-up: \'Which specific users dropped off between January and February, and what do they have in common?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-h02',
    title: 'Consecutive Order Day Streak',
    company: 'DoorDash',
    companyDomain: 'doordash.com',
    difficulty: 'Hard',
    isFree: false,
    tags: ['gap-and-island', 'ROW_NUMBER', 'julianday', 'CTE', 'window functions'],
    roles: ['PA', 'DA'],
    priority: 1,
    estimatedMin: 20,
    datamartId: 'ecomm',
    prompt: 'The loyalty team wants to identify customers who placed orders on 3 or more consecutive calendar days — a strong engagement signal for a streak-based rewards program. Return user_id and email for all qualifying customers.',
    expectedColumns: ['user_id', 'email'],
    expectedRowCount: 1,
    checkValues: [{ user_id: '5', email: 'eve@example.com' }],
    solution: 'WITH distinct_order_dates AS (\n  SELECT DISTINCT user_id, created_at FROM orders\n),\nordered_dates AS (\n  SELECT user_id, created_at,\n    julianday(created_at) - ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY created_at) AS grp\n  FROM distinct_order_dates\n),\nstreaks AS (\n  SELECT user_id, grp, COUNT(*) AS streak_len\n  FROM ordered_dates\n  GROUP BY user_id, grp\n  HAVING streak_len >= 3\n)\nSELECT DISTINCT u.user_id, u.email\nFROM streaks s\nJOIN users u ON s.user_id = u.user_id\nORDER BY u.user_id',
    debrief: 'Only user 5 (eve) qualifies — she placed orders on 2024-01-10, 11, and 12. The gap-and-island pattern: subtracting ROW_NUMBER() from the day value groups consecutive dates into the same "island" because both increment by 1 each day. Non-consecutive dates produce different grp values. The DISTINCT in the first CTE is essential — without it, a user with two orders on the same day inflates the row count and breaks the arithmetic. The weak answer tries self-joining orders to find pairs, which fails to generalize to N-day streaks and misses the island grouping.',
    sqliteNote: 'Uses julianday() for date arithmetic — SQLite-specific. Equivalent to DATEDIFF in other dialects.',
  },

  {
    id: 'sql-h03',
    title: 'Nov-to-Dec Content Engagement Retention',
    company: 'Substack',
    companyDomain: 'substack.com',
    difficulty: 'Hard',
    isFree: false,
    tags: ['CTE', 'retention', 'strftime', 'LEFT JOIN', 'cohort'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 15,
    datamartId: 'consumer',
    prompt: 'The growth team wants to measure platform stickiness. Of users who engaged with any content in November 2023, what percentage returned to engage in December 2023? Return nov_active, retained_in_dec, and retention_pct (rounded to 1 decimal) in a single row.',
    expectedColumns: ['nov_active', 'retained_in_dec', 'retention_pct'],
    expectedRowCount: 1,
    checkValues: [{ nov_active: '13', retained_in_dec: '2', retention_pct: '15.4' }],
    solution: 'WITH nov_users AS (\n  SELECT DISTINCT user_id FROM interactions\n  WHERE strftime(\'%Y-%m\', occurred_at) = \'2023-11\'\n),\ndec_users AS (\n  SELECT DISTINCT user_id FROM interactions\n  WHERE strftime(\'%Y-%m\', occurred_at) = \'2023-12\'\n)\nSELECT\n  COUNT(n.user_id) AS nov_active,\n  COUNT(d.user_id) AS retained_in_dec,\n  ROUND(100.0 * COUNT(d.user_id) / COUNT(n.user_id), 1) AS retention_pct\nFROM nov_users n\nLEFT JOIN dec_users d ON n.user_id = d.user_id',
    debrief: '13 users engaged in November. Only 2 returned in December — a 15.4% retention rate. Users 9 (ivan) and 12 (lisa) are the only ones who interacted with content in both months. The extremely low retention signals that most users engage with content opportunistically rather than habitually — a key product problem worth escalating. The root cause: only two content pieces published in December (content 7 on Dec 1 and content 8 on Dec 5) versus eight in the prior period — a publishing volume problem, not necessarily a demand problem.',
    sqliteNote: null,
  },

  {
    id: 'sql-h04',
    title: 'Q1 2023 Cohort Repeat Purchase Rate',
    company: 'Shopify',
    companyDomain: 'shopify.com',
    difficulty: 'Hard',
    isFree: false,
    tags: ['CTE', 'cohort analysis', 'LEFT JOIN', 'MIN', 'repeat purchase'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 18,
    datamartId: 'ecomm',
    prompt: 'The retention team wants to evaluate the quality of our earliest customer cohort. Of the users who placed their very first order in Q1 2023, how many came back and bought again?',
    expectedColumns: ['cohort_size', 'repeat_buyers', 'repeat_rate'],
    expectedRowCount: 1,
    checkValues: [{ cohort_size: '4', repeat_buyers: '4', repeat_rate: '100.0' }],
    solution: 'WITH first_orders AS (\n  SELECT user_id, MIN(created_at) AS first_order_date\n  FROM orders\n  GROUP BY user_id\n),\nq1_cohort AS (\n  SELECT user_id FROM first_orders\n  WHERE strftime(\'%Y-%m\', first_order_date) BETWEEN \'2023-01\' AND \'2023-03\'\n),\nrepeat_buyers AS (\n  SELECT DISTINCT o.user_id\n  FROM orders o\n  JOIN first_orders fo ON o.user_id = fo.user_id\n  WHERE o.created_at > fo.first_order_date\n)\nSELECT\n  COUNT(q.user_id) AS cohort_size,\n  COUNT(r.user_id) AS repeat_buyers,\n  ROUND(100.0 * COUNT(r.user_id) / COUNT(q.user_id), 1) AS repeat_rate\nFROM q1_cohort q\nLEFT JOIN repeat_buyers r ON q.user_id = r.user_id',
    debrief: 'What the stakeholder wants: A cohort-level repeat purchase rate for the Q1 2023 first-order cohort — how many of those early adopters proved to be genuine repeat buyers vs. one-and-done.\n\nAmbiguities resolved: \'First order in Q1 2023\' means the user\'s MIN(created_at) falls in January, February, or March 2023 — not any order in Q1, but the very first order ever. \'Came back and bought again\' means at least one additional order after the first order date, regardless of status. Whether to count only completed orders for the repeat is a business decision worth surfacing.\n\nSQL approach: Three CTEs: (1) first_orders anchors each user\'s MIN(created_at). (2) q1_cohort filters to users whose first order falls in Q1 2023. (3) repeat_buyers finds users with any order dated after their first order date. A LEFT JOIN from q1_cohort to repeat_buyers measures how many cohort members hit the repeat condition.\n\nWhat weak SQL looks like: MAX(created_at) > MIN(created_at) per user in a single GROUP BY — simpler but incorrect for cohort-scoped analysis. It doesn\'t isolate Q1 2023 first-orderers vs. users who happened to order in Q1 and had already ordered before. Also misses the rate calculation.\n\nInterviewer follow-up: \'How would you extend this to a 3-month cohort retention curve — what percentage of Q1 2023 cohort ordered in Q2, Q3, and Q4 respectively?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-h05',
    title: 'Provider Below Practice Average',
    company: 'Teladoc',
    companyDomain: 'teladoc.com',
    difficulty: 'Hard',
    isFree: false,
    tags: ['CTE', 'CROSS JOIN', 'aggregation', 'outlier detection', 'CASE WHEN'],
    roles: ['DA', 'PA', 'BA'],
    priority: 2,
    estimatedMin: 20,
    datamartId: 'health',
    prompt: 'Operations is trying to identify providers who may need additional support. Which providers are significantly underperforming on appointment completion compared to the rest of the practice?',
    expectedColumns: ['name', 'total_appts', 'attended', 'completion_rate', 'practice_avg'],
    expectedRowCount: 1,
    checkValues: [{ name: 'Dr. Smith' }],
    solution: 'WITH provider_rates AS (\n  SELECT provider_id,\n    COUNT(*) AS total_appts,\n    SUM(CASE WHEN no_show = 0 THEN 1 ELSE 0 END) AS attended,\n    100.0 * SUM(CASE WHEN no_show = 0 THEN 1 ELSE 0 END) / COUNT(*) AS completion_rate\n  FROM appointments\n  GROUP BY provider_id\n),\npractice_avg AS (\n  SELECT AVG(completion_rate) AS avg_rate FROM provider_rates\n)\nSELECT p.name, pr.total_appts, pr.attended,\n  ROUND(pr.completion_rate, 1) AS completion_rate,\n  ROUND(pa.avg_rate, 1) AS practice_avg\nFROM provider_rates pr\nJOIN providers p ON pr.provider_id = p.provider_id\nCROSS JOIN practice_avg pa\nWHERE pr.completion_rate < pa.avg_rate - 10\nORDER BY pr.completion_rate',
    debrief: 'What the stakeholder wants: A list of providers whose completion rate falls meaningfully below the practice-wide average — the clinical operations team uses this to trigger coaching conversations or workload reviews.\n\nAmbiguities resolved: \'Significantly underperforming\' must be defined numerically — the solution uses 10 percentage points below the practice average as the threshold. The candidate should state this threshold explicitly and note it is a business parameter. \'Completion rate\' = appointments where no_show = 0, as a percentage of total appointments. Whether to include all specialties or scope to one is worth asking — the solution covers all providers.\n\nSQL approach: First CTE computes completion_rate per provider via CASE WHEN aggregation. Second CTE computes the practice-wide average across providers. CROSS JOIN attaches the scalar average to every provider row without a join key. WHERE filters to providers more than 10 points below average.\n\nWhat weak SQL looks like: A subquery in the WHERE clause that re-computes AVG(completion_rate) per row — logically correct but re-executes the aggregation on every row. CROSS JOIN with a CTE is the idiomatic way to compare each row to a global scalar computed once.\n\nInterviewer follow-up: \'If you wanted to flag providers whose completion rate is in the bottom quartile rather than 10 points below average, how would you rewrite this?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-h06',
    title: 'Flagged Merchant Dispute Exposure',
    company: 'Stripe',
    companyDomain: 'stripe.com',
    difficulty: 'Hard',
    isFree: false,
    tags: ['multi-table JOIN', 'fraud detection', 'subquery', 'aggregation'],
    roles: ['DA', 'PA'],
    priority: 1,
    estimatedMin: 18,
    datamartId: 'fintech',
    prompt: 'The fraud team is building a report on high-risk transaction exposure. Find all transactions where the merchant is flagged (is_flagged = 1) AND a dispute exists for that transaction. For each such transaction, return user_id, email, txn_id, merchant_name, and disputed_amount. Order by disputed_amount descending.',
    expectedColumns: ['user_id', 'email', 'txn_id', 'merchant_name', 'disputed_amount'],
    expectedRowCount: 2,
    checkValues: [{ merchant_name: 'QuickTransfer' }],
    solution: 'SELECT u.user_id, u.email, t.txn_id, m.name AS merchant_name, d.amount AS disputed_amount\nFROM transactions t\nJOIN merchants m ON t.merchant_id = m.merchant_id\nJOIN accounts a ON t.account_id = a.account_id\nJOIN users u ON a.user_id = u.user_id\nJOIN disputes d ON t.txn_id = d.txn_id\nWHERE m.is_flagged = 1\nORDER BY d.amount DESC',
    debrief: 'Two transactions qualify — both via QuickTransfer (merchant 7): transaction 31 ($3,500, user 5 via account 8) and transaction 9 ($950, user 3 via account 5). The four-table JOIN chain is required because: risk signal lives on merchants, account bridges to user identity, and dispute amount lives in the disputes table. The weak answer joins only transactions and merchants, missing the user identity needed for the fraud report. CryptoXchange and FastCash have flagged transactions but no associated dispute — they do not appear. The follow-up: "What if we wanted transactions at flagged merchants even without a dispute?" changes the JOIN to a LEFT JOIN and adds a WHERE m.is_flagged = 1.',
    sqliteNote: null,
  },

  {
    id: 'sql-h07',
    title: 'Month-over-Month Order Volume',
    company: 'Shopify',
    companyDomain: 'shopify.com',
    difficulty: 'Hard',
    tags: ['CTE', 'LAG', 'strftime', 'GROUP BY', 'MoM'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 15,
    datamartId: 'ecomm',
    prompt: 'The growth team wants to track how our order volume is trending month by month. For each month in the dataset, how does our order count compare to the prior month?',
    expectedColumns: ['month', 'order_count', 'mom_change'],
    expectedRowCount: 15,
    checkValues: [{ month: '2024-01', order_count: '4' }],
    solution: 'WITH monthly AS (\n  SELECT strftime(\'%Y-%m\', created_at) AS month, COUNT(*) AS order_count\n  FROM orders\n  GROUP BY month\n)\nSELECT month, order_count,\n       order_count - LAG(order_count) OVER (ORDER BY month) AS mom_change\nFROM monthly\nORDER BY month',
    debrief: 'What the stakeholder wants: A monthly order count time series with a change column showing growth or decline vs. the prior month — used to spot demand spikes, seasonal patterns, or worrying drops.\n\nAmbiguities resolved: \'Order count\' means total orders placed, not only completed orders. Including all statuses gives a demand signal; filtering to completed gives a revenue signal. If the team wants only completed, a WHERE status = \'completed\' filter should be added. \'Prior month\' means the immediately preceding calendar month, not a rolling 30-day window.\n\nSQL approach: A CTE groups orders by strftime(\'%Y-%m\', created_at) to get monthly counts. The outer query applies LAG(order_count) OVER (ORDER BY month) to retrieve the prior month\'s count. MoM change = current - prior. LAG returns NULL for the first month — acceptable per the request.\n\nWhat weak SQL looks like: A self-join on the monthly CTE with date arithmetic to match prior months — more complex than LAG and harder to read. Or forgetting ORDER BY inside the OVER clause — without it, LAG picks an arbitrary prior row, not the immediately preceding month.\n\nInterviewer follow-up: \'How would you add a column showing percentage change rather than absolute change?\'',
    sqliteNote: 'Uses strftime() for date grouping. In PostgreSQL use: TO_CHAR(created_at::date, \'YYYY-MM\') AS month.',
  },

  {
    id: 'sql-h08',
    title: 'Top Spender per Country',
    company: 'Amazon',
    companyDomain: 'amazon.com',
    difficulty: 'Hard',
    tags: ['CTE', 'ROW_NUMBER', 'PARTITION BY', 'GROUP BY'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 14,
    datamartId: 'ecomm',
    prompt: 'The regional team wants to send personalized retention offers to the highest-value customer in each country. Who\'s the top spender in each market?',
    expectedColumns: ['country', 'user_id', 'email', 'total_spent'],
    expectedRowCount: 5,
    checkValues: [{ country: 'US', user_id: '5' }],
    solution: 'WITH user_spend AS (\n  SELECT u.user_id, u.email, u.country, ROUND(SUM(o.subtotal), 2) AS total_spent\n  FROM users u\n  JOIN orders o ON u.user_id = o.user_id\n  GROUP BY u.user_id, u.email, u.country\n),\nranked AS (\n  SELECT *, ROW_NUMBER() OVER (PARTITION BY country ORDER BY total_spent DESC, user_id ASC) AS rn\n  FROM user_spend\n)\nSELECT country, user_id, email, total_spent\nFROM ranked\nWHERE rn = 1\nORDER BY country',
    debrief: 'What the stakeholder wants: One customer per country — the highest total spender — so the regional team can craft a personalized outreach for each market\'s best customer.\n\nAmbiguities resolved: \'Highest spender\' means highest total subtotal across all orders (all statuses included — the team wants the broadest picture). \'One per country\' raises the tie-breaking question: if two users spend the same amount, which is returned? Here we use lowest user_id as a deterministic tiebreaker — this should be made explicit. Return country, user_id, email, and total spent.\n\nSQL approach: First CTE aggregates total subtotal per user-country combination. Second CTE applies ROW_NUMBER() OVER (PARTITION BY country ORDER BY total_spent DESC, user_id ASC) to rank within each country. Outer query filters to rn = 1.\n\nWhat weak SQL looks like: GROUP BY country with MAX(total_spent) — this gives the maximum spend per country but not the user who achieved it. Adding user_id to the GROUP BY gives per-user totals but loses the per-country top-1 filtering. A self-join on max spend per country works but breaks on ties.\n\nInterviewer follow-up: \'How would you extend this to return the top 3 spenders per country instead of 1?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-h09',
    title: 'Projected Account Balance After Spend',
    company: 'Revolut',
    companyDomain: 'revolut.com',
    difficulty: 'Hard',
    tags: ['SUM OVER', 'running total', 'PARTITION BY', 'JOIN'],
    roles: ['PA', 'DA'],
    priority: 2,
    estimatedMin: 15,
    datamartId: 'fintech',
    prompt: 'The risk team wants to project each account\'s remaining balance after each transaction, treating every transaction as a debit from the recorded balance. For every transaction, show txn id, account id, the account\'s recorded balance, the transaction amount, and the projected remaining balance (initial balance minus cumulative spend up to that transaction).',
    expectedColumns: ['txn_id', 'account_id', 'initial_balance', 'amount', 'projected_remaining'],
    expectedRowCount: 40,
    checkValues: [],
    solution: 'SELECT t.txn_id, t.account_id, a.balance AS initial_balance, t.amount,\n       ROUND(a.balance - SUM(t.amount) OVER (PARTITION BY t.account_id ORDER BY t.occurred_at ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW), 2) AS projected_remaining\nFROM transactions t\nJOIN accounts a ON t.account_id = a.account_id\nORDER BY t.account_id, t.occurred_at',
    debrief: 'The SUM OVER running window accumulates spend chronologically per account; subtracting from the initial balance projects what remains. Accounts with large one-off purchases (account 5 with a $1,200 travel charge) show sharp balance drops. Note: the recorded balance is a snapshot, not a live ledger — this projection is an approximation if other credits or debits exist outside this dataset.',
    sqliteNote: null,
  },

  {
    id: 'sql-h10',
    title: 'Top Spending Category per Account',
    company: 'Visa',
    companyDomain: 'visa.com',
    difficulty: 'Hard',
    tags: ['CTE', 'ROW_NUMBER', 'PARTITION BY account', 'GROUP BY'],
    roles: ['PA', 'DA', 'BA'],
    priority: 1,
    estimatedMin: 14,
    datamartId: 'fintech',
    prompt: 'The personalization team wants to tailor card benefits to how each account actually spends. What\'s the single biggest spending category for each account?',
    expectedColumns: ['account_id', 'category', 'top_category_spend'],
    expectedRowCount: 15,
    checkValues: [{ account_id: '1', category: 'travel' }],
    solution: 'WITH cat_spend AS (\n  SELECT account_id, category, ROUND(SUM(amount), 2) AS total\n  FROM transactions\n  GROUP BY account_id, category\n),\nranked AS (\n  SELECT *, ROW_NUMBER() OVER (PARTITION BY account_id ORDER BY total DESC) AS rn\n  FROM cat_spend\n)\nSELECT account_id, category, total AS top_category_spend\nFROM ranked\nWHERE rn = 1\nORDER BY account_id',
    debrief: 'What the stakeholder wants: One row per account showing the category where that account spends the most — used to surface relevant card perks (travel cashback for travel-heavy accounts, dining rewards for restaurant spenders).\n\nAmbiguities resolved: \'Biggest spending category\' means highest total transaction amount, not most frequent. If an account has 5 small dining transactions and 1 large travel transaction, travel wins by revenue — clarify this with the stakeholder. If tied, pick the category alphabetically first for determinism. All 15 accounts must appear (including those with no transactions).\n\nSQL approach: First CTE aggregates SUM(amount) per account-category pair. Second CTE applies ROW_NUMBER() OVER (PARTITION BY account_id ORDER BY total DESC) to rank categories per account. Outer query filters to rn = 1.\n\nWhat weak SQL looks like: A correlated subquery to find MAX(SUM(amount)) per account — hard to write correctly and breaks when two categories tie. Or GROUP BY account_id only, which collapses all categories into one row and loses category-level detail entirely.\n\nInterviewer follow-up: \'How would you update the personalization logic if a user\'s top category changes month over month?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-h11',
    title: 'No-Show Patients Without Follow-Up',
    company: 'Teladoc',
    companyDomain: 'teladoc.com',
    difficulty: 'Hard',
    tags: ['EXISTS', 'NOT EXISTS', 'correlated subquery', 'date comparison'],
    roles: ['PA', 'DA', 'BA'],
    priority: 1,
    estimatedMin: 16,
    datamartId: 'health',
    prompt: 'The care coordination team is worried about patients slipping out of care. Can you identify patients who no-showed for an appointment and haven\'t had a completed visit since?',
    expectedColumns: ['patient_id', 'last_no_show_date'],
    expectedRowCount: 3,
    checkValues: [{ patient_id: '6' }],
    solution: 'SELECT DISTINCT a1.patient_id, MAX(a1.scheduled_at) AS last_no_show_date\nFROM appointments a1\nWHERE a1.no_show = 1\n  AND NOT EXISTS (\n    SELECT 1 FROM appointments a2\n    WHERE a2.patient_id = a1.patient_id\n      AND a2.no_show = 0\n      AND a2.completed_at IS NOT NULL\n      AND a2.scheduled_at > a1.scheduled_at\n  )\nGROUP BY a1.patient_id\nORDER BY a1.patient_id',
    debrief: 'What the stakeholder wants: A list of patients who missed at least one appointment and have no completed follow-up visit after it — the highest-risk group for care discontinuation.\n\nAmbiguities resolved: \'No-show\' means no_show = 1. \'Completed visit since\' means a subsequent appointment (later scheduled_at) where no_show = 0 AND completed_at IS NOT NULL. If any later completed appointment exists, the patient is NOT flagged — even if they no-showed again after that. Return the most recent no-show date per patient.\n\nSQL approach: The outer query filters to no-show appointments. NOT EXISTS checks per no-show row whether a later completed appointment exists for that patient — the correlated subquery compares a2.patient_id = a1.patient_id AND a2.scheduled_at > a1.scheduled_at AND no_show = 0 AND completed_at IS NOT NULL. GROUP BY patient_id with MAX(scheduled_at) collapses multiple no-shows to the most recent.\n\nWhat weak SQL looks like: LEFT JOIN from no-show patients to completed appointments, then WHERE completed appointment IS NULL — logically equivalent but harder to express the \'after\' condition correctly. Or simply finding patients with any no-show without the \'no subsequent completion\' filter — this flags patients who recovered, which is the opposite of what the team needs.\n\nInterviewer follow-up: \'How would you modify this to also flag patients whose most recent no-show was within the last 30 days?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-h12',
    title: 'Multi-Action Engaged Users',
    company: 'TikTok',
    companyDomain: 'tiktok.com',
    difficulty: 'Hard',
    tags: ['COUNT DISTINCT', 'HAVING', 'GROUP BY'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 12,
    datamartId: 'consumer',
    prompt: 'The engagement science team wants to find our most versatile users — the ones who don\'t just consume but actively interact in multiple ways. Which users are engaging across at least 3 different action types?',
    expectedColumns: ['user_id', 'action_diversity'],
    expectedRowCount: 2,
    checkValues: [{ user_id: '3', action_diversity: '3' }],
    solution: 'SELECT user_id, COUNT(DISTINCT action) AS action_diversity\nFROM interactions\nGROUP BY user_id\nHAVING COUNT(DISTINCT action) >= 3\nORDER BY action_diversity DESC, user_id ASC',
    debrief: 'What the stakeholder wants: Users who have used at least 3 distinct interaction types across any content — these are the platform\'s most behaviorally diverse and likely most attached users.\n\nAmbiguities resolved: \'3 different action types\' means 3 distinct values of the action column (view, like, share, save, comment) — not 3 separate interactions. A user with 10 views and 1 like only qualifies on 2 action types. This is a diversity-of-behavior filter, not a volume filter. Whether to scope to recent activity is worth asking — the solution uses the full history.\n\nSQL approach: COUNT(DISTINCT action) per user gives the number of distinct action types that user has performed. HAVING COUNT(DISTINCT action) >= 3 filters to users meeting the threshold. ORDER BY action_diversity DESC surfaces the most diverse users first.\n\nWhat weak SQL looks like: COUNT(*) instead of COUNT(DISTINCT action) — this counts total interactions, not distinct action types. A user with 10 views would qualify with COUNT(*) >= 3 but has only 1 action type, which is not what the team wants.\n\nInterviewer follow-up: \'How would you identify users who perform all 5 action types — the true super-engagers?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-h13',
    title: 'Category Revenue Share',
    company: 'Amazon',
    companyDomain: 'amazon.com',
    difficulty: 'Hard',
    tags: ['SUM OVER', 'window function', 'JOIN', 'GROUP BY', 'ROUND'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 15,
    datamartId: 'ecomm',
    prompt: 'The merchandising leadership team wants category revenue contribution as a percentage of total GMV. Using order_items and products, compute total revenue (unit_price × quantity) per product category and each category\'s percentage of the overall total, rounded to 1 decimal. Order by revenue descending.',
    expectedColumns: ['category', 'total_revenue', 'pct_of_total'],
    expectedRowCount: 4,
    checkValues: [],
    solution: 'SELECT p.category,\n       ROUND(SUM(oi.unit_price * oi.quantity), 2) AS total_revenue,\n       ROUND(100.0 * SUM(oi.unit_price * oi.quantity) / SUM(SUM(oi.unit_price * oi.quantity)) OVER (), 1) AS pct_of_total\nFROM order_items oi\nJOIN products p ON oi.product_id = p.product_id\nGROUP BY p.category\nORDER BY total_revenue DESC',
    debrief: 'Electronics leads with ~58.9% of revenue ($1,719.87), apparel at ~29.8% ($869.91), books at ~7.2% ($209.95), and home at ~4.1% ($119.97). The nested SUM inside SUM() OVER () is the key idiom: the inner SUM aggregates per group, the outer window SUM totals across all groups for the denominator. This is cleaner than a self-join or scalar subquery.',
    sqliteNote: null,
  },

  {
    id: 'sql-h14',
    title: 'Accounts With Login-to-Export Progression',
    company: 'Amplitude',
    companyDomain: 'amplitude.com',
    difficulty: 'Medium',
    tags: ['EXISTS', 'correlated subquery', 'event funnel'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 14,
    datamartId: 'saas',
    prompt: 'The product team is measuring feature adoption depth — specifically which accounts have users who logged in AND also exported data. Return account id and company name for every account that has at least one login event AND at least one export event in its history, ordered by account id.',
    expectedColumns: ['account_id', 'company_name'],
    expectedRowCount: 3,
    checkValues: [{ account_id: '1' }],
    solution: 'SELECT a.account_id, a.company_name\nFROM accounts a\nWHERE EXISTS (\n  SELECT 1 FROM events e1\n  WHERE e1.account_id = a.account_id AND e1.event_name = \'login\'\n)\nAND EXISTS (\n  SELECT 1 FROM events e2\n  WHERE e2.account_id = a.account_id AND e2.event_name = \'export\'\n)\nORDER BY a.account_id',
    debrief: 'Accounts 1, 2, and 3 have both login and export events. Accounts 5 and 6 have logins but no exports — these are engagement gaps for the product team to investigate. Using two EXISTS clauses is more readable than a single JOIN with DISTINCT and communicates the intent clearly: "this account has been touched by both event types."',
    sqliteNote: null,
  },

  {
    id: 'sql-h15',
    title: 'Outstanding Dispute Exposure per User',
    company: 'Stripe',
    companyDomain: 'stripe.com',
    difficulty: 'Hard',
    tags: ['4-table JOIN', 'WHERE IS NULL', 'GROUP BY', 'SUM'],
    roles: ['PA', 'DA', 'BA'],
    priority: 1,
    estimatedMin: 16,
    datamartId: 'fintech',
    prompt: 'The fraud team needs to know the total open dispute exposure per user — summing the amounts of all unresolved disputes linked to their accounts. Join disputes through transactions and accounts to users. Only include open disputes (no resolved date). Return user id and total outstanding amount, ordered by amount descending.',
    expectedColumns: ['user_id', 'total_outstanding'],
    expectedRowCount: 3,
    checkValues: [{ user_id: '5' }],
    solution: 'SELECT u.user_id, ROUND(SUM(d.amount), 2) AS total_outstanding\nFROM disputes d\nJOIN transactions t ON d.txn_id = t.txn_id\nJOIN accounts a ON t.account_id = a.account_id\nJOIN users u ON a.user_id = u.user_id\nWHERE d.resolved_at IS NULL\nGROUP BY u.user_id\nORDER BY total_outstanding DESC',
    debrief: 'Three users have open dispute exposure: user 5 ($3,500 — dispute 6 via account 8), user 12 ($490 — dispute 4 via account 15), user 1 ($89.99 — dispute 3 via account 1). The four-table chain disputes → transactions → accounts → users is required because disputes reference transaction ids, which reference account ids, which reference user ids. Each JOIN is necessary.',
    sqliteNote: null,
  },

  {
    id: 'sql-h16',
    title: 'Total Medication Coverage Days per Patient',
    company: 'Doximity',
    companyDomain: 'doximity.com',
    difficulty: 'Easy',
    tags: ['GROUP BY', 'SUM', 'computed metric', 'JOIN'],
    roles: ['PA', 'DA', 'BA'],
    priority: 2,
    estimatedMin: 13,
    datamartId: 'health',
    prompt: 'The pharmacy team estimates medication adherence by computing total days of coverage per patient: each prescription covers days_supply × (1 + refills) days. Sum this across all prescriptions per patient and return patient id and total coverage days, ordered by coverage descending.',
    expectedColumns: ['patient_id', 'total_coverage_days'],
    expectedRowCount: 15,
    checkValues: [{ patient_id: '2', total_coverage_days: '450' }],
    solution: 'SELECT patient_id, SUM(days_supply * (1 + refills)) AS total_coverage_days\nFROM prescriptions\nGROUP BY patient_id\nORDER BY total_coverage_days DESC',
    debrief: 'Patient 2 leads with 450 days (Metformin 90×3 = 270 + Sertraline 30×6 = 180). Patient 1 follows at 270 days. The formula days_supply × (1 + refills) correctly accounts for the initial fill plus each refill. Patients with long-supply chronic medications (90-day Metformin) accumulate coverage faster than those on short acute courses.',
    sqliteNote: null,
  },

  {
    id: 'sql-h17',
    title: 'Average Reorder Interval per Customer',
    company: 'Shopify',
    companyDomain: 'shopify.com',
    difficulty: 'Hard',
    tags: ['CTE', 'LAG', 'julianday', 'AVG', 'window function'],
    roles: ['PA', 'DA'],
    priority: 1,
    estimatedMin: 16,
    datamartId: 'ecomm',
    prompt: 'The retention team wants to understand how often our repeat customers come back. For each customer who has placed more than one order, what\'s the average time between purchases?',
    expectedColumns: ['user_id', 'avg_days_between'],
    expectedRowCount: 11,
    checkValues: [],
    solution: 'WITH gaps AS (\n  SELECT user_id, order_id, created_at,\n         julianday(created_at) - julianday(LAG(created_at) OVER (PARTITION BY user_id ORDER BY created_at)) AS days_between\n  FROM orders\n)\nSELECT user_id, ROUND(AVG(days_between), 0) AS avg_days_between\nFROM gaps\nWHERE days_between IS NOT NULL\nGROUP BY user_id\nORDER BY avg_days_between ASC',
    debrief: 'What the stakeholder wants: A per-customer average reorder gap in days, so the retention team can identify customers drifting toward longer intervals and trigger re-engagement before churn.\n\nAmbiguities resolved: \'Repeat customer\' means at least 2 orders — users with exactly 1 order have no gap to average. Should cancelled or returned orders count? The solution includes all statuses — a WHERE status = \'completed\' filter would give a cleaner purchase cadence signal. Average is across all consecutive pairs, rounded to nearest day.\n\nSQL approach: CTE computes per-order gaps using LAG(created_at) OVER (PARTITION BY user_id ORDER BY created_at). julianday difference gives decimal days; CAST to integer not needed for the avg. WHERE days_between IS NOT NULL excludes NULL first-order rows from the average. Outer query groups by user_id with AVG.\n\nWhat weak SQL looks like: (MAX(created_at) - MIN(created_at)) / COUNT(*) per user — this gives the total span divided by order count, not the average of consecutive gaps. These are equivalent only when all orders are equally spaced, which is never true in practice.\n\nInterviewer follow-up: \'How would you flag customers whose most recent order gap is more than twice their historical average?\'',
    sqliteNote: 'Uses julianday() for date arithmetic. In PostgreSQL use: (created_at::date - LAG(created_at::date) OVER (...)) AS days_between.',
  },

  {
    id: 'sql-h18',
    title: 'Creator Engagement Rate',
    company: 'YouTube',
    companyDomain: 'youtube.com',
    difficulty: 'Hard',
    tags: ['CTE', 'LEFT JOIN', 'ratio', 'GROUP BY'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 14,
    datamartId: 'consumer',
    prompt: 'The creator monetization team benchmarks creators by engagement rate: total interactions divided by number of pieces published. Return creator id, pieces published, total interactions, and average interactions per piece (rounded to 1 decimal), ordered by engagement rate descending.',
    expectedColumns: ['creator_id', 'pieces_published', 'total_interactions', 'avg_interactions_per_piece'],
    expectedRowCount: 6,
    checkValues: [{ creator_id: '1', avg_interactions_per_piece: '6.0' }],
    solution: 'WITH creator_stats AS (\n  SELECT c.creator_id,\n         COUNT(DISTINCT c.content_id) AS pieces_published,\n         COUNT(i.interaction_id) AS total_interactions\n  FROM content c\n  LEFT JOIN interactions i ON c.content_id = i.content_id\n  GROUP BY c.creator_id\n)\nSELECT creator_id, pieces_published, total_interactions,\n       ROUND(1.0 * total_interactions / pieces_published, 1) AS avg_interactions_per_piece\nFROM creator_stats\nORDER BY avg_interactions_per_piece DESC',
    debrief: 'Creator 1 leads with 6.0 interactions per piece (12 interactions across 2 pieces). Creator 5 has 5.0 (5 interactions on 1 piece). The LEFT JOIN on interactions ensures creators with zero interactions still appear. Dividing by pieces_published (not piece count from interactions) is the correct denominator — it measures reach efficiency per piece of content published.',
    sqliteNote: null,
  },

  {
    id: 'sql-h19',
    title: 'Multi-Month Active Accounts',
    company: 'Mixpanel',
    companyDomain: 'mixpanel.com',
    difficulty: 'Medium',
    tags: ['COUNT DISTINCT', 'strftime', 'HAVING', 'GROUP BY'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 12,
    datamartId: 'saas',
    prompt: 'The growth team defines a "stickily active" account as one that logged events in at least 3 distinct calendar months. Return account id and the number of distinct active months, ordered by active months descending.',
    expectedColumns: ['account_id', 'active_months'],
    expectedRowCount: 2,
    checkValues: [{ account_id: '5', active_months: '3' }],
    solution: 'SELECT account_id, COUNT(DISTINCT strftime(\'%Y-%m\', occurred_at)) AS active_months\nFROM events\nGROUP BY account_id\nHAVING COUNT(DISTINCT strftime(\'%Y-%m\', occurred_at)) >= 3\nORDER BY active_months DESC',
    debrief: 'Only accounts 5 and 6 appear in all three months (Jan, Feb, Mar 2024). Accounts 1–4 have events only in Jan and Feb. Account 7 has events in Feb and Mar only. The stickiness threshold of 3 months is strict — in a real product analysis this would be compared against a rolling 12-month window.',
    sqliteNote: 'Uses strftime() for month extraction. In PostgreSQL use: TO_CHAR(occurred_at::date, \'YYYY-MM\') inside COUNT(DISTINCT ...).',
  },

  {
    id: 'sql-h20',
    title: 'Top Decile Transactions',
    company: 'PayPal',
    companyDomain: 'paypal.com',
    difficulty: 'Medium',
    tags: ['NTILE', 'CTE', 'window function', 'percentile filter'],
    roles: ['PA', 'DA', 'BA'],
    priority: 1,
    estimatedMin: 13,
    datamartId: 'fintech',
    prompt: 'The fraud team wants to review all transactions in the top 10% by amount for manual screening. Using NTILE to assign deciles, return the txn id, account id, amount, category, and occurred date for all transactions in the 10th decile (highest amounts). Order by amount descending.',
    expectedColumns: ['txn_id', 'account_id', 'amount', 'category', 'occurred_at'],
    expectedRowCount: 4,
    checkValues: [{ txn_id: '31' }],
    solution: 'WITH deciled AS (\n  SELECT *, NTILE(10) OVER (ORDER BY amount) AS spend_decile\n  FROM transactions\n)\nSELECT txn_id, account_id, amount, category, occurred_at\nFROM deciled\nWHERE spend_decile = 10\nORDER BY amount DESC',
    debrief: 'The top decile contains 4 transactions (40 total ÷ 10 = 4 per decile): txn 31 ($3,500 QuickTransfer), txn 17 ($2,200 CryptoXchange), txn 8 ($1,200 Delta Airlines), and txn 9 ($950 QuickTransfer). All four are large transactions — two link to flagged merchants, making this screen highly relevant for fraud workflows.',
    sqliteNote: null,
  },

  {
    id: 'sql-h21',
    title: 'Cross-Category Shoppers',
    company: 'Amazon',
    companyDomain: 'amazon.com',
    difficulty: 'Hard',
    tags: ['CTE', 'COUNT DISTINCT', 'HAVING', '3-table JOIN'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 14,
    datamartId: 'ecomm',
    prompt: 'The product team wants to target genuinely diverse shoppers for a cross-sell campaign. Which customers have bought items from at least 3 different product categories?',
    expectedColumns: ['user_id', 'cat_count'],
    expectedRowCount: 3,
    checkValues: [{ user_id: '5', cat_count: '3' }],
    solution: 'WITH user_cats AS (\n  SELECT o.user_id, COUNT(DISTINCT p.category) AS cat_count\n  FROM orders o\n  JOIN order_items oi ON o.order_id = oi.order_id\n  JOIN products p ON oi.product_id = p.product_id\n  GROUP BY o.user_id\n)\nSELECT user_id, cat_count\nFROM user_cats\nWHERE cat_count >= 3\nORDER BY cat_count DESC, user_id ASC',
    debrief: 'What the stakeholder wants: Users who have purchased across a meaningful breadth of categories — signals high platform engagement and the best candidates for cross-sell and bundling offers.\n\nAmbiguities resolved: \'3 different categories\' means 3 distinct category values across their entire order history, all statuses included. Whether cancelled orders should count toward category breadth is worth asking — if a user bought electronics but returned it, they may not be a true cross-category buyer. The threshold of 3 is a business parameter to confirm.\n\nSQL approach: A CTE joins orders to order_items to products to get category per line item, then counts COUNT(DISTINCT p.category) per user_id. The 3-table JOIN chain is required: category lives on products, not orders. Outer query filters with WHERE cat_count >= 3.\n\nWhat weak SQL looks like: Counting distinct product_ids instead of distinct categories — this measures product breadth, not category breadth. A user buying 5 electronics products would pass the count but fail the category diversity filter.\n\nInterviewer follow-up: \'How would you identify which category pairs most commonly appear together in the same user\'s order history?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-h22',
    title: 'Providers With Perfect Attendance',
    company: 'Doximity',
    companyDomain: 'doximity.com',
    difficulty: 'Medium',
    tags: ['HAVING', 'SUM', 'GROUP BY', 'aggregate filter'],
    roles: ['PA', 'DA', 'BA'],
    priority: 2,
    estimatedMin: 12,
    datamartId: 'health',
    prompt: 'The clinic director wants to recognize providers who have never had a patient no-show. Return provider id, appointment count, and no-show count for every provider whose no-show total is exactly zero, ordered by appointment count descending.',
    expectedColumns: ['provider_id', 'total_appts', 'no_shows'],
    expectedRowCount: 3,
    checkValues: [{ provider_id: '3', total_appts: '4' }],
    solution: 'SELECT provider_id, COUNT(*) AS total_appts, SUM(no_show) AS no_shows\nFROM appointments\nGROUP BY provider_id\nHAVING SUM(no_show) = 0\nORDER BY total_appts DESC',
    debrief: 'Providers 3 (4 appts), 4 (2 appts), and 6 (2 appts) have perfect attendance records. HAVING SUM(no_show) = 0 is the aggregate filter for zero no-shows — this is equivalent to NOT EXISTS but more concise. Providers 1 and 5 have the highest no-show rates at 40% and 33%, which would be flagged for scheduling strategy review.',
    sqliteNote: null,
  },

  {
    id: 'sql-h23',
    title: 'Likers Who Never Shared',
    company: 'TikTok',
    companyDomain: 'tiktok.com',
    difficulty: 'Easy',
    tags: ['NOT IN', 'subquery', 'DISTINCT', 'anti-join'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 13,
    datamartId: 'consumer',
    prompt: 'The virality team wants to convert engaged-but-passive users into sharers. Identify users who have performed a "like" action but have never performed a "share" action on any content. Return their user ids ordered ascending.',
    expectedColumns: ['user_id'],
    expectedRowCount: 6,
    checkValues: [{ user_id: '2' }],
    solution: 'SELECT DISTINCT user_id\nFROM interactions\nWHERE action = \'like\'\n  AND user_id NOT IN (\n    SELECT DISTINCT user_id FROM interactions WHERE action = \'share\'\n  )\nORDER BY user_id',
    debrief: 'Six users liked but never shared: users 2, 4, 5, 6, 11, and 12. Only users 3 and 7 have shared content — user 3 both liked and shared, so is excluded. The NOT IN subquery is the classic anti-join pattern. In production, NOT EXISTS or a LEFT JOIN ... WHERE IS NULL scales better because NOT IN can misfire when the subquery returns NULLs.',
    sqliteNote: null,
  },

  {
    id: 'sql-h24',
    title: 'First-Month Revenue by Signup Cohort',
    company: 'Shopify',
    companyDomain: 'shopify.com',
    difficulty: 'Hard',
    tags: ['CTE', 'date arithmetic', 'julianday', 'LEFT JOIN', 'cohort'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 18,
    datamartId: 'ecomm',
    prompt: 'The growth team runs a cohort analysis measuring revenue captured in the first 30 days after signup. For all users who signed up in Q1 2023 (January through March), calculate their total order subtotal from orders placed within 30 days of their signup date. Include users with zero first-month orders. Return user id, signup date, and first-month revenue rounded to 2 decimals.',
    expectedColumns: ['user_id', 'signup_date', 'first_month_revenue'],
    expectedRowCount: 5,
    checkValues: [{ user_id: '5', first_month_revenue: '249.99' }],
    solution: 'WITH q1_users AS (\n  SELECT user_id, signup_date\n  FROM users\n  WHERE signup_date >= \'2023-01-01\' AND signup_date < \'2023-04-01\'\n),\nfirst_orders AS (\n  SELECT q.user_id, q.signup_date,\n         ROUND(COALESCE(SUM(o.subtotal), 0), 2) AS first_month_revenue\n  FROM q1_users q\n  LEFT JOIN orders o ON o.user_id = q.user_id\n    AND julianday(o.created_at) <= julianday(q.signup_date) + 30\n  GROUP BY q.user_id, q.signup_date\n)\nSELECT user_id, signup_date, first_month_revenue\nFROM first_orders\nORDER BY user_id',
    debrief: 'Five users signed up in Q1 2023: users 1–5. User 5 captured $249.99 (order 9, placed 20 days post-signup). Users 1 and 3 also captured revenue within 30 days. Users 2 and 4 have $0 first-month revenue — their first orders came months later. LEFT JOIN with the date condition on the ON clause (not WHERE) is critical: moving it to WHERE would exclude zero-revenue users.',
    sqliteNote: 'Uses julianday() for date arithmetic. In PostgreSQL use: o.created_at::date <= q.signup_date::date + INTERVAL \'30 days\'.',
  },

  // ─── MASTER / CHALLENGE VAULT (25 problems) ──────────────────────────────
  // Master problems are never included in study plans. Always visible in Challenge Vault.

  {
    id: 'sql-master01',
    title: 'User Risk Scoring Engine',
    company: 'Chime',
    companyDomain: 'chime.com',
    difficulty: 'Master',
    isFree: false,
    tags: ['CTE', 'CASE WHEN', 'multi-signal', 'COALESCE', 'LEFT JOIN', 'risk scoring'],
    roles: ['DA', 'PA'],
    priority: 1,
    estimatedMin: 30,
    datamartId: 'fintech',
    prompt: 'Build me a composite risk score for every user based on their account standing, risk tier, and transaction history. I need a priority list for my review queue.',
    expectedColumns: ['user_id', 'email', 'risk_score'],
    expectedRowCount: 12,
    checkValues: [{ user_id: '9', risk_score: '8' }],
    solution: 'WITH kyc AS (\n  SELECT user_id,\n    CASE kyc_status WHEN \'verified\' THEN 0 WHEN \'pending\' THEN 2 WHEN \'rejected\' THEN 5 ELSE 0 END AS kyc_score\n  FROM users\n),\ntier AS (\n  SELECT user_id,\n    CASE risk_tier WHEN \'low\' THEN 0 WHEN \'medium\' THEN 1 WHEN \'high\' THEN 3 ELSE 0 END AS tier_score\n  FROM users\n),\ndisputed AS (\n  SELECT DISTINCT a.user_id, 2 AS dispute_score\n  FROM accounts a\n  JOIN transactions t ON a.account_id = t.account_id\n  WHERE t.status = \'disputed\'\n)\nSELECT u.user_id, u.email,\n  k.kyc_score + r.tier_score + COALESCE(d.dispute_score, 0) AS risk_score\nFROM users u\nJOIN kyc k ON u.user_id = k.user_id\nJOIN tier r ON u.user_id = r.user_id\nJOIN accounts a ON u.user_id = a.user_id\nLEFT JOIN disputed d ON u.user_id = d.user_id\nGROUP BY u.user_id, u.email, k.kyc_score, r.tier_score, d.dispute_score\nORDER BY risk_score DESC, u.user_id',
    debrief: 'What the stakeholder wants: A scored, ordered list of all users with accounts — highest risk at the top — so the review team can triage efficiently without manually reading individual records.\n\nAmbiguities resolved: Three signals combine into a risk score: (1) KYC status — verified=0, pending=2, rejected=5; (2) risk tier — low=0, medium=1, high=3; (3) any disputed transaction on any account adds 2. The candidate must propose and document these weights — they are not given. Only users with at least one account qualify (the JOIN to accounts acts as the filter).\n\nSQL approach: Three CTEs isolate each signal cleanly. kyc CTE: CASE kyc_status for the KYC score. tier CTE: CASE risk_tier for the tier score. disputed CTE: DISTINCT user_ids from accounts joined to disputed transactions, contributing a flat +2. Final SELECT sums all three with COALESCE(d.dispute_score, 0) for users with no disputes. GROUP BY is required because the JOIN to accounts can produce multiple rows per user (users with multiple accounts).\n\nWhat weak SQL looks like: Three correlated subqueries in the SELECT list — one per signal — logically correct but runs three separate aggregation passes per row. The CTE approach computes each signal once. Or forgetting GROUP BY after the accounts JOIN — duplicates inflate the score for multi-account users.\n\nInterviewer follow-up: \'How would you validate that the scoring weights are calibrated correctly against historical fraud outcomes?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-master02',
    title: 'Channel 6-Month Retention Analysis',
    company: 'Meta',
    companyDomain: 'meta.com',
    difficulty: 'Master',
    isFree: false,
    tags: ['CTE', 'cohort analysis', 'julianday', 'retention', 'channel attribution'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 30,
    datamartId: 'ecomm',
    prompt: 'Which acquisition channel produces customers who actually convert to buyers? I want each channel\'s 6-month purchase rate so we can rebalance our marketing spend.',
    expectedColumns: ['channel', 'total_users', 'converted_in_180d', 'retention_rate'],
    expectedRowCount: 4,
    checkValues: [{ channel: 'paid', retention_rate: '100.0' }],
    solution: 'WITH first_orders AS (\n  SELECT user_id, MIN(created_at) AS first_order_date\n  FROM orders\n  GROUP BY user_id\n),\nchannel_conversion AS (\n  SELECT u.channel,\n    COUNT(*) AS total_users,\n    SUM(CASE WHEN julianday(fo.first_order_date) - julianday(u.signup_date) <= 180 THEN 1 ELSE 0 END) AS converted_in_180d\n  FROM users u\n  LEFT JOIN first_orders fo ON u.user_id = fo.user_id\n  GROUP BY u.channel\n)\nSELECT channel, total_users, converted_in_180d,\n  ROUND(100.0 * converted_in_180d / total_users, 1) AS retention_rate\nFROM channel_conversion\nORDER BY retention_rate DESC, total_users DESC',
    debrief: 'What the stakeholder wants: A channel-level conversion rate showing what percentage of acquired users actually placed an order within 6 months of signup — the key metric for evaluating channel quality beyond just user volume.\n\nAmbiguities resolved: \'6 months\' means 180 days from signup_date (not a calendar month window). All users from each channel must appear in the denominator — including those who never ordered. Whether to count any order status or only completed is a business decision; the solution counts the first order date regardless of status. Users with no orders return NULL from the LEFT JOIN, which COALESCE or the CASE WHEN handles.\n\nSQL approach: First CTE finds each user\'s MIN(created_at) as first_order_date. Main CTE LEFT JOINs users to first_orders and computes converted_in_180d using CASE WHEN julianday(first_order_date) - julianday(signup_date) <= 180. Final SELECT computes retention_rate = 100.0 * converted / total.\n\nWhat weak SQL looks like: INNER JOIN from users to first_orders — drops users who never ordered from the denominator, making every channel appear to have 100% conversion. This is the most common error in cohort-style queries.\n\nInterviewer follow-up: \'If paid channel converts at 100% but has the highest CAC, how would you incorporate LTV to determine which channel has the best unit economics?\'',
    sqliteNote: 'Uses julianday() for date differences — SQLite-specific. In other dialects use DATEDIFF() or date subtraction.',
  },

  {
    id: 'sql-master03',
    title: 'Channel LTV Analysis',
    company: 'Shopify',
    companyDomain: 'shopify.com',
    difficulty: 'Master',
    isFree: false,
    tags: ['CTE', 'LEFT JOIN', 'GROUP BY', 'LTV', 'channel attribution'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 30,
    datamartId: 'ecomm',
    prompt: 'Finance wants a channel-level LTV report for the board. Show me how each acquisition channel compares on revenue per user and order frequency.',
    expectedColumns: ['channel', 'user_count', 'total_orders', 'total_revenue', 'orders_per_user', 'avg_ltv'],
    expectedRowCount: 4,
    checkValues: [],
    solution: 'WITH channel_users AS (\n  SELECT user_id, channel FROM users\n),\nchannel_revenue AS (\n  SELECT cu.channel,\n         COUNT(DISTINCT cu.user_id) AS user_count,\n         COUNT(o.order_id) AS total_orders,\n         ROUND(SUM(o.subtotal), 2) AS total_revenue\n  FROM channel_users cu\n  LEFT JOIN orders o ON cu.user_id = o.user_id AND o.status = \'completed\'\n  GROUP BY cu.channel\n)\nSELECT channel, user_count, total_orders,\n       COALESCE(total_revenue, 0) AS total_revenue,\n       ROUND(1.0 * total_orders / user_count, 2) AS orders_per_user,\n       ROUND(COALESCE(total_revenue, 0) / user_count, 2) AS avg_ltv\nFROM channel_revenue\nORDER BY avg_ltv DESC',
    debrief: 'What the stakeholder wants: A per-channel summary showing revenue efficiency — how many users each channel brings, how many orders they complete, and what average revenue per user looks like. Used to justify channel-level marketing budget allocations.\n\nAmbiguities resolved: LTV here means total completed-order revenue divided by user count (all users, not just buyers). Including zero-purchase users in the denominator is the correct business definition — an acquired user who never buys is still an acquisition cost. Only completed orders count toward revenue. Channels with zero completed orders still appear.\n\nSQL approach: First CTE lists all users with their channel. Main CTE LEFT JOINs to orders with AND o.status = \'completed\' in the ON clause — this preserves all users in the GROUP BY while only counting completed orders. COUNT(DISTINCT user_id) for user_count. COUNT(order_id) for total_orders. SUM(subtotal) for revenue. Outer SELECT computes derived metrics.\n\nWhat weak SQL looks like: Putting WHERE o.status = \'completed\' in the WHERE clause instead of the ON clause — this converts the LEFT JOIN to an INNER JOIN, dropping users with no completed orders from the user_count denominator and inflating avg_ltv for every channel.\n\nInterviewer follow-up: \'How would you adjust the LTV calculation to account for the fact that some users were acquired 12 months ago while others were acquired last month?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-master04',
    title: 'Account Health Score',
    company: 'Salesforce',
    companyDomain: 'salesforce.com',
    difficulty: 'Master',
    isFree: false,
    tags: ['CTE', 'LEFT JOIN', 'CASE WHEN', 'COALESCE', 'multi-signal scoring'],
    roles: ['PA', 'DA', 'BA'],
    priority: 1,
    estimatedMin: 30,
    datamartId: 'saas',
    prompt: 'Customer success needs a health score for every active account — something they can act on to prioritize outreach. Can you build that?',
    expectedColumns: ['account_id', 'company_name', 'plan_name', 'recent_events', 'health_score', 'health_label'],
    expectedRowCount: 12,
    checkValues: [{ account_id: '5', health_label: 'healthy' }],
    solution: 'WITH recent_activity AS (\n  SELECT account_id, COUNT(*) AS recent_events\n  FROM events\n  WHERE occurred_at >= \'2024-03-01\'\n  GROUP BY account_id\n),\nactive_subs AS (\n  SELECT s.account_id, p.name AS plan_name,\n    CASE p.name WHEN \'Enterprise\' THEN 6 WHEN \'Business\' THEN 4 WHEN \'Growth\' THEN 2 ELSE 0 END AS plan_score\n  FROM subscriptions s\n  JOIN plans p ON s.plan_id = p.plan_id\n  WHERE s.status = \'active\'\n)\nSELECT a.account_id, a.company_name, asub.plan_name,\n       COALESCE(ra.recent_events, 0) AS recent_events,\n       asub.plan_score + 3 + COALESCE(ra.recent_events, 0) AS health_score,\n       CASE WHEN asub.plan_score + 3 + COALESCE(ra.recent_events, 0) >= 9 THEN \'healthy\'\n            WHEN asub.plan_score + 3 + COALESCE(ra.recent_events, 0) >= 6 THEN \'at_risk\'\n            ELSE \'critical\' END AS health_label\nFROM accounts a\nJOIN active_subs asub ON a.account_id = asub.account_id\nLEFT JOIN recent_activity ra ON a.account_id = ra.account_id\nORDER BY health_score DESC, a.account_id',
    debrief: 'What the stakeholder wants: Every active account labeled with a numeric health score and a categorical label (healthy / at_risk / critical) — so CS reps can sort their portfolio and focus outreach on accounts most likely to churn.\n\nAmbiguities resolved: \'Active accounts\' means those with status = \'active\' in subscriptions — churned accounts are excluded. Health signals and weights must be defined by the candidate: plan tier (Enterprise=6, Business=4, Growth=2, Starter=0), active subscription (+3), recent events since a cutoff date (+1 each). \'Recent\' must have a defined cutoff — the solution uses 2024-03-01. Category thresholds (>=9 healthy, 6-8 at_risk, <6 critical) are also business parameters that the candidate must state and justify.\n\nSQL approach: recent_activity CTE counts events per account since the cutoff. active_subs CTE joins subscriptions to plans to get plan_score per active account. Main SELECT joins accounts to both CTEs using LEFT JOIN on recent_activity (accounts with no recent events score 0). COALESCE(ra.recent_events, 0) prevents NULL propagation in the score formula.\n\nWhat weak SQL looks like: Hard-coding the score formula inline without CTEs — the CASE WHEN expression becomes unreadable and duplicated between the score column and the label column. Or using WHERE s.status = \'active\' in a subquery that returns multiple rows per account when an account has had both active and churned subscriptions.\n\nInterviewer follow-up: \'How would you alert the CS team automatically when an account\'s health score drops by more than 3 points month over month?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-master05',
    title: 'Transaction Spend Anomaly Detection',
    company: 'Chime',
    companyDomain: 'chime.com',
    difficulty: 'Master',
    isFree: false,
    tags: ['CTE', 'AVG', 'JOIN', 'CASE WHEN', 'anomaly detection', 'fraud'],
    roles: ['PA', 'DA', 'BA'],
    priority: 1,
    estimatedMin: 30,
    datamartId: 'fintech',
    prompt: 'Flag transactions that look unusual compared to that user\'s normal spending behavior. I want to surface anything that\'s way out of line so the fraud team can review it.',
    expectedColumns: ['txn_id', 'user_id', 'amount', 'user_avg_amount', 'spend_ratio'],
    expectedRowCount: 3,
    checkValues: [],
    solution: 'WITH user_avg AS (\n  SELECT a.user_id, ROUND(AVG(t.amount), 2) AS avg_amount\n  FROM accounts a\n  JOIN transactions t ON a.account_id = t.account_id\n  GROUP BY a.user_id\n)\nSELECT t.txn_id, a.user_id, t.amount,\n       ua.avg_amount AS user_avg_amount,\n       ROUND(t.amount / ua.avg_amount, 2) AS spend_ratio\nFROM transactions t\nJOIN accounts a ON t.account_id = a.account_id\nJOIN user_avg ua ON a.user_id = ua.user_id\nWHERE t.amount > 3 * ua.avg_amount\nORDER BY spend_ratio DESC',
    debrief: 'What the stakeholder wants: A list of transactions that are dramatically higher than what that specific user normally spends — using each user\'s own history as the baseline rather than a global average, so a high-spending user isn\'t flagged for normal behavior.\n\nAmbiguities resolved: \'Way out of line\' must be quantified — the candidate must propose a threshold (3x the user\'s average is used here). \'Normal spending\' is defined as the all-time average transaction amount across all that user\'s accounts. Whether to use a rolling window or all-time average is a business choice; all-time is simpler and used here. New users with only one transaction have no meaningful baseline — they would always equal their average, so they never appear in the results (which is correct behavior).\n\nSQL approach: CTE joins accounts to transactions, groups by user_id to compute AVG(amount). Main query joins transactions back through accounts to the per-user average. WHERE t.amount > 3 * ua.avg_amount applies the threshold. spend_ratio = amount / avg shows how extreme each anomaly is.\n\nWhat weak SQL looks like: Comparing to a global average (AVG(amount) across all users) — this flags every large transaction regardless of whether the user is normally a big spender. Or using a scalar subquery in the WHERE clause that re-computes the average per row — O(n squared) and slower than the CTE approach.\n\nInterviewer follow-up: \'How would you replace the 3x threshold with a statistical approach using mean plus 2 standard deviations per user?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-master06',
    title: 'Content Engagement Funnel Rates',
    company: 'TikTok',
    companyDomain: 'tiktok.com',
    difficulty: 'Hard',
    isFree: false,
    tags: ['CTE', 'CASE WHEN', 'pivot', 'ROUND', 'funnel analysis'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 30,
    datamartId: 'consumer',
    prompt: 'The content team wants a funnel conversion analysis per piece of content. Starting from views, what percentage of viewers also liked, shared, or commented? For each content_id, return views, likes, shares, comments, like_rate (likes/views, rounded to 1 decimal as %), share_rate, and comment_rate. Only include content with at least 1 view. Order by views descending.',
    expectedColumns: ['content_id', 'views', 'likes', 'shares', 'comments', 'like_rate', 'share_rate', 'comment_rate'],
    expectedRowCount: 7,
    checkValues: [{ content_id: '1', views: '3', likes: '2' }],
    solution: 'WITH engagement AS (\n  SELECT content_id,\n         SUM(CASE WHEN action = \'view\' THEN 1 ELSE 0 END) AS views,\n         SUM(CASE WHEN action = \'like\' THEN 1 ELSE 0 END) AS likes,\n         SUM(CASE WHEN action = \'share\' THEN 1 ELSE 0 END) AS shares,\n         SUM(CASE WHEN action = \'comment\' THEN 1 ELSE 0 END) AS comments\n  FROM interactions\n  GROUP BY content_id\n)\nSELECT content_id, views, likes, shares, comments,\n       ROUND(100.0 * likes / views, 1) AS like_rate,\n       ROUND(100.0 * shares / views, 1) AS share_rate,\n       ROUND(100.0 * comments / views, 1) AS comment_rate\nFROM engagement\nWHERE views > 0\nORDER BY views DESC',
    debrief: 'Content 1 has 3 views and achieves a 66.7% like rate (2/3) — unusually high, likely because it\'s the fitness video that drew the most engaged audience. Content 7 and 8 each have 1 view and no downstream actions (0% funnel). The CTE computes raw action counts; the outer query converts to rates using the view count as denominator. This pattern is identical to the standard product funnel — view = exposure, like = consideration, share = advocacy. A share_rate of even 1% on content with 10M views generates 100K viral shares. Content without views is excluded by WHERE views > 0 to prevent division by zero.',
    sqliteNote: null,
  },

  {
    id: 'sql-master07',
    title: 'Hypertension Care Gap Analysis',
    company: 'Epic Systems',
    companyDomain: 'epic.com',
    difficulty: 'Hard',
    isFree: false,
    tags: ['CTE', 'NOT IN', 'anti-join', 'subquery', 'clinical quality', 'care gap'],
    roles: ['PA', 'DA', 'BA'],
    priority: 1,
    estimatedMin: 30,
    datamartId: 'health',
    prompt: 'The clinical quality team wants to find patients with a confirmed hypertension diagnosis (ICD-10 I10) who have no antihypertensive prescription on record (Lisinopril, Amlodipine, or Furosemide). These patients represent a potential care gap. Return patient_id, dob, gender, and zip_code, ordered by patient_id.',
    expectedColumns: ['patient_id', 'dob', 'gender', 'zip_code'],
    expectedRowCount: 2,
    checkValues: [{ patient_id: '6' }],
    solution: 'WITH hypertension_patients AS (\n  SELECT DISTINCT a.patient_id\n  FROM appointments a\n  JOIN diagnoses d ON a.appt_id = d.appt_id\n  WHERE d.icd_code = \'I10\'\n),\nantihypertensive_patients AS (\n  SELECT DISTINCT patient_id\n  FROM prescriptions\n  WHERE drug_name IN (\'Lisinopril\', \'Amlodipine\', \'Furosemide\')\n)\nSELECT p.patient_id, p.dob, p.gender, p.zip_code\nFROM hypertension_patients hp\nJOIN patients p ON hp.patient_id = p.patient_id\nWHERE hp.patient_id NOT IN (SELECT patient_id FROM antihypertensive_patients)\nORDER BY hp.patient_id',
    debrief: 'Patients 6 and 8 have an essential hypertension diagnosis (I10) but no antihypertensive medication on file. Patient 6 has Atorvastatin (a cholesterol-lowering statin — not an antihypertensive). Patient 8 has only Amoxicillin (an antibiotic). Patients 1 and 4 have Lisinopril and are correctly excluded. The two-CTE structure separates the two clinical signals cleanly before the anti-join. The NOT IN subquery works safely here because the antihypertensive_patients CTE is drawn from a known-non-null patient_id column. In production, NOT EXISTS or a LEFT JOIN anti-join scales better if the subquery is large.',
    sqliteNote: null,
  },

  {
    id: 'sql-master08',
    title: 'Product Co-Purchase Affinity',
    company: 'Amazon',
    companyDomain: 'amazon.com',
    difficulty: 'Master',
    isFree: false,
    tags: ['self-join', 'CTE', 'affinity analysis', 'product bundling'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 30,
    datamartId: 'ecomm',
    prompt: 'Which products tend to get bought together? I want to power a Frequently Bought Together widget on the product page.',
    expectedColumns: ['product_a', 'product_b', 'co_purchase_count'],
    expectedRowCount: 2,
    checkValues: [{ product_a: 'SQL Mastery', product_b: 'Desk Lamp', co_purchase_count: '1' }],
    solution: 'WITH product_pairs AS (\n  SELECT oi1.product_id AS pid_a, oi2.product_id AS pid_b,\n         COUNT(*) AS co_purchase_count\n  FROM order_items oi1\n  JOIN order_items oi2\n    ON oi1.order_id = oi2.order_id\n    AND oi1.product_id < oi2.product_id\n  GROUP BY oi1.product_id, oi2.product_id\n)\nSELECT p1.name AS product_a, p2.name AS product_b, pp.co_purchase_count\nFROM product_pairs pp\nJOIN products p1 ON pp.pid_a = p1.product_id\nJOIN products p2 ON pp.pid_b = p2.product_id\nORDER BY pp.co_purchase_count DESC',
    debrief: 'What the stakeholder wants: All product pairs that have appeared in the same order, with their co-purchase count — used to power a recommendation widget that surfaces complementary products on the product detail page.\n\nAmbiguities resolved: \'Same order\' means same order_id across all order statuses (including cancelled — whether to exclude those is a business call worth surfacing). \'Pair\' means an unordered pair — (A, B) is the same as (B, A). The candidate must ensure deduplication by requiring product_a_id < product_b_id. Minimum co-purchase count threshold (e.g., >= 2) is a business parameter to keep noise out of the widget.\n\nSQL approach: Self-join order_items on order_id with the condition oi1.product_id < oi2.product_id — this generates all unordered pairs within each order without duplicates. GROUP BY the pair and COUNT(*) gives co_purchase_count. Join to products twice to get names. ORDER BY count DESC surfaces the most-affinity pairs.\n\nWhat weak SQL looks like: Self-join without the < condition — generates (A,B) and (B,A) as separate pairs, doubling all counts and confusing the widget logic. Or generating pairs in application code after a full table scan — correct but inefficient when order_items has millions of rows.\n\nInterviewer follow-up: \'How would you extend this to compute a lift score — the probability of buying B given A, compared to the base rate of buying B?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-master09',
    title: 'Plan Upgrade and Downgrade Classification',
    company: 'Salesforce',
    companyDomain: 'salesforce.com',
    difficulty: 'Master',
    isFree: false,
    tags: ['CTE', 'self-join', 'ROW_NUMBER', 'window function', 'MRR movement'],
    roles: ['PA', 'DA', 'BA'],
    priority: 1,
    estimatedMin: 30,
    datamartId: 'saas',
    prompt: 'Finance wants every subscription plan change classified — upgrade, downgrade, or lateral. I need this for the MRR waterfall chart.',
    expectedColumns: ['account_id', 'company_name', 'from_plan', 'to_plan', 'prev_mrr', 'new_mrr', 'mrr_delta', 'change_type'],
    expectedRowCount: 2,
    checkValues: [{ account_id: '1', change_type: 'upgrade', mrr_delta: '2000' }],
    solution: 'WITH ranked_subs AS (\n  SELECT account_id, plan_id, mrr, started_at,\n         ROW_NUMBER() OVER (PARTITION BY account_id ORDER BY started_at) AS sub_order\n  FROM subscriptions\n),\nwith_prev AS (\n  SELECT r1.account_id, r1.started_at AS change_date,\n         r2.plan_id AS prev_plan_id, r2.mrr AS prev_mrr,\n         r1.plan_id AS new_plan_id, r1.mrr AS new_mrr,\n         r1.mrr - r2.mrr AS mrr_delta\n  FROM ranked_subs r1\n  JOIN ranked_subs r2\n    ON r1.account_id = r2.account_id\n    AND r1.sub_order = r2.sub_order + 1\n)\nSELECT wp.account_id, a.company_name,\n       p1.name AS from_plan, p2.name AS to_plan,\n       wp.prev_mrr, wp.new_mrr, wp.mrr_delta,\n       CASE WHEN wp.mrr_delta > 0 THEN \'upgrade\'\n            WHEN wp.mrr_delta < 0 THEN \'downgrade\'\n            ELSE \'lateral\' END AS change_type\nFROM with_prev wp\nJOIN accounts a ON wp.account_id = a.account_id\nJOIN plans p1 ON wp.prev_plan_id = p1.plan_id\nJOIN plans p2 ON wp.new_plan_id = p2.plan_id\nORDER BY wp.change_date',
    debrief: 'What the stakeholder wants: A row-by-row classification of every plan change event, showing from/to plan, MRR delta, and whether it was expansion, contraction, or neutral — the inputs to the monthly MRR waterfall that investors and finance review.\n\nAmbiguities resolved: \'Plan change\' means an account that has more than one subscription in chronological order. The comparison is current subscription vs the immediately preceding one (not the first one). \'Upgrade\' = positive MRR delta, \'downgrade\' = negative, \'lateral\' = zero delta (same MRR, different plan). First subscriptions per account are not a \'change\' and should not appear in the output.\n\nSQL approach: First CTE uses ROW_NUMBER() OVER (PARTITION BY account_id ORDER BY started_at) to assign a sequence number per subscription. Second CTE self-joins ranked_subs on sub_order = sub_order + 1 to pair each subscription with its predecessor — the \'previous plan\' pattern. Main SELECT joins to accounts and plans twice for names, then classifies by CASE WHEN mrr_delta > 0.\n\nWhat weak SQL looks like: Using LAG(mrr) OVER (PARTITION BY account_id ORDER BY started_at) instead of the self-join — simpler and equally correct; interviewers usually accept either. The ROW_NUMBER + self-join approach is shown here because it\'s more explicit and generalizes to comparing N subscriptions apart. Or using MIN/MAX per account — this only works for the first-to-last comparison, not consecutive changes.\n\nInterviewer follow-up: \'How would you build a full monthly MRR waterfall showing new, expansion, contraction, churn, and reactivation revenue for each month?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-master10',
    title: 'High-Risk Account Flagging',
    company: 'Stripe',
    companyDomain: 'stripe.com',
    difficulty: 'Master',
    isFree: false,
    tags: ['CTE', 'JOIN', 'EXISTS', 'multi-signal', 'fraud', 'risk'],
    roles: ['PA', 'DA', 'BA'],
    priority: 1,
    estimatedMin: 30,
    datamartId: 'fintech',
    prompt: 'The trust and safety team wants to flag users who show multiple risk signals: (1) at least one transaction through a flagged merchant, and (2) at least one open dispute (resolved_at IS NULL). For qualifying users return user_id, email, kyc_status, risk_tier, flagged_merchant_txns (count), and open_dispute_count. Order by open_dispute_count descending.',
    expectedColumns: ['user_id', 'email', 'kyc_status', 'risk_tier', 'flagged_merchant_txns', 'open_dispute_count'],
    expectedRowCount: 1,
    checkValues: [{ user_id: '5' }],
    solution: 'WITH flagged_txns AS (\n  SELECT a.user_id, COUNT(t.txn_id) AS flagged_merchant_txns\n  FROM transactions t\n  JOIN merchants m ON t.merchant_id = m.merchant_id\n  JOIN accounts a ON t.account_id = a.account_id\n  WHERE m.is_flagged = 1\n  GROUP BY a.user_id\n),\nopen_disputes AS (\n  SELECT a.user_id, COUNT(d.dispute_id) AS open_dispute_count\n  FROM disputes d\n  JOIN transactions t ON d.txn_id = t.txn_id\n  JOIN accounts a ON t.account_id = a.account_id\n  WHERE d.resolved_at IS NULL\n  GROUP BY a.user_id\n)\nSELECT u.user_id, u.email, u.kyc_status, u.risk_tier,\n       ft.flagged_merchant_txns, od.open_dispute_count\nFROM users u\nJOIN flagged_txns ft ON u.user_id = ft.user_id\nJOIN open_disputes od ON u.user_id = od.user_id\nORDER BY od.open_dispute_count DESC',
    debrief: 'Only user 5 (eve) meets both criteria: she transacted with QuickTransfer (flagged merchant, txn 9 = $950) AND has an open dispute on txn 31 ($3,500, account 8). The JOIN between the two CTEs implements a logical AND — the user must appear in both signal sets. A LEFT JOIN with WHERE od.open_dispute_count IS NOT NULL would give the same result but obscures the intent. User 3 (carol) also transacted with a flagged merchant (account 5, txn 9 is user 3\'s... wait, account 5 belongs to user 3 in the fintech datamart). This is a two-signal filter: volume alone (flagged merchant) is noisy; requiring an open dispute simultaneously reduces false positives significantly.',
    sqliteNote: null,
  },

  {
    id: 'sql-master11',
    title: 'Referred User Engagement vs Organic',
    company: 'Airbnb',
    companyDomain: 'airbnb.com',
    difficulty: 'Hard',
    isFree: false,
    tags: ['CTE', 'LEFT JOIN', 'CASE WHEN', 'GROUP BY', 'referral quality'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 30,
    datamartId: 'consumer',
    prompt: 'The growth team wants to evaluate whether our referral program is bringing in higher-quality users than organic acquisition. How do referred and organic users compare on engagement and premium conversion?',
    expectedColumns: ['referred_type', 'user_count', 'total_interactions', 'avg_interactions', 'premium_pct'],
    expectedRowCount: 2,
    checkValues: [],
    solution: 'WITH user_types AS (\n  SELECT user_id, is_premium,\n         CASE WHEN referrer_id IS NOT NULL THEN \'referred\' ELSE \'organic\' END AS referred_type\n  FROM users\n),\nuser_interactions AS (\n  SELECT ut.user_id, ut.referred_type, ut.is_premium,\n         COUNT(i.interaction_id) AS interactions\n  FROM user_types ut\n  LEFT JOIN interactions i ON ut.user_id = i.user_id\n  GROUP BY ut.user_id, ut.referred_type, ut.is_premium\n)\nSELECT referred_type,\n       COUNT(user_id) AS user_count,\n       SUM(interactions) AS total_interactions,\n       ROUND(1.0 * SUM(interactions) / COUNT(user_id), 2) AS avg_interactions,\n       ROUND(100.0 * SUM(is_premium) / COUNT(user_id), 1) AS premium_pct\nFROM user_interactions\nGROUP BY referred_type\nORDER BY referred_type',
    debrief: 'What the stakeholder wants: A side-by-side comparison of referred vs organic users across engagement (interactions) and premium conversion rate — to decide whether the referral program justifies its incentive cost.\n\nAmbiguities resolved: \'Referred\' means referrer_id IS NOT NULL; \'organic\' means referrer_id IS NULL. All users must appear in both groups even if they have zero interactions — this requires a LEFT JOIN to interactions. \'Premium_pct\' is the percentage of users in each group who are premium, not the percentage of interactions from premium users.\n\nSQL approach: First CTE tags each user as referred or organic. Second CTE LEFT JOINs to interactions to count per user. Final SELECT aggregates by referred_type: user_count, sum of interactions, average interactions, and ROUND(100.0 * SUM(is_premium) / COUNT(user_id), 1) for premium_pct. SUM(is_premium) works because is_premium is a 0/1 integer.\n\nWhat weak SQL looks like: A single GROUP BY with a CASE WHEN in the SELECT — this works for the classification but can\'t handle the LEFT JOIN requirement cleanly without a CTE. Or INNER JOIN to interactions — drops zero-interaction users and overstates engagement averages for whichever group has more inactive members.\n\nInterviewer follow-up: \'How would you determine whether the difference in premium conversion rate between referred and organic users is statistically significant?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-master12',
    title: 'Prescription Coverage Days by Patient',
    company: 'Optum',
    companyDomain: 'optum.com',
    difficulty: 'Master',
    isFree: false,
    tags: ['CTE', 'GROUP BY', 'SUM', 'ROUND', 'medication adherence'],
    roles: ['PA', 'DA', 'BA'],
    priority: 2,
    estimatedMin: 30,
    datamartId: 'health',
    prompt: 'The pharmacy team wants to understand total medication coverage per patient — how many days of medication supply they have been prescribed in total, factoring in refills. Total coverage days = days_supply * (refills + 1). For each patient with at least one prescription, return patient_id, rx_count, total_coverage_days, and the drug name with the most coverage days (top_drug). Order by total_coverage_days descending.',
    expectedColumns: ['patient_id', 'rx_count', 'total_coverage_days', 'top_drug'],
    expectedRowCount: 14,
    checkValues: [{ patient_id: '2', total_coverage_days: '450' }],
    solution: 'WITH coverage AS (\n  SELECT patient_id, drug_name,\n         SUM(days_supply * (refills + 1)) AS drug_coverage_days\n  FROM prescriptions\n  GROUP BY patient_id, drug_name\n),\npatient_totals AS (\n  SELECT patient_id,\n         COUNT(*) AS rx_count,\n         SUM(drug_coverage_days) AS total_coverage_days\n  FROM coverage\n  GROUP BY patient_id\n),\ntop_drugs AS (\n  SELECT patient_id, drug_name AS top_drug,\n         ROW_NUMBER() OVER (PARTITION BY patient_id ORDER BY drug_coverage_days DESC) AS rn\n  FROM coverage\n)\nSELECT pt.patient_id, pt.rx_count, pt.total_coverage_days,\n       td.top_drug\nFROM patient_totals pt\nJOIN top_drugs td ON pt.patient_id = td.patient_id AND td.rn = 1\nORDER BY pt.total_coverage_days DESC',
    debrief: 'Patient 2 (bob) has 450 total coverage days — the most. His Metformin prescription: 90 days_supply × (2 refills + 1) = 270 days, plus Sertraline: 30 × (5+1) = 180 days, total 450. The formula days_supply × (refills + 1) converts a single-dispense prescription into total days of therapy. The ROW_NUMBER() window function identifies the top drug per patient without a correlated subquery. Three CTEs chain: coverage (per patient-drug), patient_totals (per patient), top_drugs (ranked by coverage per patient). The 14 rows reflect the 14 distinct patients who have at least one prescription.',
    sqliteNote: null,
  },

  {
    id: 'sql-master13',
    title: 'Returning vs New Customer Revenue Split',
    company: 'Shopify',
    companyDomain: 'shopify.com',
    difficulty: 'Hard',
    isFree: false,
    tags: ['CTE', 'ROW_NUMBER', 'window function', 'CASE WHEN', 'cohort'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 30,
    datamartId: 'ecomm',
    prompt: 'The finance team wants to split completed order revenue into new customer revenue (user\'s first ever completed order) vs returning customer revenue. Classify each completed order as "new" or "returning", then aggregate total revenue and order count by customer_type. Return customer_type, order_count, and total_revenue rounded to 2 decimals.',
    expectedColumns: ['customer_type', 'order_count', 'total_revenue'],
    expectedRowCount: 2,
    checkValues: [],
    solution: 'WITH ranked_orders AS (\n  SELECT order_id, user_id, subtotal,\n         ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY created_at) AS order_rank\n  FROM orders\n  WHERE status = \'completed\'\n)\nSELECT\n  CASE WHEN order_rank = 1 THEN \'new\' ELSE \'returning\' END AS customer_type,\n  COUNT(*) AS order_count,\n  ROUND(SUM(subtotal), 2) AS total_revenue\nFROM ranked_orders\nGROUP BY customer_type\nORDER BY customer_type DESC',
    debrief: 'Each user\'s first completed order is classified as "new customer revenue"; all subsequent orders are "returning customer revenue." ROW_NUMBER() within the WHERE status=\'completed\' CTE correctly ranks only completed orders — a returned or cancelled order does not consume the "first" slot. The split between new and returning revenue tells a critical business story: a high returning-customer share signals strong retention and healthy LTV. New customer revenue that always exceeds returning revenue suggests the business is running a leaky bucket — acquiring but not retaining. The exact split for this dataset: 12 users placed their first completed order (new), and several placed multiple (returning).',
    sqliteNote: null,
  },

  {
    id: 'sql-master14',
    title: 'Churned Account Reactivation Candidates',
    company: 'Salesforce',
    companyDomain: 'salesforce.com',
    difficulty: 'Master',
    isFree: false,
    tags: ['CTE', 'JOIN', 'HAVING', 'ROW_NUMBER', 'win-back', 'churn analysis'],
    roles: ['PA', 'DA', 'BA'],
    priority: 2,
    estimatedMin: 30,
    datamartId: 'saas',
    prompt: 'The sales team wants to prioritize churned accounts for win-back outreach. A reactivation candidate is a churned account whose highest MRR subscription exceeded $500. Return account_id, company_name, industry, employee_count, peak_mrr (max MRR across all subscriptions), and churned_at (ended_at of the last subscription). Order by peak_mrr descending.',
    expectedColumns: ['account_id', 'company_name', 'industry', 'employee_count', 'peak_mrr', 'churned_at'],
    expectedRowCount: 3,
    checkValues: [{ account_id: '13', peak_mrr: '999' }],
    solution: 'WITH churned_accounts AS (\n  SELECT account_id, MAX(mrr) AS peak_mrr, MAX(ended_at) AS churned_at\n  FROM subscriptions\n  WHERE status = \'churned\'\n  GROUP BY account_id\n  HAVING MAX(mrr) > 500\n)\nSELECT a.account_id, a.company_name, a.industry, a.employee_count,\n       ca.peak_mrr, ca.churned_at\nFROM churned_accounts ca\nJOIN accounts a ON ca.account_id = a.account_id\nORDER BY ca.peak_mrr DESC',
    debrief: 'Three churned accounts had peak MRR above $500: accounts 1, 3, and 13 — all on the Business plan ($999 MRR). Account 1 is excluded from the final result because it also has an active Enterprise subscription (it upgraded, not fully churned). Wait — account 1\'s sub 1 is churned at $999, but the CTE only looks at churned subscriptions, so account 1 appears with its churned sub. The HAVING MAX(mrr) > 500 filter correctly excludes accounts 14 and 15 (Growth plan, $299 MRR). In a real win-back model, peak_mrr drives the expected contract value, while employee_count and industry inform the outreach message — manufacturing and finance accounts often respond to ROI-focused pitches.',
    sqliteNote: null,
  },

  // master15: fintech - Monthly Transaction Volume by Category
  // master16: consumer - Creator Portfolio Performance
  {
    id: 'sql-master16',
    title: 'Creator Portfolio Performance',
    company: 'YouTube',
    companyDomain: 'youtube.com',
    difficulty: 'Hard',
    isFree: false,
    tags: ['JOIN', 'GROUP BY', 'COUNT DISTINCT', 'CASE WHEN', 'ROUND', 'content analytics'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 25,
    datamartId: 'consumer',
    prompt: 'The creator team wants a portfolio dashboard for each creator. Join content and interactions tables. Return creator_id, username, content_count (distinct content pieces), total_interactions, likes (count of like actions), shares (count of share actions), and avg_per_content (total_interactions / content_count, rounded to 1 decimal). Order by total_interactions descending.',
    expectedColumns: ['creator_id', 'username', 'content_count', 'total_interactions', 'likes', 'shares', 'avg_per_content'],
    expectedRowCount: 6,
    checkValues: [{ creator_id: '1', username: 'alice', content_count: '2', total_interactions: '12', likes: '3' }],
    solution: 'SELECT c.creator_id, u.username,\n       COUNT(DISTINCT c.content_id) AS content_count,\n       COUNT(i.interaction_id) AS total_interactions,\n       SUM(CASE WHEN i.action = \'like\' THEN 1 ELSE 0 END) AS likes,\n       SUM(CASE WHEN i.action = \'share\' THEN 1 ELSE 0 END) AS shares,\n       ROUND(1.0 * COUNT(i.interaction_id) / COUNT(DISTINCT c.content_id), 1) AS avg_per_content\nFROM content c\nJOIN users u ON c.creator_id = u.user_id\nLEFT JOIN interactions i ON c.content_id = i.content_id\nGROUP BY c.creator_id, u.username\nORDER BY total_interactions DESC',
    debrief: 'Six creators have published content. Alice (creator 1) leads with 12 interactions across 2 pieces — 8 on her fitness video and 4 on her travel story — giving an avg of 6.0 per content. The LEFT JOIN on interactions is critical: if an inner join were used and a creator had zero interactions, they would be dropped. The CASE WHEN pivot for likes and shares avoids subqueries and keeps the aggregation in a single pass. avg_per_content multiplied by 1.0 forces float division in SQLite; integer division would truncate to 0 for creators with fewer interactions than content pieces.',
    sqliteNote: null,
  },

  // master17: health - Provider Prescription Volume by Specialty
  // master18: ecomm - Channel First-Order Value Analysis
  {
    id: 'sql-master18',
    title: 'Channel First-Order Value',
    company: 'Shopify',
    companyDomain: 'shopify.com',
    difficulty: 'Master',
    isFree: false,
    tags: ['CTE', 'MIN', 'JOIN', 'AVG', 'ROUND', 'acquisition analytics'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 25,
    datamartId: 'ecomm',
    prompt: 'The growth team wants to evaluate acquisition channel quality by first-order value. A user\'s first order is defined as the order with the earliest created_at. Return channel, buyers (users who placed at least one order), avg_first_order_value (average first-order subtotal, rounded to 2 decimal places), and total_first_order_revenue (sum of first-order subtotals, rounded to 2 decimal places). Order by avg_first_order_value descending.',
    expectedColumns: ['channel', 'buyers', 'avg_first_order_value', 'total_first_order_revenue'],
    expectedRowCount: 4,
    checkValues: [{ channel: 'paid', buyers: '4' }],
    solution: 'WITH user_first_order AS (\n  SELECT user_id, MIN(created_at) AS first_order_date\n  FROM orders\n  GROUP BY user_id\n),\nfirst_order_values AS (\n  SELECT u.channel, o.subtotal\n  FROM user_first_order ufo\n  JOIN orders o ON ufo.user_id = o.user_id AND ufo.first_order_date = o.created_at\n  JOIN users u ON ufo.user_id = u.user_id\n)\nSELECT channel,\n       COUNT(*) AS buyers,\n       ROUND(AVG(subtotal), 2) AS avg_first_order_value,\n       ROUND(SUM(subtotal), 2) AS total_first_order_revenue\nFROM first_order_values\nGROUP BY channel\nORDER BY avg_first_order_value DESC',
    debrief: 'Paid channel users (bob, eve, iris, kate) have the highest average first order at $162.49, driven by eve\'s and iris\'s $249.99 first orders. Email has only one buyer (grace, $159.99) so its average equals its single data point. Users 13–15 have no orders at all and are correctly excluded — they never appear in the CTE. The two-CTE pattern (first_order → join back for subtotal) is cleaner than a correlated subquery or self-join and generalises to any "first event" attribution problem.',
    sqliteNote: null,
  },

  // master19: saas - Account Event Activity Tier
  {
    id: 'sql-master19',
    title: 'Account Event Activity Tier',
    company: 'Gainsight',
    companyDomain: 'gainsight.com',
    difficulty: 'Master',
    isFree: false,
    tags: ['CTE', 'COALESCE', 'CASE WHEN', 'JOIN', 'customer success'],
    roles: ['PA', 'DA', 'BA'],
    priority: 2,
    estimatedMin: 25,
    datamartId: 'saas',
    prompt: 'The customer success team wants to classify each active account by its engagement level. For each account with an active subscription, return account_id, company_name, plan_name, total_events (0 if no events in the events table), and activity_tier: "high" if total_events >= 8, "medium" if >= 3, else "low". Order by total_events descending, then account_id ascending.',
    expectedColumns: ['account_id', 'company_name', 'plan_name', 'total_events', 'activity_tier'],
    expectedRowCount: 12,
    checkValues: [{ account_id: '1', plan_name: 'Enterprise', total_events: '11', activity_tier: 'high' }],
    solution: 'WITH account_events AS (\n  SELECT account_id, COUNT(*) AS total_events\n  FROM events\n  GROUP BY account_id\n),\nactive_subs AS (\n  SELECT s.account_id, p.name AS plan_name\n  FROM subscriptions s\n  JOIN plans p ON s.plan_id = p.plan_id\n  WHERE s.status = \'active\'\n)\nSELECT a.account_id, a.company_name, asub.plan_name,\n       COALESCE(ae.total_events, 0) AS total_events,\n       CASE WHEN COALESCE(ae.total_events, 0) >= 8 THEN \'high\'\n            WHEN COALESCE(ae.total_events, 0) >= 3 THEN \'medium\'\n            ELSE \'low\' END AS activity_tier\nFROM accounts a\nJOIN active_subs asub ON a.account_id = asub.account_id\nLEFT JOIN account_events ae ON a.account_id = ae.account_id\nORDER BY total_events DESC, a.account_id',
    debrief: 'Account 1 (Acme Corp, Enterprise) leads with 11 events — classified as high. Accounts 2, 3, and 4 each have 8-9 events (high). Account 5 (Echo Tech, Enterprise) has 7 events — medium, despite paying the highest MRR. Accounts 9–12 and 11 have zero events — the COALESCE is what prevents NULLs from those accounts showing up as NULL instead of 0. The churned accounts 13–15 are excluded by the active_subs CTE. This pattern — joining plan context to event activity — is the foundation of customer health scoring in CS platforms like Gainsight.',
    sqliteNote: null,
  },

  // master20: fintech - Active User Account Portfolio Summary
  // master21: consumer - Users Who Made Referrals
  {
    id: 'sql-master21',
    title: 'Referral Performance by Referrer',
    company: 'Cash App',
    companyDomain: 'cash.app',
    difficulty: 'Hard',
    isFree: false,
    tags: ['self-join', 'GROUP BY', 'SUM', 'referral analytics'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 20,
    datamartId: 'consumer',
    prompt: 'The growth team wants to rank users by their referral program impact. Which users have brought in the most people, and how many of those referrals converted to premium?',
    expectedColumns: ['user_id', 'username', 'is_premium', 'referrals_made', 'premium_referrals'],
    expectedRowCount: 6,
    checkValues: [{ user_id: '1', username: 'alice', referrals_made: '2', premium_referrals: '0' }],
    solution: 'SELECT u.user_id, u.username, u.is_premium,\n       COUNT(refs.user_id) AS referrals_made,\n       SUM(refs.is_premium) AS premium_referrals\nFROM users u\nJOIN users refs ON refs.referrer_id = u.user_id\nGROUP BY u.user_id, u.username, u.is_premium\nORDER BY referrals_made DESC, u.user_id',
    debrief: 'What the stakeholder wants: A leaderboard of users who have made referrals, showing how many people they brought in and how many of those became premium — the two metrics the growth team uses to identify high-value referrers worth rewarding.\n\nAmbiguities resolved: Only users who referred at least one person should appear — zero-referral users are excluded. \'Premium referrals\' means the count of referred users who have is_premium = 1 at query time (not necessarily at the time of referral). Whether referrers must be premium themselves is not required — include all referrers regardless of their own premium status.\n\nSQL approach: Self-join on users: alias u as the referrer, refs as the referred (WHERE refs.referrer_id = u.user_id). INNER JOIN naturally excludes non-referrers. GROUP BY referrer fields. COUNT(refs.user_id) = referrals_made; SUM(refs.is_premium) = premium_referrals (binary 0/1 flag summed). ORDER BY referrals_made DESC, user_id ASC for determinism.\n\nWhat weak SQL looks like: Separate subqueries for referrals_made and premium_referrals — this scans the users table twice. The self-join computes both in a single pass. Or LEFT JOIN — this would include all 15 users with 0 referrals, requiring a HAVING COUNT > 0 filter to match the intended output.\n\nInterviewer follow-up: \'How would you compute each referrer\'s premium conversion rate, and which referrers would you prioritize for a bonus based on quality rather than volume?\'',
    sqliteNote: null,
  },

  // master22: health - Monthly Appointment No-Show Rate
  // master23: ecomm - Full-Price vs Discounted Revenue by Channel
  {
    id: 'sql-master23',
    title: 'Discount Mix by Acquisition Channel',
    company: 'Wayfair',
    companyDomain: 'wayfair.com',
    difficulty: 'Hard',
    isFree: false,
    tags: ['JOIN', 'CASE WHEN', 'SUM', 'COUNT', 'GROUP BY', 'discount analytics'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 25,
    datamartId: 'ecomm',
    prompt: 'The revenue team wants to understand how heavily each acquisition channel relies on discounts. For completed orders only, return channel, full_price_revenue (subtotal where discount = 0), discounted_revenue (subtotal where discount > 0), full_price_orders (count), and discounted_orders (count). Order by channel ascending.',
    expectedColumns: ['channel', 'full_price_revenue', 'discounted_revenue', 'full_price_orders', 'discounted_orders'],
    expectedRowCount: 4,
    checkValues: [{ channel: 'paid', full_price_orders: '6', discounted_orders: '4' }],
    solution: 'SELECT u.channel,\n       SUM(CASE WHEN o.discount = 0 THEN o.subtotal ELSE 0 END) AS full_price_revenue,\n       SUM(CASE WHEN o.discount > 0 THEN o.subtotal ELSE 0 END) AS discounted_revenue,\n       COUNT(CASE WHEN o.discount = 0 THEN 1 END) AS full_price_orders,\n       COUNT(CASE WHEN o.discount > 0 THEN 1 END) AS discounted_orders\nFROM orders o\nJOIN users u ON o.user_id = u.user_id\nWHERE o.status = \'completed\'\nGROUP BY u.channel\nORDER BY u.channel',
    debrief: 'Paid channel has 10 completed orders split 6 full-price / 4 discounted — the highest discount usage of any channel. Email has only 1 completed order (grace) which was discounted. Referral is split 3/3 — exactly half of referral-acquired customers used a discount. Organic is 5 full-price / 1 discounted. The insight is that paid-channel customers, who were likely acquired through promotional campaigns, continue to expect discounts — a unit-economics concern if acquisition cost is high. COUNT(CASE WHEN ... THEN 1 END) is the standard pattern for conditional counting without SUM.',
    sqliteNote: null,
  },

  // master24: saas - Monthly New Subscription Acquisition by Plan Tier
  // master25: fintech - User Transaction Risk Profile
  {
    id: 'sql-master25',
    title: 'User Transaction Risk Profile',
    company: 'Marqeta',
    companyDomain: 'marqeta.com',
    difficulty: 'Master',
    isFree: false,
    tags: ['CTE', 'JOIN', 'CASE WHEN', 'SUM', 'LEFT JOIN', 'risk analytics'],
    roles: ['PA', 'DA', 'BA'],
    priority: 2,
    estimatedMin: 30,
    datamartId: 'fintech',
    prompt: 'Build me a risk profile for every user who has transacted — something the review team can triage from. I want to know who is elevated risk, who is moderate, and who is clean.',
    expectedColumns: ['user_id', 'email', 'risk_tier', 'kyc_status', 'total_txns', 'disputed_count', 'flagged_merchant_txns', 'risk_level'],
    expectedRowCount: 12,
    checkValues: [{ user_id: '3', disputed_count: '2', flagged_merchant_txns: '2', risk_level: 'elevated' }],
    solution: 'WITH user_txn AS (\n  SELECT a.user_id,\n         COUNT(t.txn_id) AS total_txns,\n         ROUND(SUM(t.amount), 2) AS total_spent,\n         SUM(CASE WHEN t.status = \'disputed\' THEN 1 ELSE 0 END) AS disputed_count,\n         SUM(CASE WHEN m.is_flagged = 1 THEN 1 ELSE 0 END) AS flagged_merchant_txns\n  FROM accounts a\n  JOIN transactions t ON a.account_id = t.account_id\n  LEFT JOIN merchants m ON t.merchant_id = m.merchant_id\n  GROUP BY a.user_id\n)\nSELECT u.user_id, u.email, u.risk_tier, u.kyc_status,\n       ut.total_txns, ut.disputed_count, ut.flagged_merchant_txns,\n       CASE WHEN ut.disputed_count >= 2 OR ut.flagged_merchant_txns >= 2\n                 OR u.risk_tier = \'high\' THEN \'elevated\'\n            WHEN ut.disputed_count >= 1 OR ut.flagged_merchant_txns >= 1 THEN \'moderate\'\n            ELSE \'normal\' END AS risk_level\nFROM users u\nJOIN user_txn ut ON u.user_id = ut.user_id\nORDER BY ut.disputed_count DESC, ut.flagged_merchant_txns DESC, u.user_id',
    debrief: 'What the stakeholder wants: A scored, labeled profile for every transacting user showing the key risk signals — disputed transactions, flagged merchant exposure, and baseline risk tier — so the review team can sort by risk level and work from highest priority down.\n\nAmbiguities resolved: \'Has transacted\' means at least one row in transactions linked through accounts. Risk level classification must be defined: \'elevated\' if disputed_count >= 2 OR flagged_merchant_txns >= 2 OR risk_tier = \'high\'; \'moderate\' if either disputed >= 1 OR flagged >= 1; else \'normal\'. These thresholds are business parameters the candidate must state. Users with NULL merchant_id transactions (P2P transfers) must not be dropped — LEFT JOIN on merchants handles this.\n\nSQL approach: Single CTE joins accounts to transactions to merchants (LEFT JOIN on merchants to preserve NULL merchant_ids). Aggregates per user_id: total_txns, disputed_count via SUM(CASE WHEN status = \'disputed\'), flagged_merchant_txns via SUM(CASE WHEN is_flagged = 1). Main SELECT joins users to the CTE and applies CASE WHEN for risk_level.\n\nWhat weak SQL looks like: INNER JOIN on merchants — drops all transactions without a merchant_id (P2P transfers), understating total_txns and making clean P2P-heavy users appear lower risk than they are. Or three separate subqueries for each signal — correct but re-scans the transactions table three times instead of once.\n\nInterviewer follow-up: \'How would you add a velocity signal — number of transactions in the last 7 days — to the profile, and how would it change the risk classification logic?\'',
    sqliteNote: null,
  },

  // ── EASY e13–e30 ─────────────────────────────────────────────────────────

  {
    id: 'sql-e13',
    title: 'Active Product Catalog',
    company: 'Shopify',
    companyDomain: 'shopify.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'basic filter'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 4,
    datamartId: 'ecomm',
    prompt: 'The merchandising team is running a pricing review and needs a clean list of all currently active products. Return product_id, name, category, and list_price for every product where is_active = 1, ordered by category then list_price ascending.',
    expectedColumns: ['product_id', 'name', 'category', 'list_price'],
    expectedRowCount: 9,
    checkValues: [{ product_id: '5', name: 'Yoga Mat', category: 'apparel', list_price: '34.99' }],
    solution: 'SELECT product_id, name, category, list_price\nFROM products\nWHERE is_active = 1\nORDER BY category, list_price',
    debrief: 'Nine products are active — product 10 (Plant Pot) is excluded because is_active = 0. ORDER BY category, list_price returns apparel first, so Yoga Mat at $34.99 is row 1. A common mistake is omitting the is_active filter — that returns 10 rows and surfaces a discontinued product. Always check for a soft-delete or active-flag column before pulling catalog data.',
    sqliteNote: null,
  },

  {
    id: 'sql-e14',
    title: 'UK User Segment',
    company: 'ASOS',
    companyDomain: 'asos.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'basic filter', 'country segmentation'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 3,
    datamartId: 'ecomm',
    prompt: 'The UK growth team needs a list of all UK-registered users for a regional email promotion. Return user_id, email, and signup_date, ordered by signup_date ascending.',
    expectedColumns: ['user_id', 'email', 'signup_date'],
    expectedRowCount: 3,
    checkValues: [{ user_id: '2', email: 'bob@example.com' }],
    solution: 'SELECT user_id, email, signup_date\nFROM users\nWHERE country = \'UK\'\nORDER BY signup_date',
    debrief: 'Three UK users: bob (Jan 15), hank (Apr 10), ned (Aug 1). The filter requires an exact string match — country = \'UK\' will miss \'United Kingdom\' or \'uk\'. In production, always check how country values were ingested before filtering. If entry was inconsistent, apply LOWER() normalization or use a reference table.',
    sqliteNote: null,
  },

  {
    id: 'sql-e15',
    title: 'Discount Audit',
    company: 'Amazon',
    companyDomain: 'amazon.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'comparison operator', 'discount analysis'],
    roles: ['PA', 'DA'],
    priority: 2,
    estimatedMin: 4,
    datamartId: 'ecomm',
    prompt: 'Finance wants to audit all orders where a discount was applied. Return order_id, user_id, created_at, subtotal, and discount for every order where discount > 0, sorted by discount descending.',
    expectedColumns: ['order_id', 'user_id', 'created_at', 'subtotal', 'discount'],
    expectedRowCount: 8,
    checkValues: [{ order_id: '19', discount: '50.0' }],
    solution: 'SELECT order_id, user_id, created_at, subtotal, discount\nFROM orders\nWHERE discount > 0\nORDER BY discount DESC',
    debrief: 'Eight orders carry a discount. The largest is order 19 — $50 off a $249.99 Smart Watch (20%). Note that discount = 0 rather than NULL for undiscounted orders in this schema, so discount > 0 works cleanly. If the column allowed NULLs, WHERE discount IS NOT NULL AND discount > 0 would be safer. Always inspect the schema comment to know the sentinel value.',
    sqliteNote: null,
  },

  {
    id: 'sql-e16',
    title: 'Orders by Status',
    company: 'Shopify',
    companyDomain: 'shopify.com',
    difficulty: 'Easy',
    tags: ['GROUP BY', 'COUNT', 'aggregation'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 4,
    datamartId: 'ecomm',
    prompt: 'The ops team asked for a quick status breakdown of our orders — they want to understand what share of orders are completed, returned, or cancelled.',
    expectedColumns: ['status', 'order_count'],
    expectedRowCount: 3,
    checkValues: [{ status: 'completed', order_count: '23' }],
    solution: 'SELECT status, COUNT(*) AS order_count\nFROM orders\nGROUP BY status\nORDER BY order_count DESC',
    debrief: 'Completed: 23, returned: 3, cancelled: 2. Return rate is 3/28 = 10.7%. A common mistake is COUNT(order_id) instead of COUNT(*) — they give the same result here, but COUNT(column) skips NULLs and could differ if the column were nullable. COUNT(*) counts all rows regardless of NULLs and is the safer habit for row-count aggregations.',
    sqliteNote: null,
  },

  {
    id: 'sql-e17',
    title: 'Premium User List',
    company: 'eBay',
    companyDomain: 'ebay.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'boolean filter'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 3,
    datamartId: 'ecomm',
    prompt: 'The loyalty team wants to see all premium subscribers for a VIP campaign. Return user_id, email, signup_date, and channel for all users where is_premium = 1, ordered by signup_date.',
    expectedColumns: ['user_id', 'email', 'signup_date', 'channel'],
    expectedRowCount: 6,
    checkValues: [{ user_id: '1', email: 'alice@example.com', channel: 'organic' }],
    solution: 'SELECT user_id, email, signup_date, channel\nFROM users\nWHERE is_premium = 1\nORDER BY signup_date',
    debrief: 'Six premium users: alice, carol, eve, grace, iris, leo. The is_premium column is stored as an integer (0/1) — the standard pattern since SQLite has no native BOOLEAN type. WHERE is_premium = 1 is correct. WHERE is_premium = TRUE also works in SQLite but is less portable across engines.',
    sqliteNote: null,
  },

  {
    id: 'sql-e18',
    title: 'Q1 2024 Order Volume',
    company: 'Target',
    companyDomain: 'target.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'date filter', 'range filter'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 5,
    datamartId: 'ecomm',
    prompt: 'Finance needs all orders placed in Q1 2024 (January through March) for the quarterly revenue close. Return order_id, user_id, created_at, subtotal, and status, ordered by created_at.',
    expectedColumns: ['order_id', 'user_id', 'created_at', 'subtotal', 'status'],
    expectedRowCount: 7,
    checkValues: [{ order_id: '11', user_id: '5' }],
    solution: 'SELECT order_id, user_id, created_at, subtotal, status\nFROM orders\nWHERE created_at >= \'2024-01-01\' AND created_at < \'2024-04-01\'\nORDER BY created_at',
    debrief: 'Seven orders fall in Q1 2024. The range filter >= \'2024-01-01\' AND < \'2024-04-01\' is the safest pattern — it is index-friendly and handles edge cases cleanly. Using BETWEEN would include 2024-03-31 but miss any timestamp on 2024-04-01 at 00:00. For date-only columns like this one, BETWEEN \'2024-01-01\' AND \'2024-03-31\' also works correctly.',
    sqliteNote: null,
  },

  {
    id: 'sql-e19',
    title: 'Sessions by Device',
    company: 'Etsy',
    companyDomain: 'etsy.com',
    difficulty: 'Easy',
    tags: ['GROUP BY', 'COUNT', 'device analysis'],
    roles: ['PA', 'DA'],
    priority: 2,
    estimatedMin: 4,
    datamartId: 'ecomm',
    prompt: 'The UX team is prioritising the mobile redesign and wants to know our device mix across all sessions. What does the split look like?',
    expectedColumns: ['device', 'session_count'],
    expectedRowCount: 3,
    checkValues: [{ device: 'mobile', session_count: '11' }],
    solution: 'SELECT device, COUNT(*) AS session_count\nFROM sessions\nGROUP BY device\nORDER BY session_count DESC',
    debrief: 'Mobile leads with 11 sessions (44%), desktop has 10 (40%), tablet has 4 (16%). The UX team should also look at avg session duration by device — mobile sessions average 150 seconds vs desktop at 323 seconds, suggesting friction on smaller screens rather than healthy quick-check usage. A device count is a starting point, not a conclusion.',
    sqliteNote: null,
  },

  {
    id: 'sql-e20',
    title: 'Tech Industry Accounts',
    company: 'Salesforce',
    companyDomain: 'salesforce.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'basic filter', 'industry segmentation'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 3,
    datamartId: 'saas',
    prompt: 'The sales team is running a product upsell campaign targeting tech-sector accounts. Return account_id, company_name, employee_count, and created_at for all accounts in the tech industry, ordered by employee_count descending.',
    expectedColumns: ['account_id', 'company_name', 'employee_count', 'created_at'],
    expectedRowCount: 3,
    checkValues: [{ account_id: '5', company_name: 'Echo Tech', employee_count: '300' }],
    solution: 'SELECT account_id, company_name, employee_count, created_at\nFROM accounts\nWHERE industry = \'tech\'\nORDER BY employee_count DESC',
    debrief: 'Three tech accounts: Echo Tech (300 seats), Acme Corp (150), November SaaS (100). Echo Tech is the largest and a natural upsell candidate. In a real pipeline you\'d join this to subscriptions to confirm which tier each is on before pitching an upgrade.',
    sqliteNote: null,
  },

  {
    id: 'sql-e21',
    title: 'Active Subscription Roster',
    company: 'HubSpot',
    companyDomain: 'hubspot.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'subscription status'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 3,
    datamartId: 'saas',
    prompt: 'The CS team needs a full roster of active subscriptions for account health scoring. Return sub_id, account_id, plan_id, started_at, and mrr for every active subscription, ordered by mrr descending.',
    expectedColumns: ['sub_id', 'account_id', 'plan_id', 'started_at', 'mrr'],
    expectedRowCount: 12,
    checkValues: [],
    solution: 'SELECT sub_id, account_id, plan_id, started_at, mrr\nFROM subscriptions\nWHERE status = \'active\'\nORDER BY mrr DESC',
    debrief: 'Twelve active subscriptions remain — five churned (accounts 1 original, 3 original, 13, 14, 15). The three Enterprise accounts generate $2,999 MRR each. Two Starter accounts pay $0. A natural follow-up: GROUP BY plan_id with SUM(mrr) to get total MRR by tier — the metric finance actually tracks.',
    sqliteNote: null,
  },

  {
    id: 'sql-e22',
    title: 'Free Plan Accounts',
    company: 'Notion',
    companyDomain: 'notion.so',
    difficulty: 'Easy',
    tags: ['JOIN', 'filter', 'free tier'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 5,
    datamartId: 'saas',
    prompt: 'Growth wants to run an upgrade campaign. Pull a list of all accounts currently on the free plan so we can prioritise outreach.',
    expectedColumns: ['account_id', 'company_name', 'industry'],
    expectedRowCount: 2,
    checkValues: [{ account_id: '9', company_name: 'India Foods' }],
    solution: 'SELECT a.account_id, a.company_name, a.industry\nFROM accounts a\nJOIN subscriptions s ON a.account_id = s.account_id\nJOIN plans p ON s.plan_id = p.plan_id\nWHERE s.status = \'active\'\n  AND p.tier = \'free\'\nORDER BY a.account_id',
    debrief: 'Two accounts are on the active free Starter plan: India Foods (food) and Lima Education (education). The three-table JOIN is necessary because tier lives in plans, not subscriptions. Filtering for status = \'active\' is equally important — without it, you might surface churned accounts that were once free, inflating your upgrade target list.',
    sqliteNote: null,
  },

  {
    id: 'sql-e23',
    title: 'Verified Low-Risk Users',
    company: 'Stripe',
    companyDomain: 'stripe.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'multi-condition filter', 'risk management'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 4,
    datamartId: 'fintech',
    prompt: 'The compliance team needs users who are both fully verified AND classified as low risk, to fast-track them for premium product access. Return user_id, email, and country where kyc_status = \'verified\' AND risk_tier = \'low\', ordered by user_id.',
    expectedColumns: ['user_id', 'email', 'country'],
    expectedRowCount: 5,
    checkValues: [{ user_id: '1', email: 'alice@fin.com', country: 'US' }],
    solution: 'SELECT user_id, email, country\nFROM users\nWHERE kyc_status = \'verified\'\n  AND risk_tier = \'low\'\nORDER BY user_id',
    debrief: 'Five users are both verified and low-risk: alice, carol, eve, grace, jack. User 12 (leo) is low-risk but pending KYC — excluded by the first condition. User 4 is verified but high-risk — excluded by the second. AND logic means both conditions must be true simultaneously. When compliance asks for "safe" users, you almost always need both identity verification and risk scoring.',
    sqliteNote: null,
  },

  {
    id: 'sql-e24',
    title: 'High-Balance Active Accounts',
    company: 'Robinhood',
    companyDomain: 'robinhood.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'range filter', 'multi-condition'],
    roles: ['PA', 'DA'],
    priority: 2,
    estimatedMin: 4,
    datamartId: 'fintech',
    prompt: 'The wealth team wants to identify active accounts holding more than $5,000 for a premium advisory upsell. Return account_id, user_id, account_type, and balance for all active accounts where balance > 5000, sorted by balance descending.',
    expectedColumns: ['account_id', 'user_id', 'account_type', 'balance'],
    expectedRowCount: 9,
    checkValues: [{ account_id: '5', balance: '25000.0' }],
    solution: 'SELECT account_id, user_id, account_type, balance\nFROM accounts\nWHERE status = \'active\'\n  AND balance > 5000\nORDER BY balance DESC',
    debrief: 'Nine of the thirteen active accounts hold over $5,000. The investment account (account 5) tops at $25,000. The frozen account (6) and closed account (12) are excluded by status = \'active\'. This filter matters: a frozen high-balance account should not be marketed to. Both conditions are required to identify genuine upsell candidates.',
    sqliteNote: null,
  },

  {
    id: 'sql-e25',
    title: 'Flagged Merchant Registry',
    company: 'PayPal',
    companyDomain: 'paypal.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'boolean filter', 'fraud prevention'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 3,
    datamartId: 'fintech',
    prompt: 'The fraud team needs a list of all merchants flagged as fraud-risk so they can be routed to manual review. Return merchant_id, name, category, and country for all merchants where is_flagged = 1, ordered by merchant_id.',
    expectedColumns: ['merchant_id', 'name', 'category', 'country'],
    expectedRowCount: 3,
    checkValues: [{ merchant_id: '7', name: 'QuickTransfer', country: 'NG' }],
    solution: 'SELECT merchant_id, name, category, country\nFROM merchants\nWHERE is_flagged = 1\nORDER BY merchant_id',
    debrief: 'Three flagged merchants: QuickTransfer (Nigeria), CryptoXchange (China), FastCash Ltd (Brazil). All three appear under the \'shopping\' category despite being transfer/exchange services — a sign that the category field is not granular enough for fraud classification. In a real system you\'d flag transactions to these merchants in real time, not just query the list after the fact.',
    sqliteNote: null,
  },

  {
    id: 'sql-e26',
    title: 'Open Dispute Queue',
    company: 'Visa',
    companyDomain: 'visa.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'IS NULL', 'NULL handling'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 4,
    datamartId: 'fintech',
    prompt: 'The disputes team needs to see all unresolved disputes to prioritize their workload. Return dispute_id, txn_id, opened_at, and amount for all disputes where resolved_at IS NULL, ordered by amount descending.',
    expectedColumns: ['dispute_id', 'txn_id', 'opened_at', 'amount'],
    expectedRowCount: 3,
    checkValues: [{ dispute_id: '6', amount: '3500.0' }],
    solution: 'SELECT dispute_id, txn_id, opened_at, amount\nFROM disputes\nWHERE resolved_at IS NULL\nORDER BY amount DESC',
    debrief: 'Three open disputes: dispute 6 ($3,500), dispute 4 ($490), dispute 3 ($89.99). Dispute 6 is the most urgent — txn 31 at a flagged merchant (QuickTransfer). IS NULL is the required syntax — WHERE resolved_at = NULL always returns zero rows because NULL does not equal anything, including itself, by SQL standard.',
    sqliteNote: null,
  },

  {
    id: 'sql-e28',
    title: 'iOS User Segment',
    company: 'TikTok',
    companyDomain: 'tiktok.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'platform filter'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 3,
    datamartId: 'consumer',
    prompt: 'The iOS engineering team needs a list of all iOS users to scope a feature rollout. Return user_id, username, country, and joined_at for all users where device_os = \'ios\', ordered by joined_at.',
    expectedColumns: ['user_id', 'username', 'country', 'joined_at'],
    expectedRowCount: 6,
    checkValues: [{ user_id: '1', username: 'alice', country: 'US' }],
    solution: 'SELECT user_id, username, country, joined_at\nFROM users\nWHERE device_os = \'ios\'\nORDER BY joined_at',
    debrief: 'Six iOS users: alice, david, grace, julia, mike, oscar — all US-based. iOS represents 40% of the user base (6/15). Note that device_os captures the signup device, not every device a user may use. For accurate platform penetration, you\'d need session-level device data from the sessions table.',
    sqliteNote: null,
  },

  {
    id: 'sql-e29',
    title: 'Providers Accepting New Patients',
    company: 'Oscar Health',
    companyDomain: 'hioscar.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'boolean filter', 'availability'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 3,
    datamartId: 'health',
    prompt: 'Member services needs to know which providers are currently accepting new patients to route incoming referrals. Return provider_id, name, specialty, and years_exp for all providers where accepts_new = 1, ordered by years_exp descending.',
    expectedColumns: ['provider_id', 'name', 'specialty', 'years_exp'],
    expectedRowCount: 4,
    checkValues: [{ provider_id: '2', name: 'Dr. Patel', specialty: 'cardiology', years_exp: '22' }],
    solution: 'SELECT provider_id, name, specialty, years_exp\nFROM providers\nWHERE accepts_new = 1\nORDER BY years_exp DESC',
    debrief: 'Four providers accept new patients: Dr. Patel (cardiology, 22yr), Dr. Smith (primary care, 15yr), Dr. Lee (dermatology, 8yr), Dr. Johnson (orthopedics, 5yr). Dr. Williams has 30 years of experience but is closed — a tension worth surfacing to operations if demand for primary care outstrips supply.',
    sqliteNote: null,
  },

  {
    id: 'sql-e30',
    title: 'Uninsured Patient List',
    company: 'Teladoc',
    companyDomain: 'teladoc.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'IS NULL', 'NULL handling'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 4,
    datamartId: 'health',
    prompt: 'The care coordination team needs to identify uninsured patients to connect them with financial assistance programs. Return patient_id, gender, zip_code, and registered_at for all patients where insurance_id IS NULL, ordered by registered_at.',
    expectedColumns: ['patient_id', 'gender', 'zip_code', 'registered_at'],
    expectedRowCount: 3,
    checkValues: [{ patient_id: '4', gender: 'M', zip_code: '10003' }],
    solution: 'SELECT patient_id, gender, zip_code, registered_at\nFROM patients\nWHERE insurance_id IS NULL\nORDER BY registered_at',
    debrief: 'Three uninsured patients: patient 4 (10003), patient 9 (10005), patient 14 (10002). The key pattern is IS NULL — not = NULL, which returns zero rows in every SQL database because NULL is not equal to anything, including itself. Patient 14 is the youngest (born 2005) and registered most recently, suggesting younger patients may be systematically underinsured.',
    sqliteNote: null,
  },

  // ── EASY e31–e48 ─────────────────────────────────────────────────────────

  {
    id: 'sql-e31',
    title: 'No-Show Appointment Log',
    company: 'One Medical',
    companyDomain: 'onemedical.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'boolean filter', 'operational report'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 3,
    datamartId: 'health',
    prompt: 'Operations needs a log of all missed appointments for a no-show outreach campaign. Return appt_id, patient_id, provider_id, scheduled_at, and visit_type for all appointments where no_show = 1, ordered by scheduled_at.',
    expectedColumns: ['appt_id', 'patient_id', 'provider_id', 'scheduled_at', 'visit_type'],
    expectedRowCount: 6,
    checkValues: [{ appt_id: '3', patient_id: '3', provider_id: '1' }],
    solution: 'SELECT appt_id, patient_id, provider_id, scheduled_at, visit_type\nFROM appointments\nWHERE no_show = 1\nORDER BY scheduled_at',
    debrief: 'Six no-shows total: four belong to Dr. Smith (provider 1), one to Dr. Patel, one to Dr. Johnson. Provider 1 has the highest no-show rate (4/10 = 40%). Before concluding provider 1 has a problem, check whether their appointment mix (visit type, patient demographics) differs from others — no-show rates are heavily confounded by patient characteristics.',
    sqliteNote: null,
  },

  {
    id: 'sql-e32',
    title: 'Most Prescribed Drugs',
    company: 'CVS Health',
    companyDomain: 'cvshealth.com',
    difficulty: 'Easy',
    tags: ['GROUP BY', 'COUNT', 'ORDER BY'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 4,
    datamartId: 'health',
    prompt: 'The pharmacy team is reviewing prescribing patterns and wants to know what we\'re prescribing the most. Give me a breakdown by drug.',
    expectedColumns: ['drug_name', 'rx_count'],
    expectedRowCount: 11,
    checkValues: [{ drug_name: 'Lisinopril', rx_count: '5' }],
    solution: 'SELECT drug_name, COUNT(*) AS rx_count\nFROM prescriptions\nGROUP BY drug_name\nORDER BY rx_count DESC',
    debrief: 'Lisinopril leads with 5 prescriptions — consistent with hypertension being the most common diagnosis (5 I10 diagnoses). Metformin and Atorvastatin each have 3. The remaining 8 drugs appear once each. Drug utilization data like this drives formulary decisions, but always join to diagnoses to confirm the drug-indication alignment before drawing conclusions.',
    sqliteNote: null,
  },

  {
    id: 'sql-e33',
    title: 'Transactions by Category',
    company: 'Mastercard',
    companyDomain: 'mastercard.com',
    difficulty: 'Easy',
    tags: ['GROUP BY', 'COUNT', 'category analysis'],
    roles: ['PA', 'DA'],
    priority: 1,
    estimatedMin: 4,
    datamartId: 'fintech',
    prompt: 'The analytics team wants to understand which merchant categories are seeing the most transaction volume. Can you break that down?',
    expectedColumns: ['category', 'txn_count'],
    expectedRowCount: 5,
    checkValues: [{ category: 'shopping', txn_count: '14' }],
    solution: 'SELECT category, COUNT(*) AS txn_count\nFROM transactions\nGROUP BY category\nORDER BY txn_count DESC',
    debrief: 'Shopping dominates at 14 transactions (35%), followed by food (9), travel (7), entertainment (6), utilities (4). Shopping\'s high share partly reflects that flagged merchants are categorized as \'shopping\' — a categorization artifact. A cleaner analysis would separate flagged-merchant transactions from legitimate retail purchases before ranking categories by risk-adjusted volume.',
    sqliteNote: null,
  },

  {
    id: 'sql-e34',
    title: 'Content Interaction Types',
    company: 'Instagram',
    companyDomain: 'instagram.com',
    difficulty: 'Easy',
    tags: ['GROUP BY', 'COUNT', 'engagement analysis'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 4,
    datamartId: 'consumer',
    prompt: 'The engagement team needs to know which interaction types are most common — they\'re recalibrating the recommender weights and want the actual distribution.',
    expectedColumns: ['action', 'interaction_count'],
    expectedRowCount: 5,
    checkValues: [{ action: 'view', interaction_count: '14' }],
    solution: 'SELECT action, COUNT(*) AS interaction_count\nFROM interactions\nGROUP BY action\nORDER BY interaction_count DESC',
    debrief: 'Views (14) vastly outnumber all other actions combined (16 total). Likes (8) are the second most common signal. Share (2) is the rarest — shares are often the highest-intent signal for virality, so their low count is meaningful. A recommender that weights all interactions equally would over-index on passive views; this breakdown shows why signal weighting matters.',
    sqliteNote: null,
  },

  {
    id: 'sql-e35',
    title: 'Session Source Mix',
    company: 'Google',
    companyDomain: 'google.com',
    difficulty: 'Easy',
    tags: ['GROUP BY', 'COUNT', 'acquisition analysis'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 4,
    datamartId: 'ecomm',
    prompt: 'Growth wants to know which acquisition sources are driving the most sessions right now. Pull the breakdown.',
    expectedColumns: ['source', 'session_count'],
    expectedRowCount: 5,
    checkValues: [{ source: 'organic', session_count: '9' }],
    solution: 'SELECT source, COUNT(*) AS session_count\nFROM sessions\nGROUP BY source\nORDER BY session_count DESC',
    debrief: 'Organic leads with 9 sessions (36%), paid has 7 (28%), then referral, email, and social tie at 3 each (12% each). Volume alone is misleading — referral converts at 100% while social converts at 0%. The next query should add AVG(converted) per source to reveal which channels actually drive revenue.',
    sqliteNote: null,
  },

  {
    id: 'sql-e36',
    title: 'Top 3 Most Expensive Products',
    company: 'Best Buy',
    companyDomain: 'bestbuy.com',
    difficulty: 'Easy',
    tags: ['ORDER BY', 'LIMIT', 'top-N'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 3,
    datamartId: 'ecomm',
    prompt: 'The category team wants to know the three highest list-price products to inform their premium shelf positioning. Return product_id, name, category, and list_price for the top 3 products by list_price descending.',
    expectedColumns: ['product_id', 'name', 'category', 'list_price'],
    expectedRowCount: 3,
    checkValues: [{ product_id: '3', name: 'Smart Watch', list_price: '249.99' }],
    solution: 'SELECT product_id, name, category, list_price\nFROM products\nORDER BY list_price DESC\nLIMIT 3',
    debrief: 'The three most expensive products are Smart Watch ($249.99), Winter Jacket ($159.99), and Wireless Headphones ($89.99). Smart Watch is also the highest-margin product — cost is $120, list price $249.99, yielding a 108% markup. LIMIT is simple but has a footgun: if two products shared the $249.99 price, you\'d arbitrarily drop one. Use LIMIT with a tiebreaker ORDER BY column (e.g. product_id) to make results deterministic.',
    sqliteNote: null,
  },

  {
    id: 'sql-e37',
    title: 'Events per Account',
    company: 'Mixpanel',
    companyDomain: 'mixpanel.com',
    difficulty: 'Easy',
    tags: ['GROUP BY', 'COUNT', 'engagement'],
    roles: ['PA', 'DA'],
    priority: 2,
    estimatedMin: 4,
    datamartId: 'saas',
    prompt: 'The product team wants to rank accounts by total event activity to identify the most engaged customers. Return account_id and event_count, ordered by event_count descending.',
    expectedColumns: ['account_id', 'event_count'],
    expectedRowCount: 8,
    checkValues: [{ account_id: '1', event_count: '11' }],
    solution: 'SELECT account_id, COUNT(*) AS event_count\nFROM events\nGROUP BY account_id\nORDER BY event_count DESC',
    debrief: 'Account 1 (Acme Corp) is most active with 11 events; account 8 (Hotel Chain) has just 1. Only 8 of 15 accounts appear — the other 7 have zero events and are invisible in this GROUP BY. To surface zero-event accounts, you\'d need a LEFT JOIN from accounts to events. This is a common analyst oversight: GROUP BY only sees rows that exist.',
    sqliteNote: null,
  },

  {
    id: 'sql-e39',
    title: 'Repeat Buyers',
    company: 'Amazon',
    companyDomain: 'amazon.com',
    difficulty: 'Easy',
    tags: ['GROUP BY', 'HAVING', 'repeat purchase'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 5,
    datamartId: 'ecomm',
    prompt: 'The retention team defines a "repeat buyer" as any user with 2 or more orders. Return user_id and order_count for all repeat buyers, ordered by order_count descending.',
    expectedColumns: ['user_id', 'order_count'],
    expectedRowCount: 11,
    checkValues: [{ user_id: '5', order_count: '5' }],
    solution: 'SELECT user_id, COUNT(*) AS order_count\nFROM orders\nGROUP BY user_id\nHAVING COUNT(*) >= 2\nORDER BY order_count DESC',
    debrief: 'Eleven users are repeat buyers. User 5 (eve) leads with 5 orders, including three consecutive days in January 2024. HAVING filters after aggregation — it is the only way to filter on aggregated values. WHERE COUNT(*) >= 2 is a syntax error because WHERE runs before GROUP BY and has no access to the aggregated count. Users 13–15 have no orders at all and correctly do not appear.',
    sqliteNote: null,
  },

  {
    id: 'sql-e40',
    title: 'Appointments per Provider',
    company: 'Zocdoc',
    companyDomain: 'zocdoc.com',
    difficulty: 'Easy',
    tags: ['GROUP BY', 'COUNT', 'workload analysis'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 4,
    datamartId: 'health',
    prompt: 'Operations wants to understand appointment load per provider to identify over-capacity situations. Return provider_id and appt_count for each provider, ordered by appt_count descending.',
    expectedColumns: ['provider_id', 'appt_count'],
    expectedRowCount: 6,
    checkValues: [{ provider_id: '1', appt_count: '10' }],
    solution: 'SELECT provider_id, COUNT(*) AS appt_count\nFROM appointments\nGROUP BY provider_id\nORDER BY appt_count DESC',
    debrief: 'Dr. Smith (provider 1) carries 10 appointments — more than twice any other provider. Provider 4 (psychiatry) and provider 6 (primary care) each have just 2. Provider 6 is not accepting new patients (accepts_new = 0) despite low volume — operations should explore whether capacity issues or panel closure policy is responsible.',
    sqliteNote: null,
  },

  {
    id: 'sql-e42',
    title: 'Enterprise-Eligible Accounts',
    company: 'Slack',
    companyDomain: 'slack.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'HAVING', 'range filter'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 4,
    datamartId: 'saas',
    prompt: 'Sales defines enterprise-eligible as accounts with 100 or more employees. Return account_id, company_name, industry, and employee_count for all such accounts, ordered by employee_count descending.',
    expectedColumns: ['account_id', 'company_name', 'industry', 'employee_count'],
    expectedRowCount: 7,
    checkValues: [{ account_id: '13', company_name: 'Mike Manufacturing', employee_count: '500' }],
    solution: 'SELECT account_id, company_name, industry, employee_count\nFROM accounts\nWHERE employee_count >= 100\nORDER BY employee_count DESC',
    debrief: 'Seven accounts qualify: Mike Manufacturing (500), Echo Tech (300), Kilo Pharma (250), Capitol Finance (200), Golf Logistics (120), November SaaS (100), Acme Corp (150). The most valuable upsell targets are those currently on Growth or Business plans — joining to subscriptions would immediately reveal which of these seven have headroom to move to Enterprise.',
    sqliteNote: null,
  },

  {
    id: 'sql-e43',
    title: 'Pending KYC Queue',
    company: 'Chime',
    companyDomain: 'chime.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'status filter', 'operational queue'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 3,
    datamartId: 'fintech',
    prompt: 'The compliance operations team needs to work through the KYC backlog. Return user_id, email, risk_tier, and created_at for all users where kyc_status = \'pending\', ordered by created_at.',
    expectedColumns: ['user_id', 'email', 'risk_tier', 'created_at'],
    expectedRowCount: 2,
    checkValues: [{ user_id: '6', email: 'frank@fin.com', risk_tier: 'medium' }],
    solution: 'SELECT user_id, email, risk_tier, created_at\nFROM users\nWHERE kyc_status = \'pending\'\nORDER BY created_at',
    debrief: 'Two users are pending: frank (medium risk, Mar 10) and leo (low risk, Jun 15). Frank should be prioritized — medium-risk pending users represent more compliance exposure than low-risk pending users. In a real SLA model you\'d also compute days_since_registration to flag accounts that have been pending too long.',
    sqliteNote: null,
  },

  {
    id: 'sql-e44',
    title: 'Consumer Users by Country',
    company: 'Duolingo',
    companyDomain: 'duolingo.com',
    difficulty: 'Easy',
    tags: ['GROUP BY', 'COUNT', 'geographic analysis'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 4,
    datamartId: 'consumer',
    prompt: 'The international team needs a country breakdown of our user base to figure out where to invest in localisation first.',
    expectedColumns: ['country', 'user_count'],
    expectedRowCount: 6,
    checkValues: [{ country: 'US', user_count: '10' }],
    solution: 'SELECT country, COUNT(*) AS user_count\nFROM users\nGROUP BY country\nORDER BY user_count DESC',
    debrief: 'The US accounts for 10 of 15 users (67%). GB, CA, AU, DE, IN each have 1 user. If localization ROI is tied to user count, the US dominates every decision — but consider that non-US users may have higher willingness to pay or lower CAC in their home markets. A single-dimension count rarely tells the full story.',
    sqliteNote: null,
  },

  {
    id: 'sql-e45',
    title: 'Electronics Catalog',
    company: 'Apple',
    companyDomain: 'apple.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'category filter'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 3,
    datamartId: 'ecomm',
    prompt: 'The electronics buyer needs to review only the electronics category before a vendor meeting. Return product_id, name, brand, cost, and list_price for all electronics products, ordered by list_price descending.',
    expectedColumns: ['product_id', 'name', 'brand', 'cost', 'list_price'],
    expectedRowCount: 3,
    checkValues: [{ product_id: '3', name: 'Smart Watch', list_price: '249.99' }],
    solution: 'SELECT product_id, name, brand, cost, list_price\nFROM products\nWHERE category = \'electronics\'\nORDER BY list_price DESC',
    debrief: 'Three electronics products: Smart Watch ($249.99), Wireless Headphones ($89.99), Bluetooth Speaker ($59.99). All are from TechBrand. Margin percentages: Smart Watch 108%, Wireless Headphones 100%, Bluetooth Speaker 100%. Even within one brand, knowing both cost and list_price is essential for negotiating better COGS rather than just pushing list price higher.',
    sqliteNote: null,
  },

  {
    id: 'sql-e46',
    title: 'Telehealth Appointments',
    company: 'Hims & Hers',
    companyDomain: 'forhims.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'visit type filter'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 3,
    datamartId: 'health',
    prompt: 'The telehealth product team needs a log of all telehealth appointments to measure platform utilization. Return appt_id, patient_id, provider_id, scheduled_at, and no_show for all telehealth visits, ordered by scheduled_at.',
    expectedColumns: ['appt_id', 'patient_id', 'provider_id', 'scheduled_at', 'no_show'],
    expectedRowCount: 7,
    checkValues: [{ appt_id: '2', patient_id: '2', provider_id: '1', no_show: '0' }],
    solution: 'SELECT appt_id, patient_id, provider_id, scheduled_at, no_show\nFROM appointments\nWHERE visit_type = \'telehealth\'\nORDER BY scheduled_at',
    debrief: 'Seven telehealth appointments, including one no-show (appt 9 — patient 9 did not attend a telehealth session with Dr. Smith). Telehealth no-show rates are worth comparing to in-person no-show rates — telehealth often sees higher no-shows because the friction of not attending is lower when you\'re already at home.',
    sqliteNote: null,
  },

  {
    id: 'sql-e47',
    title: 'Churned Subscription Log',
    company: 'Zendesk',
    companyDomain: 'zendesk.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'churn analysis'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 3,
    datamartId: 'saas',
    prompt: 'The retention team is building a churn analysis and needs all churned subscriptions. Return sub_id, account_id, plan_id, started_at, ended_at, and mrr for all churned subscriptions, ordered by ended_at.',
    expectedColumns: ['sub_id', 'account_id', 'plan_id', 'started_at', 'ended_at', 'mrr'],
    expectedRowCount: 5,
    checkValues: [{ sub_id: '1', account_id: '1', mrr: '999.0' }],
    solution: 'SELECT sub_id, account_id, plan_id, started_at, ended_at, mrr\nFROM subscriptions\nWHERE status = \'churned\'\nORDER BY ended_at',
    debrief: 'Five churned subscriptions. Account 1 churned its original Business plan ($999 MRR) but then re-subscribed at Enterprise ($2,999) — so the churn is deceptive. Account 3 downgraded rather than churned outright (still active on Growth). Accounts 13, 14, 15 are genuine churn. Always check whether a churned account has a subsequent active subscription before classifying them as lost.',
    sqliteNote: null,
  },

  {
    id: 'sql-e48',
    title: 'Disputed Transactions',
    company: 'American Express',
    companyDomain: 'americanexpress.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'status filter', 'fraud analysis'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 4,
    datamartId: 'fintech',
    prompt: 'The risk team needs a full list of disputed transactions to assess exposure. Return txn_id, account_id, merchant_id, amount, occurred_at, and category for all transactions where status = \'disputed\', ordered by amount descending.',
    expectedColumns: ['txn_id', 'account_id', 'merchant_id', 'amount', 'occurred_at', 'category'],
    expectedRowCount: 6,
    checkValues: [{ txn_id: '31', amount: '3500.0', category: 'shopping' }],
    solution: 'SELECT txn_id, account_id, merchant_id, amount, occurred_at, category\nFROM transactions\nWHERE status = \'disputed\'\nORDER BY amount DESC',
    debrief: 'Six disputed transactions totaling $5,379.99. The largest is txn 31 ($3,500 at merchant 7 — QuickTransfer, a flagged merchant). Two of the six disputed transactions involve flagged merchants (merchants 7 and 9). Joining to the merchants table would immediately surface this flag, turning a status filter into a fraud pattern query.',
    sqliteNote: null,
  },

  // ── EASY e49–e66 ─────────────────────────────────────────────────────────

  {
    id: 'sql-e49',
    title: 'Avg Session Duration by Source',
    company: 'Shopify',
    companyDomain: 'shopify.com',
    difficulty: 'Easy',
    tags: ['GROUP BY', 'AVG', 'acquisition analysis'],
    roles: ['PA', 'DA'],
    priority: 2,
    estimatedMin: 5,
    datamartId: 'ecomm',
    prompt: 'The growth team wants to know whether users from different acquisition sources engage differently in a session. Compare session duration by source.',
    expectedColumns: ['source', 'avg_duration_sec'],
    expectedRowCount: 5,
    checkValues: [{ source: 'referral', avg_duration_sec: '300.0' }],
    solution: 'SELECT source, AVG(duration_sec) AS avg_duration_sec\nFROM sessions\nGROUP BY source\nORDER BY avg_duration_sec DESC',
    debrief: 'Referral sessions average 300 seconds (longest) and convert at 100%. Paid sessions are the second longest at ~293 seconds. Social is shortest at ~187 seconds and converts at 0%. Duration and conversion rate often diverge — long sessions from a low-converting source may indicate confusion rather than engagement. Always pair session duration with conversion rate before ranking channel quality.',
    sqliteNote: null,
  },

  {
    id: 'sql-e50',
    title: 'High Gross-Profit Products',
    company: 'Walmart',
    companyDomain: 'walmart.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'arithmetic expression', 'computed filter'],
    roles: ['PA', 'DA'],
    priority: 2,
    estimatedMin: 5,
    datamartId: 'ecomm',
    prompt: 'The category team wants to highlight products with a gross profit (list_price minus cost) above $30 per unit. Return product_id, name, category, and a computed column gross_profit, ordered by gross_profit descending.',
    expectedColumns: ['product_id', 'name', 'category', 'gross_profit'],
    expectedRowCount: 4,
    checkValues: [{ product_id: '3', name: 'Smart Watch', gross_profit: '129.99' }],
    solution: 'SELECT product_id, name, category, list_price - cost AS gross_profit\nFROM products\nWHERE list_price - cost > 30\nORDER BY gross_profit DESC',
    debrief: 'Four products exceed $30 gross profit per unit: Smart Watch ($129.99), Winter Jacket ($79.99), Wireless Headphones ($44.99), Running Shoes ($39.99). The WHERE clause references list_price - cost directly — you cannot reference the alias gross_profit in WHERE because WHERE is evaluated before SELECT. To filter on a computed alias you need a subquery or CTE.',
    sqliteNote: null,
  },

  {
    id: 'sql-e51',
    title: 'Distinct Buyers Count',
    company: 'Etsy',
    companyDomain: 'etsy.com',
    difficulty: 'Easy',
    tags: ['COUNT DISTINCT', 'unique users'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 4,
    datamartId: 'ecomm',
    prompt: 'Quick question from the CMO — how many unique users have actually placed an order? Need a single number for the deck.',
    expectedColumns: ['buyer_count'],
    expectedRowCount: 1,
    checkValues: [{ buyer_count: '12' }],
    solution: 'SELECT COUNT(DISTINCT user_id) AS buyer_count\nFROM orders',
    debrief: '12 of 15 registered users have placed at least one order — an 80% buyer activation rate. Users 13, 14, and 15 have never ordered. COUNT(DISTINCT user_id) counts each user once regardless of how many orders they placed. COUNT(user_id) would return 28 (total orders), and COUNT(*) also returns 28. DISTINCT is the critical keyword here for a unique-user count.',
    sqliteNote: null,
  },

  {
    id: 'sql-e52',
    title: 'US Customer Orders',
    company: 'Amazon',
    companyDomain: 'amazon.com',
    difficulty: 'Easy',
    tags: ['INNER JOIN', '2-table JOIN', 'country filter'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 5,
    datamartId: 'ecomm',
    prompt: 'The US logistics team needs a list of all orders placed by US-based customers. Return order_id, user_id, email, created_at, and subtotal for all orders where country = \'US\', ordered by created_at.',
    expectedColumns: ['order_id', 'user_id', 'email', 'created_at', 'subtotal'],
    expectedRowCount: 15,
    checkValues: [],
    solution: 'SELECT o.order_id, o.user_id, u.email, o.created_at, o.subtotal\nFROM orders o\nJOIN users u ON o.user_id = u.user_id\nWHERE u.country = \'US\'\nORDER BY o.created_at',
    debrief: 'US users (1,3,5,7,9) placed 15 of the 28 total orders (54%). The JOIN on user_id connects orders to user attributes — without it, you can\'t filter by country since orders only stores user_id. This is the fundamental reason to JOIN rather than filter the orders table directly: customer attributes live in users, not orders.',
    sqliteNote: null,
  },

  {
    id: 'sql-e53',
    title: 'Accounts with CSM 1',
    company: 'Gainsight',
    companyDomain: 'gainsight.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'filter', 'account management'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 3,
    datamartId: 'saas',
    prompt: 'CSM 1 is going on leave and their accounts need to be reassigned. Return account_id, company_name, industry, and employee_count for all accounts managed by csm_id = 1, ordered by company_name.',
    expectedColumns: ['account_id', 'company_name', 'industry', 'employee_count'],
    expectedRowCount: 6,
    checkValues: [{ account_id: '1', company_name: 'Acme Corp' }],
    solution: 'SELECT account_id, company_name, industry, employee_count\nFROM accounts\nWHERE csm_id = 1\nORDER BY company_name',
    debrief: 'CSM 1 manages 6 accounts: Acme Corp, Capitol Finance, Foxtrot Media, India Foods, Lima Education, Oscar Agency. This spans tech, finance, media, food, and education — a wide industry mix that makes the CSM harder to replace with a specialist. When reassigning, match by industry familiarity, not just by headcount.',
    sqliteNote: null,
  },

  {
    id: 'sql-e54',
    title: 'Referred Consumer Users',
    company: 'Dropbox',
    companyDomain: 'dropbox.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'IS NOT NULL', 'referral program'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 4,
    datamartId: 'consumer',
    prompt: 'The growth team wants to measure referral program reach. Return user_id, username, device_os, and referrer_id for all users who joined via a referral (referrer_id IS NOT NULL), ordered by user_id.',
    expectedColumns: ['user_id', 'username', 'device_os', 'referrer_id'],
    expectedRowCount: 7,
    checkValues: [{ user_id: '2', username: 'bob', referrer_id: '1' }],
    solution: 'SELECT user_id, username, device_os, referrer_id\nFROM users\nWHERE referrer_id IS NOT NULL\nORDER BY user_id',
    debrief: '7 of 15 users (47%) were referred by another user. IS NOT NULL is the correct syntax for checking presence of a value — WHERE referrer_id != NULL returns zero rows. The referral chain extends multiple levels: user 2 referred user 7, who referred user 10, who referred user 12. Viral loops like this are worth visualizing as a tree to measure referral depth.',
    sqliteNote: null,
  },

  {
    id: 'sql-e55',
    title: 'Never-Active Saas Users',
    company: 'Intercom',
    companyDomain: 'intercom.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'IS NULL', 'user activation'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 4,
    datamartId: 'saas',
    prompt: 'The activation team wants to identify users who have never logged in (last_active_at IS NULL) to trigger an onboarding nudge. Return user_id, account_id, email, and role for all users with no recorded activity, ordered by user_id.',
    expectedColumns: ['user_id', 'account_id', 'email', 'role'],
    expectedRowCount: 2,
    checkValues: [{ user_id: '6', email: 'viewer2@retail.com', role: 'viewer' }],
    solution: 'SELECT user_id, account_id, email, role\nFROM users\nWHERE last_active_at IS NULL\nORDER BY user_id',
    debrief: 'Two users have never been active: viewer2@retail.com (account 2, viewer) and viewer4@delta.com (account 4, viewer). Both are viewers — the lowest-privilege role. This pattern is common: viewers are often added to satisfy team requirements but never actually use the product. A viewer-specific onboarding flow may improve activation for this segment.',
    sqliteNote: null,
  },

  {
    id: 'sql-e56',
    title: 'Transactions at Non-US Merchants',
    company: 'JPMorgan Chase',
    companyDomain: 'jpmorganchase.com',
    difficulty: 'Easy',
    tags: ['JOIN', 'WHERE', 'cross-border transactions'],
    roles: ['PA', 'DA'],
    priority: 2,
    estimatedMin: 5,
    datamartId: 'fintech',
    prompt: 'The compliance team wants to flag all transactions at non-US merchants for cross-border review. Return txn_id, amount, category, occurred_at, and the merchant name and country for all transactions where the merchant\'s country is not \'US\'.',
    expectedColumns: ['txn_id', 'amount', 'category', 'occurred_at', 'name', 'country'],
    expectedRowCount: 9,
    checkValues: [],
    solution: 'SELECT t.txn_id, t.amount, t.category, t.occurred_at, m.name, m.country\nFROM transactions t\nJOIN merchants m ON t.merchant_id = m.merchant_id\nWHERE m.country != \'US\'\nORDER BY t.occurred_at',
    debrief: 'Nine transactions occurred at non-US merchants: Shell Gas (UK), QuickTransfer (Nigeria ×2), CryptoXchange (China), FastCash Ltd (Brazil ×2), Spotify (Sweden ×3). The three flagged merchants (NG, CN, BR) account for 5 of the 9 cross-border transactions. Cross-border by itself is not fraud — Spotify (SE) is entirely legitimate — so flagging must combine geography with merchant risk score.',
    sqliteNote: null,
  },

  {
    id: 'sql-e57',
    title: 'Unordered Products',
    company: 'Shopify',
    companyDomain: 'shopify.com',
    difficulty: 'Easy',
    tags: ['anti-join', 'LEFT JOIN', 'dead inventory'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 5,
    datamartId: 'ecomm',
    prompt: 'The inventory team wants to find products that have never appeared in any order, to consider discontinuing them. Return product_id, name, category, and list_price for all products with no matching order_items rows.',
    expectedColumns: ['product_id', 'name', 'category', 'list_price'],
    expectedRowCount: 1,
    checkValues: [{ product_id: '10', name: 'Plant Pot', category: 'home' }],
    solution: 'SELECT p.product_id, p.name, p.category, p.list_price\nFROM products p\nLEFT JOIN order_items oi ON p.product_id = oi.product_id\nWHERE oi.item_id IS NULL',
    debrief: 'Only the Plant Pot (product 10) has never been ordered — and it is already marked inactive (is_active = 0). The LEFT JOIN + WHERE right IS NULL pattern is the standard anti-join: LEFT JOIN keeps all products, and the NULL check on oi.item_id selects those with no matching order. NOT IN and NOT EXISTS are alternatives, but LEFT JOIN IS NULL is the safest when the right-side column could itself be NULL.',
    sqliteNote: null,
  },

  {
    id: 'sql-e58',
    title: 'Accounts with Zero Events',
    company: 'Amplitude',
    companyDomain: 'amplitude.com',
    difficulty: 'Easy',
    tags: ['anti-join', 'LEFT JOIN', 'user activation'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 5,
    datamartId: 'saas',
    prompt: 'The CS team wants to identify accounts that have generated zero product events — a strong churn signal. Return account_id and company_name for all accounts with no rows in the events table.',
    expectedColumns: ['account_id', 'company_name'],
    expectedRowCount: 7,
    checkValues: [{ account_id: '9', company_name: 'India Foods' }],
    solution: 'SELECT a.account_id, a.company_name\nFROM accounts a\nLEFT JOIN events e ON a.account_id = e.account_id\nWHERE e.event_id IS NULL\nORDER BY a.account_id',
    debrief: 'Seven accounts have never generated a product event — including India Foods and Lima Education (both on the free Starter plan). These zero-event accounts are the highest churn risk: they are paying (or not paying) for a product they are not using. The LEFT JOIN IS NULL anti-join is the correct pattern; a WHERE account_id NOT IN (SELECT account_id FROM events) would work but is dangerous if events.account_id could be NULL.',
    sqliteNote: null,
  },

  {
    id: 'sql-e59',
    title: 'Inactive Consumer Users',
    company: 'Pinterest',
    companyDomain: 'pinterest.com',
    difficulty: 'Easy',
    tags: ['anti-join', 'LEFT JOIN', 'engagement gap'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 5,
    datamartId: 'consumer',
    prompt: 'The re-engagement team needs users who have never interacted with any content. Return user_id, username, device_os, and joined_at for all users with no rows in the interactions table.',
    expectedColumns: ['user_id', 'username', 'device_os', 'joined_at'],
    expectedRowCount: 2,
    checkValues: [{ user_id: '14', username: 'nina' }],
    solution: 'SELECT u.user_id, u.username, u.device_os, u.joined_at\nFROM users u\nLEFT JOIN interactions i ON u.user_id = i.user_id\nWHERE i.interaction_id IS NULL\nORDER BY u.user_id',
    debrief: 'Nina (user 14) and oscar (user 15) have never interacted with any content. Both joined late in 2023, are non-premium, and are US-based iOS/Android users. They may have downloaded the app but never engaged with content — classic activation gap candidates for a first-content push notification.',
    sqliteNote: null,
  },

  {
    id: 'sql-e60',
    title: 'Most Common Diagnoses',
    company: 'Epic Systems',
    companyDomain: 'epic.com',
    difficulty: 'Easy',
    tags: ['GROUP BY', 'COUNT', 'clinical analytics'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 4,
    datamartId: 'health',
    prompt: 'The clinical quality team wants to identify the most frequently diagnosed conditions. Return icd_code, description, and diagnosis_count, ordered by diagnosis_count descending.',
    expectedColumns: ['icd_code', 'description', 'diagnosis_count'],
    expectedRowCount: 10,
    checkValues: [{ icd_code: 'I10', description: 'Essential hypertension', diagnosis_count: '5' }],
    solution: 'SELECT icd_code, description, COUNT(*) AS diagnosis_count\nFROM diagnoses\nGROUP BY icd_code, description\nORDER BY diagnosis_count DESC',
    debrief: 'I10 (hypertension) leads with 5 diagnoses, followed by L70 (acne, 3), M54.5/L40/E11/J06.9 with 2 each. Including description in GROUP BY is correct — icd_code and description are functionally dependent (one code always maps to one description), so grouping on just icd_code would also work in this dataset, but grouping on both is more explicit and portable.',
    sqliteNote: null,
  },

  {
    id: 'sql-e61',
    title: 'Saas Users by Role',
    company: 'Asana',
    companyDomain: 'asana.com',
    difficulty: 'Easy',
    tags: ['GROUP BY', 'COUNT', 'user segmentation'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 4,
    datamartId: 'saas',
    prompt: 'The product team wants to understand the role distribution across all users to size admin vs. end-user features. Return role and user_count, ordered by user_count descending.',
    expectedColumns: ['role', 'user_count'],
    expectedRowCount: 3,
    checkValues: [{ role: 'admin', user_count: '10' }],
    solution: 'SELECT role, COUNT(*) AS user_count\nFROM users\nGROUP BY role\nORDER BY user_count DESC',
    debrief: 'Admins: 10, members: 13, viewers: 2. Wait — the saas users table has 25 rows: let me trace. Each account has at least one admin. Member-heavy accounts signal collaborative usage; viewer-only accounts signal read-only monitoring usage. The admin:member:viewer ratio informs whether to invest in collaboration features (members) or reporting features (viewers).',
    sqliteNote: null,
  },

  {
    id: 'sql-e62',
    title: 'Converted Sessions by Source',
    company: 'Google',
    companyDomain: 'google.com',
    difficulty: 'Easy',
    tags: ['GROUP BY', 'SUM', 'conversion analysis'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 5,
    datamartId: 'ecomm',
    prompt: 'The growth team wants to know how many sessions led to a purchase (converted = 1) for each traffic source. Return source, total_sessions, and converted_sessions, ordered by converted_sessions descending.',
    expectedColumns: ['source', 'total_sessions', 'converted_sessions'],
    expectedRowCount: 5,
    checkValues: [{ source: 'referral', total_sessions: '3', converted_sessions: '3' }],
    solution: 'SELECT source,\n  COUNT(*) AS total_sessions,\n  SUM(converted) AS converted_sessions\nFROM sessions\nGROUP BY source\nORDER BY converted_sessions DESC',
    debrief: 'Referral and paid both produced 5 converted sessions (though paid had 7 total vs referral\'s 3). Organic had 5 converts from 9 sessions. Email had 1 from 3. Social had 0 from 3. SUM(converted) works because converted is an integer flag (0 or 1) — summing integers gives a count of 1s. This is a common and elegant pattern for counting binary-flag events.',
    sqliteNote: null,
  },

  {
    id: 'sql-e64',
    title: 'Orders per Referral User',
    company: 'Lyft',
    companyDomain: 'lyft.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'GROUP BY', 'channel deep-dive'],
    roles: ['PA', 'DA'],
    priority: 2,
    estimatedMin: 5,
    datamartId: 'ecomm',
    prompt: 'The referral team wants to measure how active referral-channel customers are. Return user_id and order_count for all users who signed up via the referral channel (channel = \'referral\'), ordered by order_count descending.',
    expectedColumns: ['user_id', 'order_count'],
    expectedRowCount: 3,
    checkValues: [{ user_id: '3', order_count: '3' }],
    solution: 'SELECT u.user_id, COUNT(o.order_id) AS order_count\nFROM users u\nJOIN orders o ON u.user_id = o.user_id\nWHERE u.channel = \'referral\'\nGROUP BY u.user_id\nORDER BY order_count DESC',
    debrief: 'Referral users 3 (carol), 8 (hank), and 12 (leo) placed 3, 2, and 2 orders respectively. All referral users became buyers — a 100% conversion rate. To make this join-free, you could check channel in the users table directly, but INNER JOIN naturally excludes any referral users who placed zero orders (there are none here, but the pattern matters in larger datasets).',
    sqliteNote: null,
  },

  {
    id: 'sql-e65',
    title: 'Total MRR from Active Subscriptions',
    company: 'ChartMogul',
    companyDomain: 'chartmogul.com',
    difficulty: 'Easy',
    tags: ['SUM', 'aggregation', 'MRR'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 3,
    datamartId: 'saas',
    prompt: 'Finance needs our total MRR from active subscriptions for the board update. Single number.',
    expectedColumns: ['total_mrr'],
    expectedRowCount: 1,
    checkValues: [{ total_mrr: '11790.0' }],
    solution: 'SELECT SUM(mrr) AS total_mrr\nFROM subscriptions\nWHERE status = \'active\'',
    debrief: 'Total active MRR is $11,790: three Enterprise accounts contribute $8,997 (76%), six Growth accounts contribute $1,794, one Business account contributes $999, and two Starter accounts contribute $0. SUM(mrr) includes the free-plan $0 values — they do not distort the sum but would distort an AVG. Always filter to the status you care about before aggregating financial figures.',
    sqliteNote: null,
  },

  {
    id: 'sql-e66',
    title: 'Patient Gender Distribution',
    company: 'Kaiser Permanente',
    companyDomain: 'kaiserpermanente.org',
    difficulty: 'Easy',
    tags: ['GROUP BY', 'COUNT', 'demographic analysis'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 3,
    datamartId: 'health',
    prompt: 'The population health team needs the gender breakdown of our patient panel for HEDIS reporting. What does it look like?',
    expectedColumns: ['gender', 'patient_count'],
    expectedRowCount: 3,
    checkValues: [{ gender: 'F', patient_count: '7' }],
    solution: 'SELECT gender, COUNT(*) AS patient_count\nFROM patients\nGROUP BY gender\nORDER BY patient_count DESC',
    debrief: 'Female (F): 7, Male (M): 7, Other (O): 1. The panel is nearly balanced by gender. The single O-coded patient (patient 7) would be excluded from any analysis that filters gender IN (\'M\', \'F\') — a common oversight in clinical analytics. HEDIS measures should explicitly account for all gender categories in both numerator and denominator to avoid biased quality scores.',
    sqliteNote: null,
  },

  // ── EASY e67–e84 ─────────────────────────────────────────────────────────

  {
    id: 'sql-e67',
    title: 'Patients 50 or Older',
    company: 'Humana',
    companyDomain: 'humana.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'date arithmetic', 'age filter'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 5,
    datamartId: 'health',
    prompt: 'The preventive care team wants to flag patients aged 50 or older (born before January 1, 1976) for senior screenings. Return patient_id, dob, gender, and zip_code for qualifying patients, ordered by dob ascending (oldest first).',
    expectedColumns: ['patient_id', 'dob', 'gender', 'zip_code'],
    expectedRowCount: 6,
    checkValues: [{ patient_id: '9', dob: '1943-08-07', gender: 'M', zip_code: '10005' }],
    solution: 'SELECT patient_id, dob, gender, zip_code\nFROM patients\nWHERE dob < \'1976-01-01\'\nORDER BY dob',
    debrief: 'Six patients qualify: the oldest is patient 9 (born 1943, age 83). The WHERE dob < \'1976-01-01\' pattern uses the date string comparison that SQLite supports natively for ISO-format dates (YYYY-MM-DD). Using strftime(\'%Y\', \'now\') - strftime(\'%Y\', dob) >= 50 also works but is not index-friendly. A fixed date cutoff in the WHERE clause is faster and reproducible.',
    sqliteNote: null,
  },

  {
    id: 'sql-e68',
    title: 'Content by Premium Creators',
    company: 'Patreon',
    companyDomain: 'patreon.com',
    difficulty: 'Easy',
    tags: ['INNER JOIN', 'filter', 'creator analytics'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 5,
    datamartId: 'consumer',
    prompt: 'The creator monetization team wants to audit all content published by premium users. Return content_id, creator_id, content_type, category, and published_at for all content where the creator\'s is_premium = 1, ordered by published_at.',
    expectedColumns: ['content_id', 'creator_id', 'content_type', 'category', 'published_at'],
    expectedRowCount: 7,
    checkValues: [{ content_id: '1', creator_id: '1', content_type: 'video', category: 'fitness' }],
    solution: 'SELECT c.content_id, c.creator_id, c.content_type, c.category, c.published_at\nFROM content c\nJOIN users u ON c.creator_id = u.user_id\nWHERE u.is_premium = 1\nORDER BY c.published_at',
    debrief: 'Seven of eight content items were created by premium users. Only content 7 (user 8 henry, non-premium) is excluded. The JOIN is required because is_premium lives in users, not content. This is a pattern analysts encounter constantly: the attribute you need to filter on is in a different table from the rows you want to return.',
    sqliteNote: null,
  },

  {
    id: 'sql-e69',
    title: 'Large-Value Orders',
    company: 'Nordstrom',
    companyDomain: 'nordstrom.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'range filter', 'revenue analysis'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 3,
    datamartId: 'ecomm',
    prompt: 'The high-value customer team wants to analyze orders above $100 in subtotal. Return order_id, user_id, created_at, subtotal, and status for all orders where subtotal > 100, ordered by subtotal descending.',
    expectedColumns: ['order_id', 'user_id', 'created_at', 'subtotal', 'status'],
    expectedRowCount: 7,
    checkValues: [{ order_id: '4', subtotal: '249.99' }],
    solution: 'SELECT order_id, user_id, created_at, subtotal, status\nFROM orders\nWHERE subtotal > 100\nORDER BY subtotal DESC',
    debrief: 'Seven orders exceed $100: four at $249.99 (Smart Watch orders), two at $159.99 (Winter Jacket), one at $249.99 (another Smart Watch). One of the four $249.99 orders is cancelled (order 4) — revenue metrics should filter to status = \'completed\' before summing. Gross order value and net recognized revenue are different numbers.',
    sqliteNote: null,
  },

  {
    id: 'sql-e70',
    title: 'Accounts by Currency',
    company: 'Revolut',
    companyDomain: 'revolut.com',
    difficulty: 'Easy',
    tags: ['GROUP BY', 'COUNT', 'multi-currency'],
    roles: ['PA', 'DA'],
    priority: 2,
    estimatedMin: 4,
    datamartId: 'fintech',
    prompt: 'The FX team needs to know how many accounts we have per currency for hedging analysis. Give me that breakdown.',
    expectedColumns: ['currency', 'account_count'],
    expectedRowCount: 3,
    checkValues: [{ currency: 'USD', account_count: '12' }],
    solution: 'SELECT currency, COUNT(*) AS account_count\nFROM accounts\nGROUP BY currency\nORDER BY account_count DESC',
    debrief: 'USD: 12 accounts (80%), GBP: 2 (13%), AUD: 1 (7%). USD dominance means minimal FX exposure for now — but the user base spans 7 countries. As the product expands internationally, GBP and AUD accounts will likely grow, and FX hedging cost becomes material. Pairing this with SUM(balance) by currency gives the actual exposure, not just account count.',
    sqliteNote: null,
  },

  {
    id: 'sql-e71',
    title: 'Product Order Frequency',
    company: 'Amazon',
    companyDomain: 'amazon.com',
    difficulty: 'Easy',
    tags: ['GROUP BY', 'COUNT', 'product analytics'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 4,
    datamartId: 'ecomm',
    prompt: 'The category team wants to rank products by how often they appear in orders. Return product_id and times_ordered (count of order_items rows), ordered by times_ordered descending.',
    expectedColumns: ['product_id', 'times_ordered'],
    expectedRowCount: 9,
    checkValues: [{ product_id: '1', times_ordered: '6' }],
    solution: 'SELECT product_id, COUNT(*) AS times_ordered\nFROM order_items\nGROUP BY product_id\nORDER BY times_ordered DESC',
    debrief: 'Wireless Headphones (product 1) tops with 6 orders, followed by Smart Watch (4) and Running Shoes (4). Product 10 (Plant Pot) has zero — it never appears in order_items at all, so GROUP BY will not produce a row for it. This is the GROUP BY visibility gap: products with zero sales are invisible. To surface them, LEFT JOIN from products to order_items.',
    sqliteNote: null,
  },

  {
    id: 'sql-e72',
    title: 'Appointments Without Diagnoses',
    company: 'Athenahealth',
    companyDomain: 'athenahealth.com',
    difficulty: 'Easy',
    tags: ['anti-join', 'LEFT JOIN', 'data quality'],
    roles: ['PA', 'DA'],
    priority: 2,
    estimatedMin: 5,
    datamartId: 'health',
    prompt: 'The data quality team wants to find all appointments where no diagnosis was recorded — either due to a no-show or a documentation gap. Return appt_id, patient_id, provider_id, scheduled_at, and no_show.',
    expectedColumns: ['appt_id', 'patient_id', 'provider_id', 'scheduled_at', 'no_show'],
    expectedRowCount: 7,
    checkValues: [{ appt_id: '3', patient_id: '3', no_show: '1' }],
    solution: 'SELECT a.appt_id, a.patient_id, a.provider_id, a.scheduled_at, a.no_show\nFROM appointments a\nLEFT JOIN diagnoses d ON a.appt_id = d.appt_id\nWHERE d.diagnosis_id IS NULL\nORDER BY a.appt_id',
    debrief: 'Seven appointments have no recorded diagnosis. Six of them are no-shows (no_show = 1) — expected, since a patient who did not attend cannot receive a diagnosis. Appointment 23 (patient 11, provider 6, telehealth, no no-show) is the outlier — a completed visit with no diagnosis code, a genuine documentation gap that compliance would flag.',
    sqliteNote: null,
  },

  {
    id: 'sql-e73',
    title: 'H1 2023 Account Cohort',
    company: 'Salesforce',
    companyDomain: 'salesforce.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'date range', 'cohort'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 4,
    datamartId: 'saas',
    prompt: 'The cohort analysis team wants all accounts that signed up in the first half of 2023 (January through June). Return account_id, company_name, industry, and created_at for qualifying accounts, ordered by created_at.',
    expectedColumns: ['account_id', 'company_name', 'industry', 'created_at'],
    expectedRowCount: 11,
    checkValues: [{ account_id: '1', company_name: 'Acme Corp', industry: 'tech' }],
    solution: 'SELECT account_id, company_name, industry, created_at\nFROM accounts\nWHERE created_at >= \'2023-01-01\' AND created_at < \'2023-07-01\'\nORDER BY created_at',
    debrief: 'Eleven accounts joined in H1 2023 (January through June). Account 12 (Lima Education, created_at = \'2023-07-01\') is excluded — the condition created_at < \'2023-07-01\' correctly handles the boundary. This H1 cohort would be the first group to hit their one-year anniversary and the natural target for annual renewal analysis.',
    sqliteNote: null,
  },

  {
    id: 'sql-e74',
    title: 'Interactions per Content Item',
    company: 'TikTok',
    companyDomain: 'tiktok.com',
    difficulty: 'Easy',
    tags: ['GROUP BY', 'COUNT', 'content performance'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 4,
    datamartId: 'consumer',
    prompt: 'The content team wants to rank all content pieces by total interaction count. Return content_id and interaction_count, ordered by interaction_count descending.',
    expectedColumns: ['content_id', 'interaction_count'],
    expectedRowCount: 8,
    checkValues: [{ content_id: '1', interaction_count: '8' }],
    solution: 'SELECT content_id, COUNT(*) AS interaction_count\nFROM interactions\nGROUP BY content_id\nORDER BY interaction_count DESC',
    debrief: 'Content 1 (fitness video) leads with 8 interactions. Content 3 has 5. Content 7 and 8 each have just 1. The top piece outperforms the bottom two by 8×. On a platform, the distribution of engagement is almost always highly skewed — a small number of pieces drive most of the engagement. Identifying what makes content 1 different (recency? category? creator premium status?) is the key product question.',
    sqliteNote: null,
  },

  {
    id: 'sql-e77',
    title: 'Diagnoses per Provider',
    company: 'Doximity',
    companyDomain: 'doximity.com',
    difficulty: 'Easy',
    tags: ['JOIN', 'GROUP BY', 'COUNT', 'clinical productivity'],
    roles: ['PA', 'DA'],
    priority: 2,
    estimatedMin: 5,
    datamartId: 'health',
    prompt: 'The quality team wants to understand diagnostic output per provider. Return provider_id and diagnosis_count for each provider, ordered by diagnosis_count descending.',
    expectedColumns: ['provider_id', 'diagnosis_count'],
    expectedRowCount: 6,
    checkValues: [{ provider_id: '1', diagnosis_count: '7' }],
    solution: 'SELECT a.provider_id, COUNT(d.diagnosis_id) AS diagnosis_count\nFROM appointments a\nJOIN diagnoses d ON a.appt_id = d.appt_id\nGROUP BY a.provider_id\nORDER BY diagnosis_count DESC',
    debrief: 'Dr. Smith (provider 1) has 7 diagnoses, followed by Dr. Patel and Dr. Lee with 4 each. The JOIN from appointments to diagnoses is needed because diagnoses only stores appt_id — provider_id must be retrieved through the appointment. Note that six no-show appointments produce no diagnoses, so appointment count (10 for provider 1) differs from diagnosis count (7).',
    sqliteNote: null,
  },

  {
    id: 'sql-e78',
    title: 'Revenue by Acquisition Channel',
    company: 'Klaviyo',
    companyDomain: 'klaviyo.com',
    difficulty: 'Easy',
    tags: ['JOIN', 'GROUP BY', 'SUM', 'channel attribution'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 6,
    datamartId: 'ecomm',
    prompt: 'Growth wants to attribute order revenue to the channel that acquired each customer. Which channel is driving the most revenue?',
    expectedColumns: ['channel', 'total_revenue'],
    expectedRowCount: 4,
    checkValues: [{ channel: 'paid', total_revenue: '1284.89' }],
    solution: 'SELECT u.channel, SUM(o.subtotal) AS total_revenue\nFROM users u\nJOIN orders o ON u.user_id = o.user_id\nGROUP BY u.channel\nORDER BY total_revenue DESC',
    debrief: 'Paid ($1,284.89) leads, followed by organic ($764.92), referral ($544.93), and email ($249.98). Channels with users who placed zero orders (e.g. organic users 13 and 15) are included via their channel peers — the JOIN from users to orders naturally excludes non-buyers. Important: this is gross subtotal, not net revenue — subtracting discounts and shipping would change the ranking.',
    sqliteNote: null,
  },

  {
    id: 'sql-e80',
    title: 'International Users',
    company: 'Shopify',
    companyDomain: 'shopify.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'NOT IN', 'geographic filter'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 4,
    datamartId: 'ecomm',
    prompt: 'The international expansion team wants to focus on non-US, non-UK users for a localization push. Return user_id, email, country, and channel for all users not in the US or UK, ordered by country, user_id.',
    expectedColumns: ['user_id', 'email', 'country', 'channel'],
    expectedRowCount: 5,
    checkValues: [{ user_id: '4', country: 'CA' }],
    solution: 'SELECT user_id, email, country, channel\nFROM users\nWHERE country NOT IN (\'US\', \'UK\')\nORDER BY country, user_id',
    debrief: 'Five users are in non-US/UK markets: two in CA, two in AU, one in SG. NOT IN works cleanly here because country is always populated (no NULLs). Beware: NOT IN with a subquery that could return NULLs will silently return zero rows — NULL IN (\'US\', NULL) evaluates to unknown, and NOT UNKNOWN is still unknown. When using NOT IN with subqueries, add WHERE column IS NOT NULL to be safe.',
    sqliteNote: null,
  },

  {
    id: 'sql-e81',
    title: 'Total Disputed Exposure',
    company: 'Stripe',
    companyDomain: 'stripe.com',
    difficulty: 'Easy',
    tags: ['SUM', 'aggregation', 'financial exposure'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 3,
    datamartId: 'fintech',
    prompt: 'Compliance is asking about our total dollar exposure from disputed transactions. What\'s the number?',
    expectedColumns: ['total_disputed'],
    expectedRowCount: 1,
    checkValues: [{ total_disputed: '5689.99' }],
    solution: 'SELECT SUM(amount) AS total_disputed\nFROM transactions\nWHERE status = \'disputed\'',
    debrief: 'Total disputed exposure is $5,689.99 across six transactions. The $3,500 from txn 31 (QuickTransfer) represents 61.5% of total exposure. SUM(amount) on a filtered dataset is a one-liner — but the next question is always how much of that has been resolved vs. is still open. Joining to the disputes table and filtering by resolved_at IS NOT NULL would give the unresolved portion.',
    sqliteNote: null,
  },

  {
    id: 'sql-e82',
    title: 'Enterprise Active Subscriptions',
    company: 'Zuora',
    companyDomain: 'zuora.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'JOIN', 'enterprise filter'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 5,
    datamartId: 'saas',
    prompt: 'The enterprise CS team needs a list of all active subscriptions with MRR above $1,000. Return sub_id, account_id, mrr, and started_at, ordered by mrr descending.',
    expectedColumns: ['sub_id', 'account_id', 'mrr', 'started_at'],
    expectedRowCount: 3,
    checkValues: [{ sub_id: '6', account_id: '5', mrr: '2999.0' }],
    solution: 'SELECT sub_id, account_id, mrr, started_at\nFROM subscriptions\nWHERE status = \'active\' AND mrr > 1000\nORDER BY mrr DESC',
    debrief: 'Three Enterprise subscriptions exceed $1,000 MRR: accounts 5 ($2,999), 11 ($2,999), and 1 ($2,999 — the upgraded sub from Jan 2024). All three are on the Enterprise plan. The Business plan sub (account 7, $999) is just under the $1,000 threshold — a natural upsell target. Two conditions in the WHERE are both required: status = \'active\' excludes the churned Business sub from account 1.',
    sqliteNote: null,
  },

  {
    id: 'sql-e83',
    title: 'High-Refill Prescriptions',
    company: 'Optum',
    companyDomain: 'optum.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'range filter', 'chronic care'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 3,
    datamartId: 'health',
    prompt: 'The pharmacy team defines a chronic-condition prescription as one with 3 or more authorized refills. Return rx_id, patient_id, drug_name, and refills for all prescriptions where refills >= 3, ordered by refills descending.',
    expectedColumns: ['rx_id', 'patient_id', 'drug_name', 'refills'],
    expectedRowCount: 8,
    checkValues: [{ rx_id: '16', drug_name: 'Lisinopril', refills: '6' }],
    solution: 'SELECT rx_id, patient_id, drug_name, refills\nFROM prescriptions\nWHERE refills >= 3\nORDER BY refills DESC',
    debrief: 'Eight prescriptions qualify as chronic (3+ refills). Lisinopril appears three times in the top results — consistent with it being the most prescribed drug overall. The prescription with 6 refills (rx 16) is the longest-running: the patient is authorized for over 7 months of continuous therapy. Tracking refill adherence (whether the patient actually picks up each refill) is the next layer of clinical insight.',
    sqliteNote: null,
  },

  {
    id: 'sql-e84',
    title: 'Video Content Catalog',
    company: 'Netflix',
    companyDomain: 'netflix.com',
    difficulty: 'Easy',
    tags: ['WHERE', 'content type filter'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 3,
    datamartId: 'consumer',
    prompt: 'The video team needs to audit only video content for a compression algorithm test. Return content_id, creator_id, category, published_at, and duration_sec for all videos (content_type = \'video\'), ordered by duration_sec descending.',
    expectedColumns: ['content_id', 'creator_id', 'category', 'published_at', 'duration_sec'],
    expectedRowCount: 4,
    checkValues: [{ content_id: '5', duration_sec: '3600', category: 'tech' }],
    solution: 'SELECT content_id, creator_id, category, published_at, duration_sec\nFROM content\nWHERE content_type = \'video\'\nORDER BY duration_sec DESC',
    debrief: 'Four videos in the catalog. The tech video (content 5, 60 minutes) is the longest; the fitness video (content 1, 30 minutes) is next. Articles and stories have NULL duration_sec — filtering to content_type = \'video\' correctly excludes them. If this query were run across all content types and duration_sec were returned for articles (NULL), any AVG(duration_sec) would silently exclude those NULLs rather than treating them as zero.',
    sqliteNote: null,
  },

  {
    id: 'sql-e85',
    title: 'Order Status Mix',
    company: 'Amazon',
    companyDomain: 'amazon.com',
    difficulty: 'Easy',
    tags: ['CASE WHEN', 'GROUP BY'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 5,
    datamartId: 'ecomm',
    prompt: 'The finance team is reconciling order records by outcome. Using CASE WHEN, map status \'completed\' to \'Fulfilled\', \'returned\' to \'Reversed\', and everything else to \'Cancelled\'. Return order_status and order_count, ordered by order_count descending.',
    expectedColumns: ['order_status', 'order_count'],
    expectedRowCount: 3,
    checkValues: [{ order_status: 'Fulfilled', order_count: '23' }],
    solution: 'SELECT\n  CASE WHEN status = \'completed\' THEN \'Fulfilled\'\n       WHEN status = \'returned\' THEN \'Reversed\'\n       ELSE \'Cancelled\' END AS order_status,\n  COUNT(*) AS order_count\nFROM orders\nGROUP BY status\nORDER BY order_count DESC',
    debrief: 'Fulfilled orders dominate at 23 — an 82% fulfillment rate. Three returns and two cancellations together represent an 18% order failure rate worth monitoring. CASE WHEN on GROUP BY works because SQLite groups by the underlying column (status), then aliases the output. A common mistake is to GROUP BY the alias — that fails in most SQL dialects. Always GROUP BY the raw column, alias only in SELECT.',
    sqliteNote: null,
  },

  {
    id: 'sql-e97',
    title: 'Orders Per User',
    company: 'Amazon',
    companyDomain: 'amazon.com',
    difficulty: 'Easy',
    tags: ['INNER JOIN', 'GROUP BY', 'COUNT'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 5,
    datamartId: 'ecomm',
    prompt: 'The CRM team wants to rank users by purchase activity. Join the users table to orders, count orders per user, and return user_id and order_count for all users who have placed at least one order. Order by order_count descending.',
    expectedColumns: ['user_id', 'order_count'],
    expectedRowCount: 12,
    checkValues: [{ user_id: '5', order_count: '5' }],
    solution: 'SELECT u.user_id, COUNT(o.order_id) AS order_count\nFROM users u\nINNER JOIN orders o ON u.user_id = o.user_id\nGROUP BY u.user_id\nORDER BY order_count DESC',
    debrief: 'Twelve of 15 users have placed at least one order — users 13, 14, and 15 have zero orders and are excluded by the INNER JOIN. User 5 leads with 5 orders. This ranking is purchase frequency, not purchase value — a high-frequency buyer of low-AOV items may be less valuable than an infrequent buyer of high-AOV items. For a fuller picture, add SUM(o.subtotal) alongside COUNT to surface both dimensions.',
    sqliteNote: null,
  },

  {
    id: 'sql-e100',
    title: 'Top Content by Interactions',
    company: 'TikTok',
    companyDomain: 'tiktok.com',
    difficulty: 'Easy',
    tags: ['GROUP BY', 'COUNT', 'LIMIT'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 4,
    datamartId: 'consumer',
    prompt: 'The recommendation team wants to identify the most engaging content pieces. Return content_id and interaction_count by counting rows in the interactions table, grouped by content_id. Return the top 5 by interaction_count descending.',
    expectedColumns: ['content_id', 'interaction_count'],
    expectedRowCount: 5,
    checkValues: [{ content_id: '1', interaction_count: '8' }],
    solution: 'SELECT content_id, COUNT(*) AS interaction_count\nFROM interactions\nGROUP BY content_id\nORDER BY interaction_count DESC\nLIMIT 5',
    debrief: 'Content 1 leads with 8 interactions — significantly ahead of other pieces. Interaction count is a volume metric, not a quality signal: it counts likes, shares, comments, and views equally unless you filter by interaction_type. For recommendation systems, weighted engagement scores (share = 3x, comment = 2x, like = 1x, view = 0.5x) better reflect content value than raw counts. This query is the first step in identifying candidates for the recommendation pool.',
    sqliteNote: null,
  },

  // ─── HARD h25–h42 ────────────────────────────────────────────────────────

  {
    id: 'sql-h25',
    title: 'Running Revenue Per User',
    company: 'Amazon',
    companyDomain: 'amazon.com',
    difficulty: 'Medium',
    tags: ['SUM OVER', 'window function', 'running total', 'PARTITION BY'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 14,
    datamartId: 'ecomm',
    prompt: 'Finance wants to track how each customer\'s spending has built up order by order. For every order, show how much that customer has spent in total up to that point.',
    expectedColumns: ['user_id', 'order_id', 'created_at', 'subtotal', 'running_total'],
    expectedRowCount: 28,
    checkValues: [],
    solution: 'SELECT user_id, order_id, created_at, subtotal,\n       SUM(subtotal) OVER (PARTITION BY user_id ORDER BY created_at) AS running_total\nFROM orders\nORDER BY user_id, created_at',
    debrief: 'What the stakeholder wants: A per-order running total of each customer\'s cumulative spend, so Finance can see the revenue trajectory per customer and model LTV curves.\n\nAmbiguities resolved: Should cancelled or returned orders count toward cumulative revenue? Including all statuses makes the running total volatile. A WHERE status = \'completed\' filter gives a cleaner LTV signal and should be discussed with the stakeholder. \'Running total\' means cumulative up to and including the current order sorted by date — not a full-partition sum on every row.\n\nSQL approach: SUM(subtotal) OVER (PARTITION BY user_id ORDER BY created_at) computes a cumulative sum within each user\'s order history. Adding ORDER BY to the OVER changes the default frame from full-partition to rows-preceding-through-current, making it a running sum rather than a total.\n\nWhat weak SQL looks like: Omitting ORDER BY inside the OVER — SUM(subtotal) OVER (PARTITION BY user_id) gives the total spend for that user on every row (a constant), not a running sum. The interviewer will catch this when row 1 and row 5 for the same user show the same number.\n\nInterviewer follow-up: \'How would you identify customers whose running total crossed $500 for the first time?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-h26',
    title: 'Subscription Status by Plan',
    company: 'Salesforce',
    companyDomain: 'salesforce.com',
    difficulty: 'Medium',
    tags: ['JOIN', 'GROUP BY', 'SUM', 'plan analysis', 'churn'],
    roles: ['PA', 'DA', 'PM', 'BA'],
    priority: 1,
    estimatedMin: 14,
    datamartId: 'saas',
    prompt: 'The revenue team needs a breakdown of subscriptions by plan tier and status to quantify churned vs retained MRR. Join subscriptions to plans and return plan name, status, count of subscriptions, and total MRR for each plan-status combination. Order by plan name, then status.',
    expectedColumns: ['plan_name', 'status', 'sub_count', 'total_mrr'],
    expectedRowCount: 6,
    checkValues: [{ plan_name: 'Enterprise', status: 'active', sub_count: '3' }],
    solution: 'SELECT p.name AS plan_name, s.status,\n       COUNT(*) AS sub_count,\n       SUM(s.mrr) AS total_mrr\nFROM subscriptions s\nJOIN plans p ON s.plan_id = p.plan_id\nGROUP BY p.name, s.status\nORDER BY p.name, s.status',
    debrief: 'Six plan-status combinations: Business/active(1 sub), Business/churned(3), Enterprise/active(3), Growth/active(6), Growth/churned(2), Starter/active(2). Enterprise has zero churn — all three Enterprise accounts remain active. Growth has the most churned subscriptions. The 3 churned Business subs represent $2,997 in lost MRR. GROUP BY both plan name and status produces the cross-tab; failing to include status in GROUP BY would collapse active and churned rows into one.',
    sqliteNote: null,
  },

  {
    id: 'sql-h27',
    title: 'Account Balance Quartiles',
    company: 'JPMorgan Chase',
    companyDomain: 'jpmorganchase.com',
    difficulty: 'Medium',
    tags: ['NTILE', 'window function', 'balance distribution', 'segmentation'],
    roles: ['PA', 'DA', 'BA'],
    priority: 2,
    estimatedMin: 13,
    datamartId: 'fintech',
    prompt: 'The wealth management team wants to segment accounts into four equal-size tiers by balance so advisors know which accounts to prioritize. Can you assign each account to a quartile?',
    expectedColumns: ['account_id', 'user_id', 'balance', 'currency', 'quartile'],
    expectedRowCount: 15,
    checkValues: [{ account_id: '5', quartile: '1' }],
    solution: 'SELECT account_id, user_id, balance, currency,\n       NTILE(4) OVER (ORDER BY balance DESC) AS quartile\nFROM accounts\nORDER BY quartile, balance DESC',
    debrief: 'What the stakeholder wants: Every account labeled with a quartile (1 through 4) based on balance, so the team can route Q1 accounts to dedicated relationship managers and Q4 accounts to self-service.\n\nAmbiguities resolved: Which quartile gets which label is a business decision — here Q1 = highest balance (priority clients), Q4 = lowest. This must be stated explicitly. NTILE(4) creates four equal-size groups, distributing remainder rows to the first groups (15 accounts produces groups of 4, 4, 4, 3).\n\nSQL approach: NTILE(4) OVER (ORDER BY balance DESC) assigns 1 to the highest-balance accounts first. No PARTITION BY — this ranks across all accounts. ORDER BY quartile, balance DESC in the outer query orders output for display.\n\nWhat weak SQL looks like: NTILE(4) OVER (ORDER BY balance ASC) — this assigns Q1 to the lowest-balance accounts, inverting the intended priority. A candidate who doesn\'t explicitly state sort direction is leaving a material business decision implicit. NTILE also doesn\'t respect natural value gaps — a $25,000 account and a $9,300 account can land in the same quartile based purely on row count.\n\nInterviewer follow-up: \'How would you re-segment if the team wants quartile boundaries based on fixed dollar thresholds instead of equal row counts?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-h28',
    title: 'Content Engagement Pivot',
    company: 'TikTok',
    companyDomain: 'tiktok.com',
    difficulty: 'Medium',
    tags: ['CASE WHEN', 'pivot', 'GROUP BY', 'engagement mix'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 15,
    datamartId: 'consumer',
    prompt: 'The content team wants to see the engagement mix for every piece of content — how many views, likes, shares, saves, and comments each has gotten. One row per content item.',
    expectedColumns: ['content_id', 'views', 'likes', 'shares', 'saves', 'comments'],
    expectedRowCount: 8,
    checkValues: [{ content_id: '1', views: '3', likes: '2', shares: '1', saves: '1', comments: '1' }],
    solution: 'SELECT content_id,\n       SUM(CASE WHEN action = \'view\' THEN 1 ELSE 0 END) AS views,\n       SUM(CASE WHEN action = \'like\' THEN 1 ELSE 0 END) AS likes,\n       SUM(CASE WHEN action = \'share\' THEN 1 ELSE 0 END) AS shares,\n       SUM(CASE WHEN action = \'save\' THEN 1 ELSE 0 END) AS saves,\n       SUM(CASE WHEN action = \'comment\' THEN 1 ELSE 0 END) AS comments\nFROM interactions\nGROUP BY content_id\nORDER BY content_id',
    debrief: 'What the stakeholder wants: A wide-format table with one row per content item and a separate column for each action type count, so the team can spot which content drives high shares vs. high views and tune the recommendation algorithm accordingly.\n\nAmbiguities resolved: Should content with zero interactions appear? The current approach groups FROM interactions — this naturally excludes content never interacted with. If the team wants a complete content inventory, a LEFT JOIN from content to interactions is needed. \'Engagement mix\' means raw counts per action type, not weighted scores.\n\nSQL approach: SUM(CASE WHEN action = \'view\' THEN 1 ELSE 0 END) conditionally counts rows matching each action type. One SUM CASE WHEN per column. GROUP BY content_id produces one row per content item. This is the standard SQL pivot pattern.\n\nWhat weak SQL looks like: Five separate subqueries or JOINs, one per action type — verbose, slower on large tables, and harder to extend with new action types. Or using GROUP BY action without pivoting — this gives a long format with multiple rows per content item, not the one-row-per-content layout the stakeholder asked for.\n\nInterviewer follow-up: \'How would you add a weighted engagement score column — shares at 3x, comments at 2x, likes at 1x, views at 0.5x?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-h29',
    title: 'Provider No-Show Rate',
    company: 'Doximity',
    companyDomain: 'doximity.com',
    difficulty: 'Medium',
    tags: ['GROUP BY', 'SUM', 'ROUND', 'rate calculation', 'healthcare ops'],
    roles: ['PA', 'DA', 'BA'],
    priority: 1,
    estimatedMin: 12,
    datamartId: 'health',
    prompt: 'The operations director needs a no-show rate report for each provider. For each provider_id, compute total_appts, no_shows (sum of the no_show flag), and no_show_rate as a percentage rounded to 1 decimal. Order by no_show_rate descending, then total_appts descending.',
    expectedColumns: ['provider_id', 'total_appts', 'no_shows', 'no_show_rate'],
    expectedRowCount: 6,
    checkValues: [{ provider_id: '1', total_appts: '10', no_shows: '4', no_show_rate: '40.0' }],
    solution: 'SELECT provider_id,\n       COUNT(*) AS total_appts,\n       SUM(no_show) AS no_shows,\n       ROUND(100.0 * SUM(no_show) / COUNT(*), 1) AS no_show_rate\nFROM appointments\nGROUP BY provider_id\nORDER BY no_show_rate DESC, total_appts DESC',
    debrief: 'Dr. Smith (provider 1) has the highest no-show rate at 40% (4 of 10 appointments). Dr. Johnson (provider 5) is at 33.3%. Providers 3, 4, and 6 have perfect attendance. The formula requires 100.0 — not 100 — to force float division in SQLite; integer division returns all zeros. SUM(no_show) works because the column is a binary integer (0 or 1) — it is equivalent to COUNT(CASE WHEN no_show = 1 THEN 1 END). The interviewer follow-up: does visit_type correlate with no-show rate? Telehealth patients often no-show more because the friction of not attending is lower.',
    sqliteNote: null,
  },

  {
    id: 'sql-h30',
    title: 'Discount Impact on Order Value',
    company: 'Amazon',
    companyDomain: 'amazon.com',
    difficulty: 'Medium',
    tags: ['CASE WHEN', 'GROUP BY', 'AVG', 'SUM', 'discount analysis'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 13,
    datamartId: 'ecomm',
    prompt: 'The promotions team wants to know whether our discounts are actually driving bigger orders, or just giving away margin. Can you compare discounted vs full-price orders?',
    expectedColumns: ['price_type', 'order_count', 'avg_subtotal', 'total_discount_given'],
    expectedRowCount: 2,
    checkValues: [],
    solution: 'SELECT\n  CASE WHEN discount > 0 THEN \'discounted\' ELSE \'full_price\' END AS price_type,\n  COUNT(*) AS order_count,\n  ROUND(AVG(subtotal), 2) AS avg_subtotal,\n  ROUND(SUM(discount), 2) AS total_discount_given\nFROM orders\nGROUP BY price_type\nORDER BY price_type',
    debrief: 'What the stakeholder wants: A two-row comparison of discounted vs full-price orders showing order count, average order value, and total discount given — enough to make a first-pass ROI call on the promotions program.\n\nAmbiguities resolved: \'Discounted\' means any order with discount > 0; full-price means discount = 0. This binary classification loses nuance — an order with a $0.01 discount looks identical to one with a $100 discount. The candidate should flag this and suggest a follow-up with discount size ranges. Whether to include all order statuses or only completed orders is another decision to surface.\n\nSQL approach: CASE WHEN discount > 0 THEN \'discounted\' ELSE \'full_price\' END creates the price_type label. GROUP BY this derived label. AVG(subtotal) gives average order value per group. SUM(discount) totals discount exposure. ROUND to 2 decimals for financial display.\n\nWhat weak SQL looks like: Running two separate queries (WHERE discount > 0 and WHERE discount = 0) and comparing manually — not a single result set and doesn\'t scale. Or AVG(discount) instead of SUM(discount) — the team wants total exposure, not average discount per order.\n\nInterviewer follow-up: \'If discounted orders show higher average subtotal, how would you test whether the discount caused higher spend or whether high-spenders are simply more likely to use coupons?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-h31',
    title: 'CSM Portfolio MRR',
    company: 'Salesforce',
    companyDomain: 'salesforce.com',
    difficulty: 'Hard',
    tags: ['LEFT JOIN', 'GROUP BY', 'CASE WHEN', 'SUM', 'COALESCE', 'csm workload'],
    roles: ['PA', 'DA', 'BA'],
    priority: 2,
    estimatedMin: 15,
    datamartId: 'saas',
    prompt: 'Customer success leadership wants to balance CSM workloads. For each csm_id, compute account_count and active_mrr (total MRR from active subscriptions only). Use a LEFT JOIN to subscriptions so every CSM appears even if their accounts have no active subscriptions. Order by active_mrr descending.',
    expectedColumns: ['csm_id', 'account_count', 'active_mrr'],
    expectedRowCount: 3,
    checkValues: [{ csm_id: '2', account_count: '5' }],
    solution: 'SELECT a.csm_id,\n       COUNT(DISTINCT a.account_id) AS account_count,\n       COALESCE(SUM(CASE WHEN s.status = \'active\' THEN s.mrr ELSE 0 END), 0) AS active_mrr\nFROM accounts a\nLEFT JOIN subscriptions s ON a.account_id = s.account_id\nGROUP BY a.csm_id\nORDER BY active_mrr DESC',
    debrief: 'CSM 2 manages 5 accounts with $6,596 in active MRR. CSM 1 manages 6 accounts but only $3,597 active MRR — two of their accounts (Lima Education, India Foods) are on the free Starter plan ($0 MRR). CSM 3 covers 4 accounts at $1,597. The CASE WHEN inside SUM filters to active subs without a WHERE clause that would drop churned-account rows from the GROUP BY. COUNT(DISTINCT account_id) prevents inflated counts from accounts with multiple subscription rows. The implication: CSM 1 has the largest headcount but the second-smallest MRR — an argument for rebalancing portfolios by MRR rather than account count.',
    sqliteNote: null,
  },

  {
    id: 'sql-h32',
    title: 'Disputed Transaction Merchant Exposure',
    company: 'Stripe',
    companyDomain: 'stripe.com',
    difficulty: 'Hard',
    tags: ['CTE', 'JOIN', 'WHERE filter', 'fraud', 'GROUP BY'],
    roles: ['PA', 'DA', 'BA'],
    priority: 1,
    estimatedMin: 16,
    datamartId: 'fintech',
    prompt: 'The fraud team wants to know which merchants are generating the most disputed transactions. Can you show disputed volume and count per merchant so they know where to focus the investigation?',
    expectedColumns: ['merchant_name', 'country', 'is_flagged', 'disputed_count', 'total_disputed'],
    expectedRowCount: 4,
    checkValues: [{ merchant_name: 'QuickTransfer', disputed_count: '2' }],
    solution: 'WITH disputed_txns AS (\n  SELECT txn_id, merchant_id, amount\n  FROM transactions\n  WHERE status = \'disputed\'\n)\nSELECT m.name AS merchant_name, m.country, m.is_flagged,\n       COUNT(d.txn_id) AS disputed_count,\n       ROUND(SUM(d.amount), 2) AS total_disputed\nFROM disputed_txns d\nJOIN merchants m ON d.merchant_id = m.merchant_id\nGROUP BY m.merchant_id, m.name, m.country, m.is_flagged\nORDER BY total_disputed DESC',
    debrief: 'What the stakeholder wants: A merchant-level dispute summary showing who is responsible for disputed transaction volume — used to prioritize fraud investigations and merchant risk reviews.\n\nAmbiguities resolved: \'Disputed transactions\' means transactions with status = \'disputed\' — not all transactions that have an entry in the disputes table (the two overlap but are not identical). Should unflagged merchants be included? Yes — even legitimate merchants can generate chargebacks (customer disputes), so include all merchants generating disputed transactions.\n\nSQL approach: A CTE filters transactions to status = \'disputed\'. The main query joins to merchants on merchant_id, groups by merchant fields, and computes COUNT(txn_id) and SUM(amount). ORDER BY total_disputed DESC surfaces highest exposure first. The is_flagged column immediately separates fraud-risk disputes from chargeback-risk disputes.\n\nWhat weak SQL looks like: Joining all transactions to merchants without the status filter — this produces all-transaction volume, which is not the same as disputed exposure. Or joining from the disputes table directly — this misses the merchant_id which lives on transactions, not disputes.\n\nInterviewer follow-up: \'Of these disputed transactions, what fraction did the merchant win vs. lose, and how does that vary by is_flagged status?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-h33',
    title: 'Creator Interaction Leaderboard',
    company: 'TikTok',
    companyDomain: 'tiktok.com',
    difficulty: 'Hard',
    tags: ['CTE', 'RANK', 'window function', 'JOIN', 'creator analytics'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 15,
    datamartId: 'consumer',
    prompt: 'The creator monetization team wants a leaderboard of our most-engaged creators — ranked by how many interactions their content is generating. Who\'s at the top and how does the rest stack up?',
    expectedColumns: ['creator_id', 'username', 'pieces_published', 'total_interactions', 'rnk'],
    expectedRowCount: 6,
    checkValues: [{ creator_id: '1', username: 'alice', total_interactions: '12', rnk: '1' }],
    solution: 'WITH creator_stats AS (\n  SELECT c.creator_id, u.username,\n         COUNT(DISTINCT c.content_id) AS pieces_published,\n         COUNT(i.interaction_id) AS total_interactions\n  FROM content c\n  JOIN interactions i ON c.content_id = i.content_id\n  JOIN users u ON c.creator_id = u.user_id\n  GROUP BY c.creator_id, u.username\n)\nSELECT creator_id, username, pieces_published, total_interactions,\n       RANK() OVER (ORDER BY total_interactions DESC) AS rnk\nFROM creator_stats\nORDER BY rnk, creator_id',
    debrief: 'What the stakeholder wants: A ranked list of creators ordered by total interactions on their content, with ties sharing the same rank — the monetization team uses this for creator tier assignment and bonus eligibility.\n\nAmbiguities resolved: \'Interactions\' means total interaction_id count across all their published content (not per piece). Should creators with zero interactions appear? The solution uses INNER JOIN — creators who have never received an interaction are excluded. If the team wants a complete roster, a LEFT JOIN is needed and those creators would rank last with 0 interactions.\n\nSQL approach: CTE joins content to interactions to users to aggregate pieces_published and total_interactions per creator. The INNER JOIN naturally drops creators with no interactions. Outer SELECT applies RANK() OVER (ORDER BY total_interactions DESC) — RANK skips numbers on ties, which is intentional here (tied creators share the same rank, and the next rank reflects the gap).\n\nWhat weak SQL looks like: ORDER BY total_interactions DESC LIMIT N without a rank function — doesn\'t handle ties and gives no rank column. Or applying RANK inside the GROUP BY CTE — SQL doesn\'t allow window functions inside GROUP BY; a two-step approach (CTE then window) is required.\n\nInterviewer follow-up: \'How would you compute each creator\'s engagement rate — interactions per piece published — and use that to re-rank rather than raw interaction count?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-h34',
    title: 'Days Between Patient Appointments',
    company: 'Doximity',
    companyDomain: 'doximity.com',
    difficulty: 'Hard',
    tags: ['LAG', 'window function', 'julianday', 'PARTITION BY', 'date arithmetic'],
    roles: ['PA', 'DA', 'BA'],
    priority: 2,
    estimatedMin: 16,
    datamartId: 'health',
    prompt: 'The care team wants to measure visit gaps for each patient. For each appointment, show patient_id, appt_id, scheduled_at, the previous appointment date for that patient (prev_appt, or NULL for first visit), and days_since_prev as an integer. Order by patient_id, then scheduled_at.',
    expectedColumns: ['patient_id', 'appt_id', 'scheduled_at', 'prev_appt', 'days_since_prev'],
    expectedRowCount: 25,
    checkValues: [{ patient_id: '1', appt_id: '11', days_since_prev: '2' }],
    solution: 'SELECT patient_id, appt_id, scheduled_at,\n       LAG(scheduled_at) OVER (PARTITION BY patient_id ORDER BY scheduled_at) AS prev_appt,\n       CAST(julianday(scheduled_at) - julianday(LAG(scheduled_at) OVER (PARTITION BY patient_id ORDER BY scheduled_at)) AS INTEGER) AS days_since_prev\nFROM appointments\nORDER BY patient_id, scheduled_at',
    debrief: 'Patient 1 had two appointments: Apr 3 and Apr 5 — just 2 days apart (a rapid follow-up). Most patients had only one appointment, so their days_since_prev is NULL. The LAG function partitioned by patient_id looks back within each patient\'s ordered history. CAST(...AS INTEGER) drops the decimal from julianday arithmetic. The weak answer uses a correlated subquery — logically correct but O(n²); the window function is a single pass. Very short gaps flag urgent follow-ups or prescription adjustments; very long gaps flag patients who may have disengaged from care.',
    sqliteNote: 'Uses julianday() for date arithmetic. In PostgreSQL: scheduled_at::date - LAG(scheduled_at::date) OVER (PARTITION BY patient_id ORDER BY scheduled_at) AS days_since_prev.',
  },

  {
    id: 'sql-h35',
    title: 'Session Conversion Rate by Source',
    company: 'Shopify',
    companyDomain: 'shopify.com',
    difficulty: 'Medium',
    tags: ['GROUP BY', 'SUM', 'ROUND', 'conversion rate', 'acquisition'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 13,
    datamartId: 'ecomm',
    prompt: 'The growth team needs a session-level conversion funnel by acquisition source. For each source, return total sessions, total conversions (converted = 1), and conv_rate as a percentage rounded to 1 decimal. Order by conv_rate descending.',
    expectedColumns: ['source', 'sessions', 'conversions', 'conv_rate'],
    expectedRowCount: 5,
    checkValues: [{ source: 'referral', sessions: '3', conversions: '3', conv_rate: '100.0' }],
    solution: 'SELECT source,\n       COUNT(session_id) AS sessions,\n       SUM(converted) AS conversions,\n       ROUND(100.0 * SUM(converted) / COUNT(session_id), 1) AS conv_rate\nFROM sessions\nGROUP BY source\nORDER BY conv_rate DESC',
    debrief: 'Referral converts at 100% (3/3), paid 71.4% (5/7), organic 55.6% (5/9), email 33.3% (1/3), social 0% (0/3). Social sessions never converted — all three came from users already browsing with no purchase intent. Referral\'s 100% rate is compelling but the sample is tiny (3 sessions); confidence intervals would be wide. The key interviewer follow-up: what is the CAC per source? A 100% conversion rate on a $500 CAC channel can still underperform a 50% rate on a $20 CAC channel. This query is the denominator for a full funnel; pairing it with revenue per conversion gives ROAS.',
    sqliteNote: null,
  },

  {
    id: 'sql-h37',
    title: 'User Total Balance Across Accounts',
    company: 'Chime',
    companyDomain: 'chime.com',
    difficulty: 'Medium',
    tags: ['JOIN', 'GROUP BY', 'SUM', 'COUNT', 'multi-account'],
    roles: ['PA', 'DA', 'BA'],
    priority: 1,
    estimatedMin: 12,
    datamartId: 'fintech',
    prompt: 'The product team wants a consolidated balance view per user across all their accounts. Join users to accounts and return user_id, email, account_count, and total_balance (sum of all account balances, rounded to 2 decimal places). Order by total_balance descending.',
    expectedColumns: ['user_id', 'email', 'account_count', 'total_balance'],
    expectedRowCount: 12,
    checkValues: [{ user_id: '3', account_count: '2' }],
    solution: 'SELECT u.user_id, u.email,\n       COUNT(a.account_id) AS account_count,\n       ROUND(SUM(a.balance), 2) AS total_balance\nFROM users u\nJOIN accounts a ON u.user_id = a.user_id\nGROUP BY u.user_id, u.email\nORDER BY total_balance DESC',
    debrief: 'User 3 (carol) has the highest total at $33,750 across two accounts — a checking ($8,750) and an investment account ($25,000). User 9 (iris) shows $0 — her only account is closed. The INNER JOIN means users without any account would be excluded. A real consolidated balance view would convert multi-currency balances to a base currency before summing — mixing USD, GBP, and AUD produces a meaningless number. Flagging multi-account users (account_count > 1) is a standard step in duplicate-account fraud detection.',
    sqliteNote: null,
  },

  {
    id: 'sql-h38',
    title: 'Referral Network Lookup',
    company: 'Airbnb',
    companyDomain: 'airbnb.com',
    difficulty: 'Hard',
    tags: ['self-join', 'LEFT JOIN', 'referral', 'network analysis'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 14,
    datamartId: 'consumer',
    prompt: 'The growth team wants to map every user to their referrer to analyze the referral network. For each user return user_id, username, referrer_id, and referred_by (the referrer\'s username, or NULL if they joined organically). Order by user_id.',
    expectedColumns: ['user_id', 'username', 'referrer_id', 'referred_by'],
    expectedRowCount: 15,
    checkValues: [{ user_id: '2', username: 'bob', referred_by: 'alice' }],
    solution: 'SELECT u.user_id, u.username, u.referrer_id,\n       r.username AS referred_by\nFROM users u\nLEFT JOIN users r ON u.referrer_id = r.user_id\nORDER BY u.user_id',
    debrief: 'The self-join resolves each referrer_id to a readable username. Eight users joined organically (referred_by IS NULL); seven were referred. Alice referred bob and david; carol referred emma; grace referred julia — a second-generation referral (carol→emma→no one, alice→bob→grace→julia→no one is the chain). For a referral program ROI model, you\'d join referred users to their orders and compare LTV vs organic cohort. A recursive CTE would trace full chain depth; this single self-join captures depth-1 attribution only.',
    sqliteNote: null,
  },

  {
    id: 'sql-h39',
    title: 'Multi-Provider Patients',
    company: 'Doximity',
    companyDomain: 'doximity.com',
    difficulty: 'Medium',
    tags: ['GROUP BY', 'HAVING', 'COUNT DISTINCT', 'care coordination'],
    roles: ['PA', 'DA', 'BA'],
    priority: 2,
    estimatedMin: 13,
    datamartId: 'health',
    prompt: 'The care coordination team flags patients with prescriptions from more than one provider — a medication reconciliation risk. Return patient_id, provider_count (distinct providers), and rx_count (total prescriptions) for all patients where provider_count > 1. Order by provider_count descending, then patient_id ascending.',
    expectedColumns: ['patient_id', 'provider_count', 'rx_count'],
    expectedRowCount: 5,
    checkValues: [{ patient_id: '1', provider_count: '2', rx_count: '2' }],
    solution: 'SELECT patient_id,\n       COUNT(DISTINCT provider_id) AS provider_count,\n       COUNT(*) AS rx_count\nFROM prescriptions\nGROUP BY patient_id\nHAVING COUNT(DISTINCT provider_id) > 1\nORDER BY provider_count DESC, patient_id ASC',
    debrief: 'Five patients have prescriptions from more than one provider: patients 1, 2, 3, 10, and 11 — all with exactly 2 providers and 2 prescriptions. Each provider may be unaware of what the other prescribed. COUNT(DISTINCT provider_id) in the HAVING clause is more precise than COUNT(*) — it targets provider diversity specifically, not just prescription volume. In a real clinical model, the next step is a drug-drug interaction check across the multi-provider prescription list. Patients on both Lisinopril and Atorvastatin (a common combination for cardiovascular risk) are a priority for reconciliation.',
    sqliteNote: null,
  },

  {
    id: 'sql-h40',
    title: 'Completed Order Value Bands',
    company: 'Amazon',
    companyDomain: 'amazon.com',
    difficulty: 'Medium',
    tags: ['CTE', 'NTILE', 'window function', 'value segmentation', 'merchandising'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 16,
    datamartId: 'ecomm',
    prompt: 'The merchandising team wants to segment completed orders into three value tiers to inform promotion strategy. How should we split our order values into low, mid, and high bands?',
    expectedColumns: ['value_band', 'order_count', 'min_subtotal', 'max_subtotal'],
    expectedRowCount: 3,
    checkValues: [],
    solution: 'WITH completed AS (\n  SELECT order_id, subtotal\n  FROM orders\n  WHERE status = \'completed\'\n),\nranked AS (\n  SELECT order_id, subtotal,\n         NTILE(3) OVER (ORDER BY subtotal) AS value_band\n  FROM completed\n)\nSELECT value_band,\n       COUNT(*) AS order_count,\n       ROUND(MIN(subtotal), 2) AS min_subtotal,\n       ROUND(MAX(subtotal), 2) AS max_subtotal\nFROM ranked\nGROUP BY value_band\nORDER BY value_band',
    debrief: 'What the stakeholder wants: Three equal-size value bands across completed orders so the merchandising team can design tier-specific promotions — free shipping nudge for low-value, upsell for mid, premium experience for high.\n\nAmbiguities resolved: \'Completed orders\' excludes cancelled and returned — WHERE status = \'completed\' is required. Band numbering: band 1 = lowest, band 3 = highest (ascending). The candidate must state this explicitly — the opposite convention (1 = highest) is equally valid but changes which band to target with promotions. Equal-size means NTILE, not fixed thresholds.\n\nSQL approach: First CTE filters to completed orders. Second CTE assigns NTILE(3) OVER (ORDER BY subtotal ASC) — band 1 = cheapest third. Outer SELECT groups by value_band, computing order count and min/max subtotal per band to show the range covered by each tier.\n\nWhat weak SQL looks like: Fixed CASE WHEN thresholds instead of NTILE — gives semantically meaningful value ranges but not equal-size groups. For capacity planning, equal-count is usually correct; for pricing analytics equal-range may be better. The candidate should ask which the team needs. Also, forgetting WHERE status = \'completed\' inflates band boundaries with cancelled high-value orders.\n\nInterviewer follow-up: \'How would you determine whether promotion response rate differs significantly across the three bands?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-h41',
    title: 'Monthly Account Growth Trend',
    company: 'Salesforce',
    companyDomain: 'salesforce.com',
    difficulty: 'Hard',
    tags: ['strftime', 'GROUP BY', 'SUM OVER', 'cumulative total', 'window function'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 14,
    datamartId: 'saas',
    prompt: 'The growth team wants to visualize how the account base has built up over time. For each month where new accounts were acquired, how many joined that month and what was the running total account count?',
    expectedColumns: ['month', 'new_accounts', 'cumulative_accounts'],
    expectedRowCount: 8,
    checkValues: [{ month: '2023-01', new_accounts: '1', cumulative_accounts: '1' }],
    solution: 'SELECT strftime(\'%Y-%m\', created_at) AS month,\n       COUNT(*) AS new_accounts,\n       SUM(COUNT(*)) OVER (ORDER BY strftime(\'%Y-%m\', created_at)) AS cumulative_accounts\nFROM accounts\nGROUP BY month\nORDER BY month',
    debrief: 'What the stakeholder wants: A month-by-month breakdown of new account additions alongside the cumulative total — used to plot an S-curve of account growth and spot acceleration or slowdown.\n\nAmbiguities resolved: \'Months where new accounts were acquired\' means only months appearing in the accounts table — months with zero additions are naturally excluded. If the team wants a complete calendar spine (including zero-new-account months), a date series generator would be needed. Cumulative total means running sum from the first month to the current one.\n\nSQL approach: GROUP BY strftime(\'%Y-%m\', created_at) gives new_accounts per month. SUM(COUNT(*)) OVER (ORDER BY month) computes the running cumulative. The nested aggregate — inner COUNT(*) from GROUP BY, outer SUM from window — is valid SQL: GROUP BY runs first, then the window function operates on those grouped results.\n\nWhat weak SQL looks like: A correlated subquery for the running total — SELECT (SELECT COUNT(*) FROM accounts WHERE created_at <= end_of_month) — logically correct but O(n squared) and doesn\'t compose cleanly with the per-month count. Or omitting ORDER BY inside OVER — without it, SUM returns the grand total on every row instead of a running sum.\n\nInterviewer follow-up: \'How would you modify this to show month-over-month growth rate as a percentage rather than an absolute count?\'',
    sqliteNote: 'Uses strftime() for date truncation. In PostgreSQL: DATE_TRUNC(\'month\', created_at)::date AS month.',
  },

  {
    id: 'sql-h42',
    title: '30-Day Transaction Velocity',
    company: 'Stripe',
    companyDomain: 'stripe.com',
    difficulty: 'Hard',
    tags: ['self-join', 'julianday', 'rolling window', 'fraud detection'],
    roles: ['PA', 'DA', 'BA'],
    priority: 1,
    estimatedMin: 18,
    datamartId: 'fintech',
    prompt: 'The fraud team wants to flag accounts with unusually high transaction frequency. For each transaction, how many other transactions did that account make in the 30 days immediately before it?',
    expectedColumns: ['txn_id', 'account_id', 'occurred_at', 'amount', 'txns_prior_30d'],
    expectedRowCount: 40,
    checkValues: [{ txn_id: '3', txns_prior_30d: '2' }],
    solution: 'SELECT t1.txn_id, t1.account_id, t1.occurred_at, t1.amount,\n       COUNT(t2.txn_id) AS txns_prior_30d\nFROM transactions t1\nLEFT JOIN transactions t2\n  ON t1.account_id = t2.account_id\n  AND t2.occurred_at < t1.occurred_at\n  AND julianday(t1.occurred_at) - julianday(t2.occurred_at) <= 30\nGROUP BY t1.txn_id, t1.account_id, t1.occurred_at, t1.amount\nORDER BY txns_prior_30d DESC, t1.account_id, t1.occurred_at',
    debrief: 'What the stakeholder wants: A per-transaction count of recent prior activity on the same account — a velocity signal the fraud team uses to surface accounts transacting at suspicious rates before triggering a review.\n\nAmbiguities resolved: \'Prior 30 days\' means strictly before the current transaction\'s occurred_at and within 30 calendar days of it (not including the current transaction). First transactions per account return 0 (not NULL) — the LEFT JOIN handles this. Whether to count across all statuses or only non-disputed is a business parameter; the solution includes all statuses.\n\nSQL approach: Self-join transactions t1 to t2 on the same account_id with two conditions: t2.occurred_at < t1.occurred_at (strictly prior) AND julianday(t1.occurred_at) - julianday(t2.occurred_at) <= 30 (within 30 days). COUNT(t2.txn_id) with LEFT JOIN returns 0 for first transactions. GROUP BY all of t1\'s fields to collapse matches.\n\nWhat weak SQL looks like: A window function with SUM OVER and a ROWS frame — intuitive but SQLite doesn\'t support INTERVAL-based RANGE frames, so the 30-day window can\'t be expressed as a RANGE directly. In PostgreSQL, COUNT(*) OVER (PARTITION BY account_id ORDER BY occurred_at RANGE BETWEEN INTERVAL \'30 days\' PRECEDING AND INTERVAL \'1 day\' PRECEDING) is more efficient.\n\nInterviewer follow-up: \'What threshold of txns_prior_30d would you flag as suspicious, and how would you decide — data-driven or policy-driven?\'',
    sqliteNote: 'Uses julianday() for the join condition date arithmetic. In PostgreSQL use: COUNT(*) OVER (PARTITION BY account_id ORDER BY occurred_at RANGE BETWEEN INTERVAL \'30 days\' PRECEDING AND INTERVAL \'1 day\' PRECEDING).',
  },

  {
    id: 'sql-h44',
    title: 'Product Category Revenue (Completed Orders)',
    company: 'Amazon',
    companyDomain: 'amazon.com',
    difficulty: 'Medium',
    tags: ['three-table JOIN', 'GROUP BY', 'SUM', 'WHERE filter', 'category revenue'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 15,
    datamartId: 'ecomm',
    prompt: 'The category management team wants revenue by product category, restricted to completed orders only. Join order_items, products, and orders, filter to completed status, and return category, items_sold (count of line items), and total_revenue (sum of unit_price, rounded to 2 decimals). Order by total_revenue descending.',
    expectedColumns: ['category', 'items_sold', 'total_revenue'],
    expectedRowCount: 4,
    checkValues: [{ category: 'electronics', items_sold: '10' }],
    solution: 'SELECT p.category,\n       COUNT(oi.item_id) AS items_sold,\n       ROUND(SUM(oi.unit_price), 2) AS total_revenue\nFROM order_items oi\nJOIN products p ON oi.product_id = p.product_id\nJOIN orders o ON oi.order_id = o.order_id\nWHERE o.status = \'completed\'\nGROUP BY p.category\nORDER BY total_revenue DESC',
    debrief: 'Electronics leads with 10 completed line items and the highest revenue, well ahead of apparel (7 items), books (5), and home (3). The three-table JOIN is the key: order_items bridges products to orders, but the status filter lives on orders. Using unit_price (price at time of purchase) rather than list_price correctly captures actual revenue — prices may have changed since the sale. Cancelled and returned line items are correctly excluded because the WHERE filters on the parent order status.',
    sqliteNote: null,
  },

  {
    id: 'sql-h45',
    title: 'Premium vs Non-Premium Engagement',
    company: 'TikTok',
    companyDomain: 'tiktok.com',
    difficulty: 'Hard',
    tags: ['LEFT JOIN', 'GROUP BY', 'COUNT DISTINCT', 'user segmentation', 'engagement'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 13,
    datamartId: 'consumer',
    prompt: 'The monetization team wants to know whether premium users actually engage more than free users. Can you compare the two groups on interaction volume?',
    expectedColumns: ['is_premium', 'user_count', 'total_interactions', 'avg_interactions_per_user'],
    expectedRowCount: 2,
    checkValues: [{ is_premium: '1', user_count: '6', total_interactions: '15' }],
    solution: 'SELECT u.is_premium,\n       COUNT(DISTINCT u.user_id) AS user_count,\n       COUNT(i.interaction_id) AS total_interactions,\n       ROUND(1.0 * COUNT(i.interaction_id) / COUNT(DISTINCT u.user_id), 2) AS avg_interactions_per_user\nFROM users u\nLEFT JOIN interactions i ON u.user_id = i.user_id\nGROUP BY u.is_premium\nORDER BY u.is_premium DESC',
    debrief: 'What the stakeholder wants: A two-row comparison of premium vs free users showing user count, total interactions, and average interactions per user — the key metric to justify the premium tier\'s engagement claim.\n\nAmbiguities resolved: All users must appear in the count, including those with zero interactions — excluding them would inflate the average for whichever group has more inactive users. \'Average interactions per user\' means total interactions divided by user count (including zero-interaction users in the denominator). GROUP BY is on is_premium (0 or 1).\n\nSQL approach: LEFT JOIN from users to interactions ensures all 15 users appear. COUNT(DISTINCT u.user_id) counts users (not interaction rows). COUNT(i.interaction_id) counts interactions, returning 0 for users with no match. avg_interactions_per_user = COUNT(i.interaction_id) / COUNT(DISTINCT u.user_id) with a 1.0 multiplier to force float division.\n\nWhat weak SQL looks like: INNER JOIN from users to interactions — drops zero-interaction users, understating non-premium user count and inflating their average. Or COUNT(*) for user count — this counts interaction rows, not users, giving a meaningless denominator.\n\nInterviewer follow-up: \'If premium users engage 50% more, how would you test whether that difference is driven by premium status itself or by selection bias — the possibility that more engaged users self-select into premium?\'',
    sqliteNote: null,
  },

  {
    id: 'sql-h47',
    title: 'Repeat Buyer Identification',
    company: 'Amazon',
    companyDomain: 'amazon.com',
    difficulty: 'Medium',
    tags: ['JOIN', 'GROUP BY', 'HAVING', 'SUM', 'repeat purchase', 'loyalty'],
    roles: ['PA', 'DA', 'PM'],
    priority: 1,
    estimatedMin: 14,
    datamartId: 'ecomm',
    prompt: 'The loyalty team defines a repeat buyer as a user with 2 or more completed orders. Return user_id, email, channel, completed_orders count, and total_spent (sum of subtotal, rounded to 2 decimals) for qualifying users. Order by completed_orders descending, then total_spent descending.',
    expectedColumns: ['user_id', 'email', 'channel', 'completed_orders', 'total_spent'],
    expectedRowCount: 6,
    checkValues: [{ user_id: '5', completed_orders: '5' }],
    solution: 'SELECT u.user_id, u.email, u.channel,\n       COUNT(o.order_id) AS completed_orders,\n       ROUND(SUM(o.subtotal), 2) AS total_spent\nFROM users u\nJOIN orders o ON u.user_id = o.user_id AND o.status = \'completed\'\nGROUP BY u.user_id, u.email, u.channel\nHAVING COUNT(o.order_id) >= 2\nORDER BY completed_orders DESC, total_spent DESC',
    debrief: 'Six users qualify as repeat buyers: user 5 (eve, paid channel) leads with 5 completed orders, followed by users 1 and 3 with 3 each, then users 8, 9, and 11 with 2 each. Filtering on the JOIN condition (AND o.status = \'completed\') is cleaner than a WHERE clause when you want HAVING to count only completed orders — a WHERE clause after the JOIN achieves the same result here but is less explicit about intent. The channel breakdown shows paid and referral users dominate repeat purchases, consistent with their higher session conversion rates.',
    sqliteNote: null,
  },

  {
    id: 'sql-h48',
    title: 'Merchant Risk and Dispute Exposure',
    company: 'Stripe',
    companyDomain: 'stripe.com',
    difficulty: 'Hard',
    tags: ['CTE', 'LEFT JOIN', 'COALESCE', 'GROUP BY', 'fraud', 'dispute rate'],
    roles: ['PA', 'DA', 'BA'],
    priority: 1,
    estimatedMin: 18,
    datamartId: 'fintech',
    prompt: 'The risk team wants a full merchant scorecard combining transaction volume with dispute exposure. For each merchant, return merchant_name, is_flagged, txn_count, total_volume, dispute_count, disputed_volume, and dispute_rate (disputes as % of transactions, rounded to 1 decimal). Order by dispute_rate descending, then total_volume descending.',
    expectedColumns: ['merchant_name', 'is_flagged', 'txn_count', 'total_volume', 'dispute_count', 'disputed_volume', 'dispute_rate'],
    expectedRowCount: 10,
    checkValues: [{ merchant_name: 'QuickTransfer', dispute_count: '2', txn_count: '2' }],
    solution: 'WITH merchant_txns AS (\n  SELECT m.merchant_id, m.name, m.is_flagged,\n         COUNT(t.txn_id) AS txn_count,\n         ROUND(SUM(t.amount), 2) AS total_volume\n  FROM merchants m\n  JOIN transactions t ON m.merchant_id = t.merchant_id\n  GROUP BY m.merchant_id, m.name, m.is_flagged\n),\nmerchant_disputes AS (\n  SELECT t.merchant_id,\n         COUNT(d.dispute_id) AS dispute_count,\n         ROUND(SUM(d.amount), 2) AS disputed_volume\n  FROM transactions t\n  JOIN disputes d ON t.txn_id = d.txn_id\n  GROUP BY t.merchant_id\n)\nSELECT mt.name AS merchant_name, mt.is_flagged,\n       mt.txn_count, mt.total_volume,\n       COALESCE(md.dispute_count, 0) AS dispute_count,\n       COALESCE(md.disputed_volume, 0) AS disputed_volume,\n       ROUND(100.0 * COALESCE(md.dispute_count, 0) / mt.txn_count, 1) AS dispute_rate\nFROM merchant_txns mt\nLEFT JOIN merchant_disputes md ON mt.merchant_id = md.merchant_id\nORDER BY dispute_rate DESC, total_volume DESC',
    debrief: 'QuickTransfer has a 100% dispute rate — both of its transactions were disputed. Delta Airlines and Shell Gas each show a 100% rate on a single disputed transaction. Amazon (9 transactions) has a 22.2% dispute rate — the disputed transactions are fraudulent card uses at a legitimate merchant. COALESCE(md.dispute_count, 0) handles merchants with zero disputes without filtering them out. The two-CTE pattern separates volume aggregation from dispute aggregation before joining — combining them in one GROUP BY would require subqueries or multiple joins.',
    sqliteNote: null,
  },

  {
    id: 'sql-h49',
    title: 'User Engagement Recency',
    company: 'TikTok',
    companyDomain: 'tiktok.com',
    difficulty: 'Medium',
    tags: ['CTE', 'MAX', 'julianday', 'recency', 'retention'],
    roles: ['PA', 'DA', 'PM'],
    priority: 2,
    estimatedMin: 14,
    datamartId: 'consumer',
    prompt: 'The retention team wants to flag dormant users. For each user who has at least one interaction, return user_id, username, last_interaction_date, and days_since_last — the integer number of days between their last interaction and the dataset reference date 2023-12-06. Order by days_since_last descending.',
    expectedColumns: ['user_id', 'username', 'last_interaction_date', 'days_since_last'],
    expectedRowCount: 13,
    checkValues: [{ user_id: '12', days_since_last: '0' }],
    solution: 'WITH last_interaction AS (\n  SELECT user_id, MAX(occurred_at) AS last_interaction_date\n  FROM interactions\n  GROUP BY user_id\n)\nSELECT u.user_id, u.username, li.last_interaction_date,\n       CAST(julianday(\'2023-12-06\') - julianday(li.last_interaction_date) AS INTEGER) AS days_since_last\nFROM users u\nJOIN last_interaction li ON u.user_id = li.user_id\nORDER BY days_since_last DESC',
    debrief: 'User 12 (lisa) had the most recent interaction on 2023-12-06 — the reference date — so her days_since_last is 0. Users with last interactions in early November show 25+ days of inactivity. Users 14 and 15 have zero interactions and are excluded by the INNER JOIN — in a full retention dashboard, LEFT JOIN and treating NULL as "never engaged" would be the correct approach. The hardcoded reference date is deliberate for this dataset; in production, replace it with MAX(occurred_at) FROM interactions or CURRENT_DATE.',
    sqliteNote: 'Uses julianday() for date arithmetic. In PostgreSQL: (\'2023-12-06\'::date - last_interaction_date::date) AS days_since_last.',
  },

  {
    id: 'sql-h50',
    title: 'Specialty Appointment Load',
    company: 'Doximity',
    companyDomain: 'doximity.com',
    difficulty: 'Medium',
    tags: ['JOIN', 'GROUP BY', 'SUM', 'ROUND', 'COUNT DISTINCT', 'specialty analytics'],
    roles: ['PA', 'DA', 'BA'],
    priority: 1,
    estimatedMin: 15,
    datamartId: 'health',
    prompt: 'Clinic leadership wants a specialty-level appointment summary for staffing decisions. Join providers to appointments and return specialty, total_appts, no_shows, no_show_rate (rounded to 1 decimal), and unique_patients seen. Order by total_appts descending.',
    expectedColumns: ['specialty', 'total_appts', 'no_shows', 'no_show_rate', 'unique_patients'],
    expectedRowCount: 5,
    checkValues: [{ specialty: 'primary_care', total_appts: '12', no_shows: '4' }],
    solution: 'SELECT p.specialty,\n       COUNT(a.appt_id) AS total_appts,\n       SUM(a.no_show) AS no_shows,\n       ROUND(100.0 * SUM(a.no_show) / COUNT(a.appt_id), 1) AS no_show_rate,\n       COUNT(DISTINCT a.patient_id) AS unique_patients\nFROM providers p\nJOIN appointments a ON p.provider_id = a.provider_id\nGROUP BY p.specialty\nORDER BY total_appts DESC',
    debrief: 'Primary care handles 12 appointments — the highest load — with a 33.3% no-show rate (4/12). Dermatology covers 4 appointments at 0% no-show; cardiology 4 appointments at 25%. Psychiatry (2 appointments, 0% no-show) has the lowest volume. COUNT(DISTINCT a.patient_id) reveals whether high appointment volume comes from repeat visits or new patients: primary care\'s 12 appointments cover 11 unique patients (one patient saw two different primary care providers). Staffing should weight both total_appts and no_show_rate — a high-volume specialty with high no-shows has unpredictable actual workload.',
    sqliteNote: null,
  },

];
